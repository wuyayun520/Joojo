
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_collectionId:[UInt8] = [0xf9,0xfe,0xf9,0xe4,0xb8,0xf3,0xff,0xf4,0xf5,0xe2,0xaa,0xb9,0xb0,0xf8,0xf1,0xe3,0xb0,0xfe,0xff,0xe4,0xb0,0xf2,0xf5,0xf5,0xfe,0xb0,0xf9,0xfd,0xe0,0xfc,0xf5,0xfd,0xf5,0xfe,0xe4,0xf5,0xf4]

private func generateUserRed(don num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "roomId" :*/
fileprivate let userSceneString:[Character] = ["r","o","o","m","I","d"]

/*: "type" :*/
fileprivate let show_portedString:[UInt8] = [0x62,0x6f,0x66,0x73]

private func weakCrucial(appeal num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "extra" :*/
fileprivate let show_statusName:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let data_familyData:[UInt8] = [0x44,0x5b,0x43,0x5e,0x65,0x4b,0x57]

/*: "Send Gift" :*/
fileprivate let main_rootFormat:[Character] = ["S","e","n","d"," ","G"]
fileprivate let notiQuestionPath:[Character] = ["i","f","t"]

/*: "View info" :*/
fileprivate let noti_sessionBigOddName:String = "View imemory certain guide"
fileprivate let k_rewardData:String = "highlighto"

/*: "Report" :*/
fileprivate let appSureId:String = "Reportsibling agile minute material"

/*: "uid" :*/
fileprivate let const_partDeviceValue:[UInt8] = [0x8f,0x83,0x7e]

fileprivate func bitQuery(mystery num: UInt8) -> UInt8 {
    let value = Int(num) - 26
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "name" :*/
fileprivate let userContactUrl:[UInt8] = [0xed,0xe2,0xee,0xe6]

private func whoDatabase(limit num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "Can't send blank message" :*/
fileprivate let app_cocaString:String = "Can\'"
fileprivate let mainDisabledTitle:String = " blankexist display private"
fileprivate let mainCorrectKey:[Character] = [" ","m"]
fileprivate let notiFatalSaltTitle:String = "ESSAGE"

/*: "user" :*/
fileprivate let showNoAlreadyConnectString:String = "usesufficient"

/*: "content" :*/
fileprivate let show_whatUrl:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let mainQuestionStr:String = "properly every trigger asmsgType"

/*: "txt" :*/
fileprivate let main_niteMsg:String = "trockett"

/*: "isGroup" :*/
fileprivate let const_destroyName:String = "IS"
fileprivate let main_insteadFormat:[Character] = ["G","r","o","u","p"]

/*: "jumps" :*/
fileprivate let mainEqualUrl:String = "jucocas"

/*: "message" :*/
fileprivate let dataPreviousValue:[UInt8] = [0x40,0x48,0x5e,0x5e,0x4c,0x4a,0x48]

private func presentationCommit(eff num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "TxtMsg" :*/
fileprivate let mainRoundString:String = "TxtMsgappearance opportunity"

/*: "toUid" :*/
fileprivate let notiHighlightMessage:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let user_masterData:String = "msgInfogreet hurry remote no salt"

/*: "params" :*/
fileprivate let k_supportCrucialPath:[UInt8] = [0xc1,0xd0,0xc3,0xd0,0xdc,0xc2]

private func netText(reserve num: UInt8) -> UInt8 {
    return num ^ 177
}

/*: "contentType" :*/
fileprivate let const_signKey:[UInt8] = [0x48,0x44,0x45,0x5f,0x4e,0x45,0x5f,0x7f,0x52,0x5b,0x4e]

private func memberPrevention(find num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "ImgMsg" :*/
fileprivate let mainTailKey:[Character] = ["I","m","g","M","s"]
fileprivate let k_bellyGeneticMsg:[Character] = ["g"]

/*: "img" :*/
fileprivate let kCompositionName:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let showDeviceStr:[UInt8] = [0x6b,0x6f,0x63,0x69,0x67,0x57,0x74,0x6b]

fileprivate func kingListen(value num: UInt8) -> UInt8 {
    let value = Int(num) + 254
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let app_importantUrl:String = "padditional"

/*: "msgId" :*/
fileprivate let dataSwitcherPetValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let user_replacementUrl:String = "totalMmotive chart substantial origin enough"
fileprivate let show_processingStr:String = "fCoinminimize public"

/*: "giftId" :*/
fileprivate let kSourceKey:String = "while less linkgiftId"

/*: "giftNum" :*/
fileprivate let show_logUnderTitle:String = "giftNprotocol prosecution entity scale main"
fileprivate let k_artAdvancedId:String = "corruptionm"

/*: "pkgItemsetId" :*/
fileprivate let mainGeneralString:[Character] = ["p","k","g","I","t","e","m","s","e","t","I"]
fileprivate let const_densityPath:String = "vertical"

/*: "msg" :*/
fileprivate let user_stepFormat:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let main_belowMissionValue:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let dataOptionUrl:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let k_listenPath:String = "NICK"

/*: "headPic" :*/
fileprivate let constCompleteMessage:[UInt8] = [0x6f,0x62,0x66,0x63,0x57,0x6e,0x64]

private func totalerestYeBySave(even num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "All Numbers" :*/
fileprivate let mainEntryPath:String = "absolute line invitation appearanceAl"
fileprivate let dataTextureExercisePath:[Character] = ["l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let show_documentPath:String = "sendNmagnitude race"
fileprivate let constRegulateLowMessage:[Character] = ["u","m"]

/*: "toUser" :*/
fileprivate let notiKeepMsg:String = "destroy child it runningtoUser"

/*: "msgIncome" :*/
fileprivate let showCouldHelloString:[Character] = ["m","s","g","I","n","c","o"]
fileprivate let appSpanDestroyName:String = "ME"

/*: "isConnection" :*/
fileprivate let show_capacityKey:[Character] = ["i","s","C"]
fileprivate let data_secretMsg:String = "ONNECTION"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PageChallengeClip.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class PageChallengeClip: TrainBrush {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = PushLibrary() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = DistinctionLocation() // 会话cellData
    //: private var conversationType: UserChatType = .group
    private var conversationType: UserChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = PushLibrary.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_collectionId.map{generateUserRed(don: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
            if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = DetailView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        documentSplit()
        //: createUIConstraints()
        everySetSimilar()
        //: bindInteraction()
        instructionSink()
        //: addTapGestureRecognizer()
        projectionWindow()
        //: func__chatRoom_insertTipMsg()
        doMargin()
        //: chatSettings()
        warning()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            attention()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.agreeBetween(join: false)
        //: self.destroyTipsTimer()
        self.logSkinLocation()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: CounteractionTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = CounteractionTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(MessageResponse.self, forCellReuseIdentifier: MessageResponse.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ExecuteSystemVertical.self, forCellReuseIdentifier: ExecuteSystemVertical.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(EqualSubsequent.self, forCellReuseIdentifier: EqualSubsequent.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(TemporaryView.self, forCellReuseIdentifier: TemporaryView.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ImplementProperty.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(AtSkipThread.self, forCellReuseIdentifier: ImplementProperty.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: HoldView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = HoldView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: SpineView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = SpineView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: MethodAll = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = MethodAll()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: CustomView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = CustomView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: HandleView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = HandleView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    /// 群聊设置
    //: private func chatSettings() {
    private func warning() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ElementUnder.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        agreeBetween(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func agreeBetween(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(userSceneString)): self.conversationData.groupID, String(bytes: show_portedString.map{weakCrucial(appeal: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        NameProjection.boardLine(params: params) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - CouldExitSource

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension PageChallengeClip: CouldExitSource {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EmbraceShadow! {
    func message(_ controller: CounteractionTableViewController!, rounding data: V2TIMMessage!) -> EmbraceShadow! {
        //: let dic = CompareOddSecond.parseTXMessageData(data: data.customElem.data)
        let dic = CompareOddSecond.activity(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = AgainstName.need(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EmbraceShadow!) -> ImplementProperty! {
    func reload(_ controller: CounteractionTableViewController!, embraceDown data: EmbraceShadow!) -> ImplementProperty! {
        //: if data.isKind(of: CoordinatorBasicOff.self) {
        if data.isKind(of: CoordinatorBasicOff.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = MessageResponse(style: .default, reuseIdentifier: MessageResponse.className())
            //: let cellData = data as! CoordinatorBasicOff
            let cellData = data as! CoordinatorBasicOff
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.advancedPan(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.distributionFuture(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: WantText.self) {
        if data.isKind(of: WantText.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ExecuteSystemVertical(style: .default, reuseIdentifier: ExecuteSystemVertical.className())
            //: let cellData = data as! WantText
            let cellData = data as! WantText
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: BesideParticle.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = EqualSubsequent(style: .default, reuseIdentifier: EqualSubsequent.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! BesideParticle
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: RegulateThan.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = TemporaryView(style: .default, reuseIdentifier: TemporaryView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! RegulateThan
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ImplementProperty!) {
    func pic(_ controller: CounteractionTableViewController!, immediateDrawing cell: ImplementProperty!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(ChartShowSceneScientific.share.loginUid) {
            if userModel.uid == Int(ChartShowSceneScientific.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                advancedPan(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                limit(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ImplementProperty!) {
    func over(_ controller: CounteractionTableViewController!, tagUponDataOf cell: ImplementProperty!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ExecuteSystemVertical.self) { // 图片
            //: let imageCellData = cell.messageData as! WantText
            let imageCellData = cell.messageData as! WantText
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(user_availableValue)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = SendSmall()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = FinishEdge()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [SendSmall] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = DistributionNorm(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.chemist()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: MessageResponse.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! MessageResponse
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = CompareOddSecond.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = CompareOddSecond.activity(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(show_statusName))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: data_familyData.map{$0^46}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AlwaysFit>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BeforeAssociate.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BeforeAssociate.share.nearEconomical(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TemporaryView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TemporaryView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = CompareOddSecond.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = CompareOddSecond.activity(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(show_statusName))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: data_familyData.map{$0^46}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AlwaysFit>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BeforeAssociate.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BeforeAssociate.share.nearEconomical(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ImplementProperty!) {
    func lastSkirt(_ controller: CounteractionTableViewController!, information cell: ImplementProperty!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        flow(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func logSkinLocation() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func limit(user: ActiveModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = TailView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.playAltogether(cellTitleList: [(String(main_rootFormat) + String(notiQuestionPath)).localized, "@", (String(noti_sessionBigOddName.prefix(6)) + k_rewardData.replacingOccurrences(of: "highlight", with: "nf")).localized, (String(appSureId.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.someEach(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.great(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.advancedPan(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.jump(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func someEach(user: ActiveModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.totalerval()
        //: let model = TalkingChatRoomMemberModel()
        let model = RomanMagnitudeerval()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.liveCalculate(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func great(user: ActiveModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: const_partDeviceValue.map{bitQuery(mystery: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: userContactUrl.map{whoDatabase(limit: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.menu(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func advancedPan(uid: Int) {
        //: BeforeAssociate.share.func__pushToUserDetailVC(uid: String(uid))
        BeforeAssociate.share.outsideTechnique(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func jump(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = EagerView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.obtainButton(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func bit(msgStr: String, quoteModel: FrameworkTimePlainModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            bury(showMsg: (app_cocaString + "t send" + String(mainDisabledTitle.prefix(6)) + String(mainCorrectKey) + notiFatalSaltTitle.lowercased()).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: data_familyData.map{$0^46}, encoding: .utf8)!: (showNoAlreadyConnectString.replacingOccurrences(of: "sufficient", with: "r")), String(bytes: const_partDeviceValue.map{bitQuery(mystery: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: show_whatUrl.reversed(), encoding: .utf8)!: temStr, (String(mainQuestionStr.suffix(7))): (main_niteMsg.replacingOccurrences(of: "rocket", with: "x")), (const_destroyName.lowercased() + String(main_insteadFormat)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(mainEqualUrl.replacingOccurrences(of: "coca", with: "mp"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: dataPreviousValue.map{presentationCommit(eff: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: show_portedString.map{weakCrucial(appeal: $0)}, encoding: .utf8)!: (String(mainRoundString.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(userSceneString)): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(notiHighlightMessage))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(user_masterData.prefix(7))): msgInfo, String(bytes: k_supportCrucialPath.map{netText(reserve: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            flow(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func panAndUser(img: UIImage) {
        //: let imagePath = WantText.getChatImagePath(image: img)
        let imagePath = WantText.consumption(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: const_signKey.map{memberPrevention(find: $0)}, encoding: .utf8)!: (String(mainTailKey) + String(k_bellyGeneticMsg)), (String(mainQuestionStr.suffix(7))): (String(kCompositionName)), String(bytes: showDeviceStr.map{kingListen(value: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(app_importantUrl.replacingOccurrences(of: "additional", with: "ic")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: show_portedString.map{weakCrucial(appeal: $0)}, encoding: .utf8)!: (String(mainTailKey) + String(k_bellyGeneticMsg)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(userSceneString)): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(user_masterData.prefix(7))): msgInfo, String(bytes: k_supportCrucialPath.map{netText(reserve: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            flow(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EmbraceShadow, reSend: Bool) {
    private func flow(msgData: EmbraceShadow, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bury(showMsg: show_plainHalfName)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.searched(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((app_importantUrl.replacingOccurrences(of: "additional", with: "ic"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(app_importantUrl.replacingOccurrences(of: "additional", with: "ic"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(app_importantUrl.replacingOccurrences(of: "additional", with: "ic"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        NameProjection.mend(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.analyze(msgData, field: nil, action: NSNumber(value: 0), responsePresent: .Msg_Status_Fail)
 
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.construct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(user_masterData.prefix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.analyze(msgData, field: msgInfo[(String(dataSwitcherPetValue))] as? String, action: NSNumber(value: 0), responsePresent: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(user_replacementUrl.prefix(6)) + String(show_processingStr.prefix(5)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(user_replacementUrl.prefix(6)) + String(show_processingStr.prefix(5)))] as! NSNumber
                //: ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func construct(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard ChartShowSceneScientific.share.loginUserMode.status != 1 else {
            guard ChartShowSceneScientific.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    bury(showMsg: errorStr)
                }
                //: return
                return
            }

            //: BeforeAssociate.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            BeforeAssociate.share.computeEstimated(clickEvent: noti_evenProviderActionStr, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.analyze()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            afterMap(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = PriorityView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
            //: view.show()
            view.beforeEqually()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                bury(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension PageChallengeClip: EaseSmart {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func pair(giftModel: TierBrush, giftNum: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func too() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func icon(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = AroundUndertake()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.menu(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func ready(actionType: SymbolProcessor) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            connectDown()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func each(giftModel: TierBrush, giftNum: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func pressure(giftModel: TierBrush, giftNum: String, model: RomanMagnitudeerval) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bury(showMsg: show_plainHalfName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(notiHighlightMessage))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(kSourceKey.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(show_logUnderTitle.prefix(5)) + k_artAdvancedId.replacingOccurrences(of: "corruption", with: "u"))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(userSceneString))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(mainGeneralString) + const_densityPath.replacingOccurrences(of: "vertical", with: "d"))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        NameProjection.towardHiAround(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.construct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.cropManual(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.point(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func trigger(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.sample(true)
            self.msgView.sample(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func boxAlong(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.sample(true)
            self.msgView.sample(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func coverMomentum(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.coverMomentum(tap)
    }

    //: func func__starRecordBtnDown() {}
    func thinRe() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func image(voicePath: String, voiceTime: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension PageChallengeClip: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func connectDown() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ContainPlate.send(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = AccessiblePop.descriptionBy(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: BeforeAssociate.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                BeforeAssociate.share.mare()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.panAndUser(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func mightPending() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ContainPlate.sharkThen(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.panAndUser(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func statProvide(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(userSceneString))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    //: func func__TipsNotification() {
    func directly() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(package(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: k_transitionMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func package(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: RegulateThan = userinfo[(String(user_stepFormat))] as! RegulateThan

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.sample(true)
        self.msgView.sample(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func afterMap(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: show_whatUrl.reversed(), encoding: .utf8)!: tipStr, (String(mainQuestionStr.suffix(7))): (String(main_belowMissionValue))]
        //: var message: EmbraceShadow?
        var message: EmbraceShadow?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = AgainstName.policyPan(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.sample(true)
        self.msgView.sample(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func doMargin() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] time in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard ChartShowSceneScientific.share.appUserConfigMode.rTips.count > 0 else { return }
            guard ChartShowSceneScientific.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: ChartShowSceneScientific.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.afterMap(tipStr: ChartShowSceneScientific.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func point(extral: [String: Any?], model: RomanMagnitudeerval, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(user_masterData.prefix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(const_destroyName.lowercased() + String(main_insteadFormat))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(mainQuestionStr.suffix(7)))] = (String(dataOptionUrl))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(user_masterData.prefix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: const_partDeviceValue.map{bitQuery(mystery: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (k_listenPath.lowercased() + "name"): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: constCompleteMessage.map{totalerestYeBySave(even: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(mainEntryPath.suffix(2)) + String(dataTextureExercisePath)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(dataOptionUrl))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(dataOptionUrl))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(k_listenPath.lowercased() + "name")] = "\(giftdic[(String(show_documentPath.prefix(5)) + String(constRegulateLowMessage))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(notiKeepMsg.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = AgainstName.policyPan(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.searched(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(showCouldHelloString) + appSpanDestroyName.lowercased())) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(showCouldHelloString) + appSpanDestroyName.lowercased())] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.analyze(cellData, field: msgInfoDic[(String(dataSwitcherPetValue))] as? String, action: msgIncome, responsePresent: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        underTitle(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(user_replacementUrl.prefix(6)) + String(show_processingStr.prefix(5)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(user_replacementUrl.prefix(6)) + String(show_processingStr.prefix(5)))] as! NSNumber
            //: ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: ChartShowSceneScientific.share.loginUserMode.mf_coin)
            msgInputView.giftView.reliability(needReload: false, mf_coin: ChartShowSceneScientific.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.maintain()
    }

    //: func initCachEffectData() {
    func attention() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.blackTo(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func underTitle(giftDic: [String: Any], model: RomanMagnitudeerval) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.successfully(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in StageMargin.share.smartTo() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [BackgroundPlatform]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.plainFlag(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.appear(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func omitControl(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in StageMargin.share.smartTo() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [BackgroundPlatform]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.plainFlag(model: giftModel as! IndexInput)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.gestureComprehensive(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension PageChallengeClip {
    /// 创建UI
    //: private func createUI() {
    private func documentSplit() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func everySetSimilar() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(dataSuccessResponseMessage)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(dataSuccessResponseMessage)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - const_controlData)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func instructionSink() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = AroundUndertake()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! EmbraceShadow).isKind(of: CoordinatorBasicOff.self) {
                    if (cellData as! EmbraceShadow).isKind(of: CoordinatorBasicOff.self) {
                        //: let groupCellData = cellData as! CoordinatorBasicOff
                        let groupCellData = cellData as! CoordinatorBasicOff
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(dataSwitcherPetValue))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: InputDrawing.shared.rx
        InputDrawing.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(show_capacityKey) + data_secretMsg.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
