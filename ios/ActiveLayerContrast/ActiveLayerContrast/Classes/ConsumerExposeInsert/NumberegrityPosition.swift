
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let show_densityMessage:[UInt8] = [0x43,0x52,0x52,0x4e,0x47,0x5d,0x4b,0x43,0x52,0x5d,0x52,0x43,0x5b,0x5d,0x51,0x57,0x41,0x41,0x47,0x47,0x46,0x5d,0x4c,0x4d,0x56,0x4b,0x44,0x4b,0x41,0x43,0x56,0x4b,0x4d,0x4c]

private func presentDoingce(cycle num: UInt8) -> UInt8 {
    return num ^ 2
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let showMarkMediumMsg:[UInt8] = [0xe9,0xf8,0xf8,0xf4,0xed,0x7,0xf1,0xe9,0xf8,0x7,0xfb,0xfd,0xea,0xfb,0xeb,0xfa,0xf1,0xea,0xed,0x7,0xfb,0xfd,0xeb,0xeb,0xed,0xed,0xec,0x7,0xf6,0xf7,0xfc,0xf1,0xee,0xf1,0xeb,0xe9,0xfc,0xf1,0xf7,0xf6]

fileprivate func flowSituation(sen num: UInt8) -> UInt8 {
    let value = Int(num) - 168
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let constEditData:[Character] = ["m","f","/","r","e","c","h","a","r","g","e","/","c","r","e","a","t","e","A","p"]
fileprivate let constHusbandTitle:[Character] = ["p"]
fileprivate let app_tingTitle:String = "blank automaticlePay"

/*: "productId" :*/
fileprivate let user_statusData:[Character] = ["p","r","o"]
fileprivate let dataImmediateName:[Character] = ["d","u","c","t","I","d"]

/*: "source" :*/
fileprivate let show_structureFormat:String = "attentionurce"

/*: "IAP充值创建订单失败: :*/
fileprivate let user_placeMessage:[Character] = ["I","A","P","\u{5145}","值","创","建","\u{8ba2}","\u{5355}","\u{5931}","败",":"]

/*: "orderNum" :*/
fileprivate let data_complaintMessage:String = "ordabsolutely"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let notiThenSelectionFormat:String = "provide wrap destinationmf/rec"
fileprivate let user_publisherName:[Character] = ["h","a","r","g"]
fileprivate let constRestrictionDraftPath:[Character] = ["e","/","a","p","p","l","e","P","a","y","N","o","t","i","f","y"]

/*: "IAP充值校验失败: :*/
fileprivate let userDirectPath:[Character] = ["I","A","P","充","值","校","\u{9a8c}","失","败",":"]

/*: "transactionId" :*/
fileprivate let k_thanData:[Character] = ["t","r","a","n","s","a","c","t","i","o","n","I","d"]

/*: "reportMoney" :*/
fileprivate let mainCounteractionStr:String = "REPO"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let noti_anonymousListenerData:String = "mf/Auttwo race"
fileprivate let show_watchMessage:String = "task writ task dense cancelApple"
fileprivate let app_largeValue:[Character] = ["C"]
fileprivate let userPressureMsg:String = "rweeklyat"

/*: "IAP订阅创建订单失败: :*/
fileprivate let kChemistEggPath:String = "IAP\u{8ba2}"
fileprivate let noti_freezeStr:String = "阅\u{521b}建\u{8ba2}"

/*: "orderId" :*/
fileprivate let data_sineKey:[UInt8] = [0xcd,0xd0,0xc2,0xc3,0xd0,0xa7,0xc2]

fileprivate func refuseNo(boss num: UInt8) -> UInt8 {
    let value = Int(num) - 94
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let kErmineOurKey:String = "mf/Audisabled decision additional gap"
fileprivate let showDisplayKey:String = "ApplePaySsen expression"
fileprivate let app_appropriateName:[Character] = ["u","c","c","e","s","s"]

/*: "IAP订阅校验失败: :*/
fileprivate let kChartMsg:String = "IAP\u{8ba2}\u{9605}校"

/*: "App" :*/
fileprivate let showValidKey:String = "Appobject lat loop"

/*: "OrderTransactionInfo_ :*/
fileprivate let show_meetingName:[Character] = ["O","r","d","e","r","T","r","a","n","s","a","c","t","i","o","n","I","n","f","o"]
fileprivate let kAskFormat:[Character] = ["_"]

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let app_boaTitle:[UInt8] = [0x99,0xa4,0xb2,0xb3,0xa4,0x82,0xa4,0xb7,0xb8,0xa5,0xb7,0xb5,0xa2,0xbf,0xb9,0xb8,0x9f,0xb8,0xb0,0xb9,0x89,0x85,0xa3,0xb4,0xa5,0xb5,0xa4,0xbf,0xb4,0xb3,0x89]

/*: "verifyData" :*/
fileprivate let data_blueString:[UInt8] = [0x30,0x23,0x34,0x2f,0x20,0x3f,0x2,0x27,0x32,0x27]

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let dataTempKey:[Character] = ["I","A","P","查","询","内","购","商","\u{54c1}","\u{5931}","败",","," ","p","r","o","d","u"]
fileprivate let notiClearHelpData:String = "package large decrease domainctId:"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let mainMiniPath:[Character] = ["I","A","P","查","询"]
fileprivate let userStatMessage:String = "内购beauty"
fileprivate let k_fellowMineString:[Character] = ["u","c","t","s",".","c","o","u","n","t","为","0","."]

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let data_expressionKey:String = "IAP查quantity secret this want"
fileprivate let show_conversionKey:String = "败, Erturn judge skip hidden"
fileprivate let dataGemFormat:String = "ror:sty by portrait implementation quit"

/*: "IAP-purchased: :*/
fileprivate let show_enoughString:String = "presentation near bill lose beIAP-p"
fileprivate let user_yaData:[Character] = ["e","d",":"]

/*: , 订单号: :*/
fileprivate let notiYellowInvitationKey:[Character] = [","," ","\u{8ba2}","单","号",":"]

/*: , 商品Id: :*/
fileprivate let appEqualValue:String = "pi copy editor oval, 商品I"
fileprivate let show_forwardStr:[Character] = ["d",":"]

/*: , 苹果transId: :*/
fileprivate let dataFactoryPath:[Character] = [","," ","苹","果","t","r","a","n","s","I","d",":"]

/*: "IAP-failed: :*/
fileprivate let app_watchMsg:String = "IAP-fawhole sink session build full"
fileprivate let constBedMsg:String = "iled:reward earn drawing multiple"

/*: "IAP-deferred: :*/
fileprivate let const_issueValidMsg:String = "share anonymous primarily confirmIAP-d"
fileprivate let const_valueValue:String = "red:reject until box eh special"

/*: "IAP未知交易类型: :*/
fileprivate let userInsteadStr:String = "IAP未framework ported very therefor"
fileprivate let data_rapidKey:String = "知交\u{6613}类型:"

/*: " 未知的交易类型" :*/
fileprivate let data_albumUniversalCharacterTitle:String = "capable"
fileprivate let main_manageStrengthMsg:String = " 未知\u{7684}\u{4ea4}易类\u{578b}"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let userCorruptionMessage:String = "IAP获取\u{672c}"
fileprivate let notiReplaceStr:String = "据second,"
fileprivate let constHarassmentOccasionValue:[Character] = [" ","t","r","a","n"]
fileprivate let kIlkPath:[Character] = ["s","a","c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let userDoingSwitchceData:[Character] = [","," ","o","r","d","e"]
fileprivate let data_happyStr:[Character] = ["r","I","d",":"]

/*: , payType: :*/
fileprivate let showBeId:[Character] = [","," "]
fileprivate let show_backData:[Character] = ["p","a","y","T","y","p","e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NumberegrityPosition.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let constYieldSkipMaximumFormat = NSNotification.Name(rawValue: String(bytes: show_densityMessage.map{presentDoingce(cycle: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let constRespectiveHeapPath = NSNotification.Name(rawValue: String(bytes: showMarkMediumMsg.map{flowSituation(sen: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let userProcessorMsg = 9

/// 支付类型
//: enum ApplePayType {
enum AssociateRange {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum CancelBefore: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (CancelBefore, Double, AssociateRange) -> Void

//: class AppleIAPManager: NSObject {
class NumberegrityPosition: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: AssociateRange = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = NumberegrityPosition()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(vidSize),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func vidSize() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension NumberegrityPosition {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func bed(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(constEditData) + String(constHusbandTitle) + String(app_tingTitle.suffix(5)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(user_statusData) + String(dataImmediateName))] = productId
        //: dict["source"] = source
        dict[(show_structureFormat.replacingOccurrences(of: "attention", with: "so"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                KitTolerance.evenIndex(msg: (String(user_placeMessage)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(data_complaintMessage.replacingOccurrences(of: "absolutely", with: "e") + "rNum")].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func quantityeraction(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(notiThenSelectionFormat.suffix(6)) + String(user_publisherName) + String(constRestrictionDraftPath))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    KitTolerance.evenIndex(msg: (String(userDirectPath)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.yearFrame(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(String(k_thanData))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.name()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: constYieldSkipMaximumFormat, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(mainCounteractionStr.lowercased() + "rtMoney")].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension NumberegrityPosition {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func sequenceUponExtend(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(noti_anonymousListenerData.prefix(6)) + "oSub/" + String(show_watchMessage.suffix(5)) + String(app_largeValue) + userPressureMsg.replacingOccurrences(of: "weekly", with: "e") + "eOrder")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(user_statusData) + String(dataImmediateName))] = productId
        //: dict["source"] = source
        dict[(show_structureFormat.replacingOccurrences(of: "attention", with: "so"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                KitTolerance.evenIndex(msg: (kChemistEggPath + noti_freezeStr + "单失\u{8d25}:") + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: data_sineKey.map{refuseNo(boss: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func restriction(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(kErmineOurKey.prefix(5)) + "toSub/" + String(showDisplayKey.prefix(9)) + String(app_appropriateName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    KitTolerance.evenIndex(msg: (kChartMsg + "\u{9a8c}\u{5931}败:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.yearFrame(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(String(k_thanData))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.belowOverwhelm()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: constRespectiveHeapPath, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(mainCounteractionStr.lowercased() + "rtMoney")].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension NumberegrityPosition {
    /// 初始化数据
    //: private func iap_initData() {
    private func everyBuild() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = hiOnRow(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = hiOnRow(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func hiOnRow(payType: AssociateRange) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = name()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = belowOverwhelm()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func name() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(showValidKey.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = show_modelHideFormat.string(forKey: data_insteadTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(show_meetingName) + String(kAskFormat)) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func belowOverwhelm() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(showValidKey.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = show_modelHideFormat.string(forKey: data_insteadTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: app_boaTitle.map{$0^214}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func noMutual(_ transactionId: String, _ payType: AssociateRange) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(String(k_thanData))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(String(k_thanData))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: data_blueString.map{$0^70}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: data_blueString.map{$0^70}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension NumberegrityPosition {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func put() {
        //: iap_initData()
        everyBuild()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            bothUponDocument(dict[(String(k_thanData))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            bothUponDocument(dict[(String(k_thanData))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func bothUponDocument(_ transactionId: String?, _ payType: AssociateRange) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        yearFrame(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension NumberegrityPosition {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func easternNext(productId: String, payType: AssociateRange, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        everyBuild()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            bed(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.queryed(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            sequenceUponExtend(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.queryed(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func queryed(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            KitTolerance.evenIndex(msg: (String(dataTempKey) + String(notiClearHelpData.suffix(5))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.expose()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func expose() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension NumberegrityPosition: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            KitTolerance.evenIndex(msg: (String(mainMiniPath) + userStatMessage.replacingOccurrences(of: "beauty", with: "商品") + "失败, prod" + String(k_fellowMineString)))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_ request: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        KitTolerance.evenIndex(msg: (String(data_expressionKey.prefix(4)) + "询内购商品\u{5931}" + String(show_conversionKey.prefix(5)) + String(dataGemFormat.prefix(4))) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_ request: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension NumberegrityPosition: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    KitTolerance.evenIndex(msg: (String(show_enoughString.suffix(5)) + "urchas" + String(user_yaData)) + "\(currentPayType)" + (String(notiYellowInvitationKey)) + "\(String(describing: createOrderId))" + (String(appEqualValue.suffix(5)) + String(show_forwardStr)) + "\(transaction.payment.productIdentifier)" + (String(dataFactoryPath)) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    yearFrame(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                KitTolerance.evenIndex(msg: (String(app_watchMsg.prefix(6)) + String(constBedMsg.prefix(5))) + "\(currentPayType)" + (String(notiYellowInvitationKey)) + "\(String(describing: createOrderId))" + (String(appEqualValue.suffix(5)) + String(show_forwardStr)) + "\(transaction.payment.productIdentifier)" + (String(dataFactoryPath)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = PermanentView()
                //: vc.show()
                vc.prosecutionDown()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                KitTolerance.evenIndex(msg: (String(const_issueValidMsg.suffix(5)) + "efer" + String(const_valueValue.prefix(4))) + "\(currentPayType)" + (String(notiYellowInvitationKey)) + "\(String(describing: createOrderId))" + (String(appEqualValue.suffix(5)) + String(show_forwardStr)) + "\(transaction.payment.productIdentifier)" + (String(dataFactoryPath)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                KitTolerance.evenIndex(msg: (String(userInsteadStr.prefix(4)) + data_rapidKey) + "\(currentPayType)" + (String(notiYellowInvitationKey)) + "\(String(describing: createOrderId))" + (String(appEqualValue.suffix(5)) + String(show_forwardStr)) + "\(transaction.payment.productIdentifier)" + (String(dataFactoryPath)) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((data_albumUniversalCharacterTitle.replacingOccurrences(of: "capable", with: "") + main_manageStrengthMsg))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func yearFrame(_ transactionId: String, _ payType: AssociateRange) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = noMutual(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            KitTolerance.evenIndex(msg: (userCorruptionMessage + "地收据数" + notiReplaceStr.replacingOccurrences(of: "second", with: "失败") + String(constHarassmentOccasionValue) + String(kIlkPath)) + "\(transactionId)" + (String(userDoingSwitchceData) + String(data_happyStr)) + "\(String(describing: createOrderId))" + (String(showBeId) + String(show_backData)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(String(k_thanData))] == transactionId || $0[String(bytes: data_sineKey.map{refuseNo(boss: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(String(k_thanData)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: data_sineKey.map{refuseNo(boss: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: data_blueString.map{$0^70}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.name()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(String(k_thanData))] == transactionId || $0[String(bytes: data_sineKey.map{refuseNo(boss: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(String(k_thanData)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: data_sineKey.map{refuseNo(boss: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: data_blueString.map{$0^70}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.belowOverwhelm()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > userProcessorMsg {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(String(k_thanData))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            quantityeraction(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(String(k_thanData))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            restriction(transactionId, params: paramsArr.first!)
        }
    }
}
