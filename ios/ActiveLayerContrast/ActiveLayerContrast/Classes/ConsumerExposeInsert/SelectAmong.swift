
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let k_padTitle:[Character] = ["b","g","_","m","e","s","s","a","g","e","_","t","o","p"]

/*: "777777" :*/
fileprivate let constBrownUrl:String = "script"
fileprivate let main_poolStr:[Character] = ["7","7","7","7","7"]

/*: "Messages" :*/
fileprivate let kJumpId:[Character] = ["M","e","s","s","a","g"]
fileprivate let app_textureContinuingId:[Character] = ["e","s"]

/*: "Who like me" :*/
fileprivate let appInvisibleUrl:[Character] = ["W","h","o"," ","l","i","k"]
fileprivate let dataViaPath:String = "expected prevention invitee me"

/*: "Call" :*/
fileprivate let notiGivingTitle:String = "Callreplacement category speaker body"

/*: "#FF2348" :*/
fileprivate let main_unableMightPath:String = "identity appointed command hundred enter#FF2348"

/*: "Current network unavailable" :*/
fileprivate let k_placeAccessId:String = "where recommend languageCurren"
fileprivate let const_actionKey:[Character] = [" ","u","n","a","v","a","i","l","a","b","l","e"]

/*: "icon_yidu_pre" :*/
fileprivate let noti_boyStr:String = "icon_yidmic session until"
fileprivate let data_identifyUrl:[Character] = ["u","_","p","r","e"]

/*: "transform.rotation" :*/
fileprivate let showPresentationMessage:[Character] = ["t","r","a","n","s","f","o","r","m",".","r"]
fileprivate let k_accountName:String = "OTATION"

/*: "transform.scale" :*/
fileprivate let showGemValue:String = "scriptansf"
fileprivate let showNeedHeadKey:[Character] = ["o","r","m",".","s","c","a","l","e"]

/*: "zoom&shake" :*/
fileprivate let k_groupFormat:String = "zoom&shakread no"
fileprivate let data_easterAdminListenerUrl:String = "E"

/*: "yyyy-MM-dd" :*/
fileprivate let user_hiddenPressureData:[Character] = ["y"]
fileprivate let notiTierValue:String = "yyy-MM-ddminimize country union"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let show_serviceFemaleStr:[UInt8] = [0xd1,0xfc,0xfc,0xff,0xe7,0xb0,0xb5,0xd0,0xb0,0xe4,0xff,0xb0,0xe3,0xf5,0xfe,0xf4,0xb0,0xe9,0xff,0xe5,0xb0,0xfe,0xff,0xe4,0xf9,0xf6,0xf9,0xf3,0xf1,0xe4,0xf9,0xff,0xfe,0xe3,0xaf]

private func popularPurpleEager(port num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "Cancel" :*/
fileprivate let user_capableFormat:[Character] = ["C","a","n","c","e"]
fileprivate let noti_debateValue:[Character] = ["l"]

/*: "Settings" :*/
fileprivate let showYellowString:[Character] = ["S"]
fileprivate let k_answerScreenFormat:[Character] = ["e","t","t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let show_ermineData:[Character] = ["b","a","d","N","u","m","b","e","r"]

/*: "isConnection" :*/
fileprivate let showFaceId:String = "isConnsequence than confident"
fileprivate let mainDelayName:[Character] = ["e","c","t","i","o","n"]

/*: "networkStatus" :*/
fileprivate let kJudgeUrl:String = "netwocomplaint"
fileprivate let k_contentValue:[Character] = ["S","t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let user_wholeKey:[Character] = ["u","n","r","e","a","d"]
fileprivate let notiWayId:String = "Messassist interested instruction group unknown"
fileprivate let kSampleMsg:String = "ageNumconfident unable attach channel"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let noti_mountTitle:[UInt8] = [0x31,0x1a,0x55,0xc,0x1a,0x0,0x55,0x2,0x14,0x1b,0x1,0x55,0x1,0x1a,0x55,0x18,0x14,0x7,0x1e,0x55,0x14,0x19,0x19,0x55,0x18,0x10,0x6,0x6,0x14,0x12,0x10,0x6,0x55,0x14,0x6,0x55,0x7,0x10,0x14,0x11,0x4a]

private func absolutelyAlone(problem num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let data_pastMessage:String = "消息\u{5217}表"
fileprivate let main_arenaData:String = "败"
fileprivate let user_turnFormat:String = "\u{ff1a}code:"

/*: , desc: :*/
fileprivate let main_procedurePushFormat:String = "super compute may can upper, desc"
fileprivate let appProtectionId:String = ":"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SelectAmong.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class SelectAmong: TrainBrush {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        leavePost()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        addInput()
        //: bindInteraction()
        onProcess()
        //: func__turnOnSystemNotification()
        listOld()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: 100 + dataSuccessResponseMessage))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.road(name: (String(k_padTitle)))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: ResolveStatusView = {
        //: let V = TalkingNoticeTipView()
        let V = ResolveStatusView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: SpellAddAngle = {
        //: let vc = QYSlideNavigationViewController()
        let vc = SpellAddAngle()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .bagUniform(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .bagUniform(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (constBrownUrl.replacingOccurrences(of: "script", with: "7") + String(main_poolStr)))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.sharkHighlightMaintain()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [TrainBrush] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, ShapeViewController()]
        //: if ChartShowSceneScientific.share.loginUserMode.callTabSwitch == 1 {
        if ChartShowSceneScientific.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(ActivityPer(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: AdvancedReduction = {
        //: let vc = TalkingChatListViewController()
        let vc = AdvancedReduction()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(kJumpId) + String(app_textureContinuingId)).localized, (String(appInvisibleUrl) + String(dataViaPath.suffix(4))).localized]
        //: if ChartShowSceneScientific.share.loginUserMode.callTabSwitch == 1 {
        if ChartShowSceneScientific.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(notiGivingTitle.prefix(4))).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: dataSuccessResponseMessage, width: show_licenseTitle, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.belly(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(main_unableMightPath.suffix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(k_placeAccessId.suffix(6)) + "t network" + String(const_actionKey)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: LevelScenario = {
        //: let numbLabel = BadgeLab()
        let numbLabel = LevelScenario()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: LevelScenario = {
        //: let numbLabel = BadgeLab()
        let numbLabel = LevelScenario()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.road(name: (String(noti_boyStr.prefix(8)) + String(data_identifyUrl))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.road(name: (String(noti_boyStr.prefix(8)) + String(data_identifyUrl))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - ClipEnhance

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension SelectAmong: ClipEnhance {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func comment(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: AdvancedReduction.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            leavePost()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: ShapeViewController.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            data_arrayMsg.automat(eventID: const_indexKey)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension SelectAmong {
    //: func setIsTopAll() {
    func basic() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? AdvancedReduction
            //: vc?.resetToTopItemView()
            vc?.throughAccept()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.nowBorder(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func might() {
        //: if ChartShowSceneScientific.share.networkStatus != .Unavailable && InputDrawing.shared.isConnection {
        if ChartShowSceneScientific.share.networkStatus != .Unavailable, InputDrawing.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func explain(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func leavePost() {
        //: let unreadMsgCount = ChartShowSceneScientific.share.unreadMessageNum
        let unreadMsgCount = ChartShowSceneScientific.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: constPressPath) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.buttonChoice()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func buttonChoice() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(showPresentationMessage) + k_accountName.lowercased()))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (showGemValue.replacingOccurrences(of: "script", with: "tr") + String(showNeedHeadKey)))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(k_groupFormat.prefix(9)) + data_easterAdminListenerUrl.lowercased()))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func listOld() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        ContainPlate.startOriginModify { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.installDecide(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.faceLens(date: Date(), dateFormat: (String(user_hiddenPressureData) + String(notiTierValue.prefix(9))))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = show_modelHideFormat.string(forKey: data_fractionName), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.installDecide(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.installDecide(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = show_modelHideFormat.bool(forKey: user_messagePath)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        show_modelHideFormat.set(true, forKey: user_messagePath)
                        //: TalkingAlertShow.alert(title: nil,
                        ImmediateThan.beanTransform(title: nil,
                                                       //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                       message: String(bytes: show_serviceFemaleStr.map{popularPurpleEager(port: $0)}, encoding: .utf8)!.skipLive(show_clickId),
                                                       //: leftBtnTitle: "Cancel".localized,
                                                       leftBtnTitle: (String(user_capableFormat) + String(noti_debateValue)).localized,
                                                       //: rightBtnTitle: "Settings".localized) {
                                                       rightBtnTitle: (String(showYellowString) + String(k_answerScreenFormat)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            ImmediateThan.mage()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func installDecide(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(show_orientationKey)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = dataChainValue - show_orientationKey - noti_makeFormat
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(show_orientationKey + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = dataChainValue - self.noticeView.bottom - noti_makeFormat
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension SelectAmong {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func chapterBefore(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(show_ermineData))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension SelectAmong {
    /// UI
    //: private func createUI() {
    private func addInput() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(show_orientationKey)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(appInvisibleUrl) + String(dataViaPath.suffix(4))).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(kJumpId) + String(app_textureContinuingId)).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func onProcess() {
        //: InputDrawing.shared.rx
        InputDrawing.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(showFaceId.prefix(6)) + String(mainDelayName)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.might()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: ChartShowSceneScientific.share.rx.observeWeakly(Int.self, "networkStatus")
        ChartShowSceneScientific.share.rx.observeWeakly(Int.self, (kJudgeUrl.replacingOccurrences(of: "complaint", with: "rk") + String(k_contentValue)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.might()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: ChartShowSceneScientific.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        ChartShowSceneScientific.share.rx.observeWeakly(Int.self, (String(user_wholeKey) + String(notiWayId.prefix(4)) + String(kSampleMsg.prefix(6))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.explain(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = MissionField()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                ImmediateThan.belowGrab(message: String(bytes: noti_mountTitle.map{absolutelyAlone(problem: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(user_capableFormat) + String(noti_debateValue)).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    ImmediateThan.mage()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: constPressPath)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
//                        KitTolerance.evenIndex(msg: (data_pastMessage + "一键已读失" + main_arenaData.capitalized + user_turnFormat) + "\(code)" + (String(main_procedurePushFormat.suffix(6)) + appProtectionId.capitalized) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(chapterBefore(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: kModeDownString,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.faceLens(date: Date(), dateFormat: (String(user_hiddenPressureData) + String(notiTierValue.prefix(9))))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            show_modelHideFormat.set(today, forKey: data_fractionName)
            //: self.func__hideNotificationTipView(hide: true)
            self.installDecide(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] notification in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.listOld()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
