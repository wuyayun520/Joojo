
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let mainUntilMsg:[UInt8] = [0x85,0xf3,0x80,0xba,0xf6,0xa1,0x9a,0xf6,0x81,0xeb,0xf6,0xe2,0x84,0x87,0xf5,0xf6,0x86,0xf0,0xf2,0x9b,0xf3,0x80,0x87,0xbf,0xba,0xf6,0xa1,0x87,0xf5,0xf6,0x86,0xf0,0xf2,0x87,0xf5,0xf3,0x80,0xba,0xf6,0xa1,0x87,0xf5,0x86,0xa0,0xe9,0xf7,0xed,0xa6,0xf2,0xff]

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let appShAccessibleMessage:[UInt8] = [0x9b,0xed,0xed,0xf4,0x9e,0xf6,0xf1,0xf0,0xf2,0xfd,0x98,0x9e,0xf5,0xe8,0xfc,0x98,0xbe,0xfc,0xb8,0xec,0xb9,0xed,0xed,0xf5,0x99,0xa1,0xbe,0xf7,0xb8,0xe8,0x99,0xa1,0xbe,0xfd,0xb8,0xec,0xb9,0xed,0xf5,0x99,0xa1,0xbe,0xf6,0xb8,0xe8,0x99,0xa1,0xbe,0xf2,0xe9,0xfd,0xb8,0xec,0xb9,0xed,0xf5,0x99,0xa1,0xbe,0xf4,0xf5,0xe9,0xf4,0xf4,0xb8,0xec,0xec,0xe1]

private func weArena(stop num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let kErmineMovieImmediateString:[UInt8] = [0x7a,0xc,0x7f,0x78,0x51,0x10,0x61,0x14,0x14,0x9,0x78,0x51,0x1d,0x62,0x65,0x11,0x65,0x9,0x7e,0x45,0x9,0x5e,0x14,0x9,0x1d,0x7b,0x79,0x5f,0x16,0x8,0x15,0x14,0x59,0x0,0xd]

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let appInstallUrl:String = "^[command-8"
fileprivate let notiChooseBlueKey:[Character] = ["]","\\","d"]
fileprivate let kStatAbsString:String = "{5}(?"

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let constFileId:String = "transaction regulate phone am approve^[a-"
fileprivate let k_lessSkinFormat:String = "-9]{6,2"
fileprivate let noti_cocaValue:String = "impression}+$"

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let mainEnormousStr:[UInt8] = [0xea,0x9c,0xdc,0xc0,0xc0,0xc4,0xc7,0x8b,0x8e,0xe8,0x9b,0xe8,0x9b,0x9d,0x8b,0x9c,0xef,0xe8,0xd0,0xd5,0x99,0xce,0xe8,0x9a,0x99,0xe9,0x9f,0x9d,0xe8,0x9a,0x9c,0xef,0xd5,0x99,0xce,0xe8,0x9a,0xe9,0xcf,0x86,0x98,0x82,0xc9,0x9d,0x9c,0xef,0xe8,0x9b,0xe8,0xc3,0x94,0xe8,0x9a,0x99,0xe9,0x9e,0x9d,0x9e,0xe8,0x9b,0x8b,0x90]

private func deviceGrain(cur num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let k_angleMessage:[UInt8] = [0x41,0xb,0x22,0x1d,0xb,0x22,0x1d,0x15,0x18,0x3e,0x13,0x10,0x18,0x40,0x5f,0x15,0x3e,0x13,0x10,0x17,0x40,0x3e,0x13,0x10,0x1c,0x40,0x5f,0x3e,0x13,0x14,0x40,0x22,0x3e,0x13,0x10,0x1c,0x40,0x3e,0x13,0x10,0x1c,0x40,0x22,0xc,0x3f,0x11,0xc,0x3,0x5e,0x16,0x60,0xb,0x22,0x1d,0x15,0x18,0x3e,0x13,0x10,0x18,0x40,0x5f,0x15,0x3e,0x13,0x10,0x17,0x40,0x3e,0x13,0x10,0x1c,0x40,0x5f,0x3e,0x13,0x14,0x40,0x22,0x3e,0x13,0x10,0x1c,0x40,0x3e,0x13,0x10,0x1c,0x40,0x22,0xc,0x7]

fileprivate func failAnalysisTask(small num: UInt8) -> UInt8 {
    let value = Int(num) + 29
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let appGrainData:[UInt8] = [0x4f,0x39,0x3c,0x57,0x4a,0x5e,0x3a,0x1c,0x56,0x52,0x1a,0x43,0x4e,0x1b,0x4f,0x3c,0x57,0x4a,0x5e,0x3a,0x1c,0x56,0x50,0x1a,0x4f,0x3c,0x57,0x4a,0x5e,0x3a,0x1b,0x3c,0x1f,0x3f,0x3a,0x4e,0x43,0x4e]

/*: "SELF MATCHES %@" :*/
fileprivate let user_receiveRoundKey:String = "SELF MATspot thread listen"
fileprivate let data_bucketTitle:String = "should light privacy mortalCHES %@"

/*: "Feedback" :*/
fileprivate let userPastId:[Character] = ["F","e","e","d","b"]
fileprivate let const_versionTitle:String = "acman"

/*: "Enter your feedback…" :*/
fileprivate let const_recognizeMsg:String = "signature weeklyEnte"
fileprivate let appIdentifyString:[Character] = ["f"]
fileprivate let main_decisionUrl:[Character] = ["e","e","d","b","a","c","k","…"]

/*: "#999999" :*/
fileprivate let const_seaMsg:String = "#9999"
fileprivate let kTeamDueMessage:String = "99"

/*: "Your email (Required) " :*/
fileprivate let appBigMessage:String = "Your version publish"
fileprivate let kPlusMessage:String = " (Redisk am successful"
fileprivate let app_exclusiveName:String = "QUIR"

/*: "icon_me_feelback_star" :*/
fileprivate let constScriptWarningTitle:String = "icon_access guard wait"
fileprivate let k_approveUrl:String = "eelbachanging"
fileprivate let const_functionalData:[Character] = ["_","s","t","a","r"]

/*: "#CCCCCC" :*/
fileprivate let appThirdValue:String = "highlight none#CCCCCC"

/*: "Send" :*/
fileprivate let show_rootName:String = "salt home build value fieldSend"

/*: "#D0D0D0" :*/
fileprivate let userOntoDensityStr:[Character] = ["#","D","0","D","0","D","0"]

/*: "Please fill in the content" :*/
fileprivate let userStandardName:[Character] = ["P","l","e","a","s","e"," ","f","i","l","l"," ","i","n"," ","t","h"]
fileprivate let appMUrl:[Character] = ["e"," "]
fileprivate let const_happyFormat:String = "CONTENT"

/*: "Please enter the correct email address" :*/
fileprivate let notiEvolutionPlatValue:[UInt8] = [0x73,0x73,0x65,0x72,0x64,0x64,0x61,0x20,0x6c,0x69,0x61,0x6d,0x65,0x20,0x74,0x63,0x65,0x72,0x72,0x6f,0x63,0x20,0x65,0x68,0x74,0x20,0x72,0x65,0x74,0x6e,0x65,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "content" :*/
fileprivate let main_trackString:String = "cmeasure"
fileprivate let userAttachTeamData:String = "nwillnt"

/*: "contactWay" :*/
fileprivate let show_entityMsg:String = "contstatt"
fileprivate let const_finishRagStr:String = "process economical interrupt whiteWay"

/*: "platform" :*/
fileprivate let mainExceptWrapFormat:[Character] = ["p"]
fileprivate let dataShadeFollowingUrl:[Character] = ["l","a","t","f","o","r","m"]

/*: "iphone" :*/
fileprivate let kChildMakeUrl:String = "ipwrapone"

/*: "version" :*/
fileprivate let appChallengeFormat:String = "adjustmentrsion"

/*: "type" :*/
fileprivate let showPainterValue:String = "serviceype"

/*: "Operation succeeded" :*/
fileprivate let constHeliFrequencyUrl:String = "Operarelation embarrass pool"
fileprivate let appDynamicsName:String = " succeaccessible enough by detail"

/*: "Problem statements" :*/
fileprivate let show_valueString:[Character] = ["P","r","o","b","l","e","m"," ","s","t","a","t","e","m","e","n","t","s"]

/*: "Feature advice" :*/
fileprivate let constActiveData:[Character] = ["F","e","a","t","u","r","e"," ","a","d","v","i","c","e"]

/*: "Operation questions" :*/
fileprivate let appDeviceStorageTitle:String = "Operdecide sentence pick native"
fileprivate let showMyAcrossTitle:[Character] = [" ","q","u","e","s","t","i","o","n","s"]

/*: "Others" :*/
fileprivate let noti_compositionMessage:String = "path kind in filterOthers"

/*: "#EFEDFF" :*/
fileprivate let notiContextStr:String = "cell forward bean statue numb#EFEDFF"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AccessibleVisit.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum ActiveMarker {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: mainUntilMsg.map{$0^219}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: appShAccessibleMessage.map{weArena(stop: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: kErmineMovieImmediateString.map{$0^36}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (appInstallUrl.replacingOccurrences(of: "command", with: "0") + String(notiChooseBlueKey) + kStatAbsString.capitalized + "!\\d)$")
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(constFileId.suffix(4)) + "zA-Z0" + k_lessSkinFormat.capitalized + noti_cocaValue.replacingOccurrences(of: "impression", with: "0"))
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: kErmineMovieImmediateString.map{$0^36}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: mainEnormousStr.map{deviceGrain(cur: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: k_angleMessage.map{failAnalysisTask(small: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: appGrainData.map{$0^103}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(user_receiveRoundKey.prefix(8)) + String(data_bucketTitle.suffix(7))), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class AccessibleVisit: TrainBrush {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(userPastId) + const_versionTitle.replacingOccurrences(of: "man", with: "k")).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        beyondMessage()
        //: addTapGestureRecognizer()
        projectionWindow()
        //: addKeyboardNotification()
        openerSignal()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .livingRank()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: CurrencyRebuild = {
        //: let textView = TalkingTextView.init()
        let textView = CurrencyRebuild()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(const_recognizeMsg.suffix(4)) + "r your " + String(appIdentifyString) + String(main_decisionUrl)).localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (const_seaMsg.capitalized + kTeamDueMessage.capitalized))!
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        textView.font = UIFont.bagUniform(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.sharkHighlightMaintain()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .livingRank()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (const_seaMsg.capitalized + kTeamDueMessage.capitalized))
        //: LB.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        LB.font = UIFont.bagUniform(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .livingRank()
        //: tf.textColor = UIColor.appTitleColor()
        tf.textColor = UIColor.sharkHighlightMaintain()
        //: tf.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        tf.font = UIFont.bagUniform(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(appBigMessage.prefix(5)) + "email" + String(kPlusMessage.prefix(4)) + app_exclusiveName.lowercased() + "ed) ").localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (const_seaMsg.capitalized + kTeamDueMessage.capitalized))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if TitlePer.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.BundleImageNamed(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.road(name: (String(constScriptWarningTitle.prefix(5)) + "me_f" + k_approveUrl.replacingOccurrences(of: "hanging", with: "k") + String(const_functionalData)))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.asTimingPlain(color: UIColor(hex: (String(appThirdValue.suffix(7))))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.appThemeColor(), forState: .normal)
        btn.asTimingPlain(color: UIColor.special(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(show_rootName.suffix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.bagUniform(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(action), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension AccessibleVisit {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func assetBoard(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(userOntoDensityStr)))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func action() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.bury(showMsg: (String(userStandardName) + String(appMUrl) + const_happyFormat.lowercased()).localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if TitlePer.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !ActiveMarker.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.bury(showMsg: String(bytes: notiEvolutionPlatValue.reversed(), encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(main_trackString.replacingOccurrences(of: "measure", with: "o") + userAttachTeamData.replacingOccurrences(of: "will", with: "te"))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(show_entityMsg.replacingOccurrences(of: "stat", with: "ac") + String(const_finishRagStr.suffix(3)))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(String(mainExceptWrapFormat) + String(dataShadeFollowingUrl))] = (kChildMakeUrl.replacingOccurrences(of: "wrap", with: "h"))
        //: dict["version"] = AppVersion
        dict[(appChallengeFormat.replacingOccurrences(of: "adjustment", with: "ve"))] = appOperationPath
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(showPainterValue.replacingOccurrences(of: "service", with: "t"))] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        GrayView.blackCorner()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        IncludeTag.equipment(params: dict) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            GrayView.at()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.allInsidePosition(showMsg: (String(constHeliFrequencyUrl.prefix(5)) + "tion" + String(appDynamicsName.prefix(6)) + "eded").localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func openerSignal() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(cancel(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(detailTotalensity(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func cancel(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + dataSuccessResponseMessage) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + dataSuccessResponseMessage) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func detailTotalensity(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension AccessibleVisit: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = add(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func add(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension AccessibleVisit {
    //: func designView() {
    func beyondMessage() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(show_valueString)).localized,
                   //: "Feature advice".localized,
                   (String(constActiveData)).localized,
                   //: "Operation questions".localized,
                   (String(appDeviceStorageTitle.prefix(4)) + "ation" + String(showMyAcrossTitle)).localized,
                   //: "Others".localized]
                   (String(noti_compositionMessage.suffix(6))).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(show_licenseTitle) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.special(), for: .selected)

            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.bagUniform(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if TotalSearch.share.interfaceLang == ChainAgile.es.rawValue || TotalSearch.share.interfaceLang == ChainAgile.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.bagUniform(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(userOntoDensityStr)))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.asTimingPlain(color: UIColor(hex: (String(notiContextStr.suffix(7))))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.asTimingPlain(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(assetBoard(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
