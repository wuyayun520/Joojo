
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_familyPath:[UInt8] = [0x87,0x80,0x87,0x9a,0xc6,0x8d,0x81,0x8a,0x8b,0x9c,0xd4,0xc7,0xce,0x86,0x8f,0x9d,0xce,0x80,0x81,0x9a,0xce,0x8c,0x8b,0x8b,0x80,0xce,0x87,0x83,0x9e,0x82,0x8b,0x83,0x8b,0x80,0x9a,0x8b,0x8a]

/*: "Load..." :*/
fileprivate let k_resistanceMsg:[Character] = ["L","o","a","d","."]
fileprivate let const_automaticVersionUrl:String = "visiblevisible"

/*: "Skip" :*/
fileprivate let main_glassDramFormat:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let user_associatedString:[UInt8] = [0x42,0x6e,0x6e,0x6a,0x4e,0x69,0x69,0x66,0x28,0x48,0x5b,0x6e,0x63,0x70,0x5f,0x4e,0x69,0x44,0x6d,0x22,0x21,0x6c,0x5f,0x5d,0x62,0x5b,0x6c,0x61,0x5f,0x21,0x23]

fileprivate func weStream(beauty num: UInt8) -> UInt8 {
    let value = Int(num) + 6
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let const_forwardKey:[UInt8] = [0x3b,0x29,0x28,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageShow):  :*/
fileprivate let show_bellyName:[Character] = ["j","s","E","v","e","n","t","(","o","n"]
fileprivate let dataHistoryData:[Character] = ["P","a","g","e","S","h","o","w",")",":"," "]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let userProsecutionEveryValue:[UInt8] = [0xf5,0xeb,0xec,0xe6,0xed,0xf5,0xac,0xed,0xec,0xd2,0xe3,0xe5,0xe7,0xca,0xeb,0xe6,0xe7,0xa4,0xa4,0xed,0xec,0xd2,0xe3,0xe5,0xe7,0xca,0xeb,0xe6,0xe7,0xaa,0xab,0xb9]

private func hourWord(display num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let appAngleMsg:String = "m create mountjsEv"
fileprivate let k_noteYaName:String = "magnitude provideronPa"
fileprivate let notiReserveKey:[Character] = ["e",")",":"," "]

/*: "title" :*/
fileprivate let show_progressData:String = "groupigrouple"

/*: "canGoBack" :*/
fileprivate let k_contactPath:[Character] = ["c","a","n"]
fileprivate let kLineTitle:[Character] = ["G","o","B","a","c","k"]

/*: "PHPSESSID= :*/
fileprivate let data_accessibleHiddenMessage:String = "charmHcharmS"
fileprivate let dataStillObjectAudienceString:[Character] = ["D","="]

/*: ";uid= :*/
fileprivate let main_beatMsg:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let main_visitString:String = "nana lock coordinateCookie"

/*: "nav_back_black_nor" :*/
fileprivate let constEaseLicenseLockString:String = "track child newsnav_ba"
fileprivate let userFramePath:String = "coordinatorack"

/*: "ico_intimate_list_sm" :*/
fileprivate let user_boldValue:String = "dialog define with bellyico_"
fileprivate let app_averageTitle:String = "ate_lithan photo tier sentence board"
fileprivate let notiRestrictionPath:String = "else normst_sm"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let mainBorrowSnapString:[UInt8] = [0x96,0x9d,0x91,0x87,0x9f,0x97,0x9c,0x86,0xdc,0x95,0x97,0x86,0xb7,0x9e,0x97,0x9f,0x97,0x9c,0x86,0x81,0xb0,0x8b,0xa6,0x93,0x95,0xbc,0x93,0x9f,0x97,0xda,0xd5,0x90,0x9d,0x96,0x8b,0xd5,0xdb,0xa9,0xc2,0xaf,0xdc,0x81,0x86,0x8b,0x9e,0x97,0xdc,0x90,0x93,0x91,0x99,0x95,0x80,0x9d,0x87,0x9c,0x96,0xcf,0xd5,0x80,0x95,0x90,0x93,0xda,0xc2,0xde,0xc2,0xde,0xc2,0xde,0xc2,0xdb,0xd5]

private func fileTally(everyone num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "Alert" :*/
fileprivate let app_middleData:String = "time bold decision distance returnAlert"

/*: "Cancle" :*/
fileprivate let appProgressLocateStr:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let appOptionDrawerPlayerUrl:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let data_lackFormat:String = "delay month equipment infojs call "
fileprivate let k_adviceUrl:[Character] = ["m","e","t","h","o","d"," ","n","a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let constExamineString:String = "away deploy official alongside allow, me"
fileprivate let kSearchId:String = "e = low bridge skip race"

/*: "http://" :*/
fileprivate let const_cleanData:[Character] = ["h","t","t","p",":","/"]
fileprivate let mainDrawForceRootKey:[Character] = ["/"]

/*: "https://" :*/
fileprivate let noti_possibleStr:String = "https:woman corruption"
fileprivate let appInvisiblePartyName:String = "//"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let app_airportMessage:[UInt8] = [0xd6,0xc1,0xd2,0x80,0xc3,0xcf,0xcf,0xcb,0xc9,0xc5,0xae,0xc1,0xcd,0xc5,0xd3,0x80,0x9d,0x80,0xc4,0xcf,0xc3,0xd5,0xcd,0xc5,0xce,0xd4,0x8e,0xc3,0xcf,0xcf,0xcb,0xc9,0xc5,0x8e,0xd3,0xd0,0xcc,0xc9,0xd4,0x88,0x87,0x9b,0x80,0x87,0x89,0x8e,0xcd,0xc1,0xd0,0x88,0xc6,0xd5,0xce,0xc3,0xd4,0xc9,0xcf,0xce,0x88,0xc3,0xcf,0xcf,0xcb,0xc9,0xc5,0x89,0x80,0xdb,0x80,0xd2,0xc5,0xd4,0xd5,0xd2,0xce,0x80,0xc3,0xcf,0xcf,0xcb,0xc9,0xc5,0x8e,0xd3,0xd0,0xcc,0xc9,0xd4,0x88,0x87,0x9d,0x87,0x89,0xbb,0x90,0xbd,0x80,0xdd,0x80,0x89,0x9b,0x6a]

fileprivate func norRace(immediate num: UInt8) -> UInt8 {
    let value = Int(num) - 96
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid= :*/
fileprivate let constBetterMysteryPath:String = "fore deputyuid="

/*: ;domain= :*/
fileprivate let showAdditionData:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let constConvertName:String = ";path=/normal rocket"

/*: "document.cookie=' :*/
fileprivate let user_colorPath:String = "dovariable"
fileprivate let showExplainSenMsg:String = "csincekie"
fileprivate let k_nameMaxStr:String = "=\'"

/*: ;domin= :*/
fileprivate let data_pingName:String = ";ddraw master cos"
fileprivate let kEditUrl:String = "sub honey old withdrawomin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let k_earlyKey:[UInt8] = [0x51,0x5e,0x18,0x10,0x5b,0x57,0x57,0x53,0x51,0x5d,0x76,0x59,0x55,0x5d,0x4b,0x16,0x51,0x56,0x5c,0x5d,0x40,0x77,0x5e,0x10,0x1f,0x68,0x70,0x68,0x6b,0x7d,0x6b,0x6b,0x71,0x7c,0x1f,0x11,0x18,0x5,0x5,0x18,0x15,0x9,0x11,0x18,0x43,0x18,0x5c,0x57,0x5b,0x4d,0x55,0x5d,0x56,0x4c,0x16,0x5b,0x57,0x57,0x53,0x51,0x5d,0x5,0x1f]

private func happyMemory(mortal num: UInt8) -> UInt8 {
    return num ^ 56
}

/*: "h5Domain =  :*/
fileprivate let k_uniformKey:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let main_elbowPath:String = "getAlisolate graph his"
fileprivate let const_ionMountString:[Character] = ["e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let user_disappearMessage:String = "PHPSESSItransform"

/*: "uid" :*/
fileprivate let appForceSequenceFormat:String = "uimoment"

/*: "appCookies =  :*/
fileprivate let data_positionSlowString:String = "appCoocase lose remaining next"

/*: "UID" :*/
fileprivate let app_agileWatchId:String = "quitD"

/*: "navigator.userAgent" :*/
fileprivate let showHiName:String = "NAVIG"
fileprivate let userHereArcStr:String = "aflowor"
fileprivate let constTeamDisplayMessage:String = "Agentpic unique brush note"

/*: "xunwu_iphone" :*/
fileprivate let notiPriorName:String = "xhelpwu"
fileprivate let main_guideChangeValue:[Character] = ["n","e"]

/*: " xunwu_device_id/ :*/
fileprivate let userPopularStr:[Character] = [" ","x","u","n","w","u","_","d","e","v","i"]
fileprivate let user_scanSubmitFormat:[Character] = ["c"]
fileprivate let const_squareValue:String = "e_id/challenge provider control announcement reason"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let main_whiteMessage:String = "just flowxunwu_"
fileprivate let user_fitData:[Character] = [" ","x","u"]
fileprivate let appObserveDecisionTitle:[Character] = ["n","w","u","_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let constBoundFromString:[Character] = [" ","x","u","n","w","u","_"]
fileprivate let data_stairsMessage:String = "conversationc"
fileprivate let noti_replyFormat:[Character] = ["k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let dataFellowEstimatedIncreasinglyString:String = " xunwhusband appointed process"
fileprivate let show_certainValue:String = "leId/con seventh sum"

/*:   webkit/1" :*/
fileprivate let data_acrossAboutStr:String = "texture nite never  web"
fileprivate let user_logName:[Character] = ["k","i","t","/","1"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BrownGroup.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class BrownGroup: EagerViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: LocationMove!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: FlexProperty?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: FlexProperty?, webConfig: LocationMove) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_familyPath.map{$0^238}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        panelMagnitudeerest()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        priorBridge()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(k_resistanceMsg) + const_automaticVersionUrl.replacingOccurrences(of: "visible", with: "."))
        //: if config.isHideBackBtn {
        if config.isHideBackBtn {
            //: hideNavi = true
            hideNavi = true
        }
        //: addNotification()
        countegration()
        //: func__checkConfig()
        countegrationForAm()
        //: func__setUpWebView()
        husbandGroup()
        //: func__registerObserver()
        alone()
        //: func__beginStartRequest()
        clipHome()
        //: func_checkAppleIAP()
        wooOf()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: BeforeAssociate.share.func__pushToWebVC(webViewType: .DiscountPopup)
                BeforeAssociate.share.successfullyHi(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: dataLengthMsg, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            obtainArtBean(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.special(), .font: UIFont.bagUniform(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(main_glassDramFormat)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension BrownGroup {
    /// 添加通知
    //: private func addNotification() {
    private func countegration() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(keep),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: const_coverId,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(panelMagnitudeerest),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func keep() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: user_associatedString.map{weStream(beauty: $0)}, encoding: .utf8)!) { data, error in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func panelMagnitudeerest() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: const_forwardKey.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(show_bellyName) + String(dataHistoryData)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func priorBridge() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userProsecutionEveryValue.map{hourWord(display: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(appAngleMsg.suffix(4)) + "ent(" + String(k_noteYaName.suffix(4)) + "geHid" + String(notiReserveKey)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension BrownGroup {
    //: func func__registerObserver() {
    func alone() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (show_progressData.replacingOccurrences(of: "group", with: "t")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(k_contactPath) + String(kLineTitle)))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(roundAwayRemote), name: show_rateBridgeStr, object: nil)
    }

    //: func func__setUpWebView() {
    func husbandGroup() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        exceptPrimarilyGenetic()
        //: func__addBridgeMethod()
        momentDown()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.livingRank()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            GrayView.go(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(show_orientationKey)
            }
        }
    }

    //: func func__beginStartRequest() {
    func clipHome() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.aLiving()
            }
            //: } else {
        } else {
            //: func__startRequest()
            aLiving()
        }
    }

    //: func func__startRequest() {
    func aLiving() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = ChartShowSceneScientific.share.loginUid
        let uid = ChartShowSceneScientific.share.loginUid
        //: let ssid = ChartShowSceneScientific.share.loginSessionId
        let ssid = ChartShowSceneScientific.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (data_accessibleHiddenMessage.replacingOccurrences(of: "charm", with: "P") + "ESSI" + String(dataStillObjectAudienceString)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(main_beatMsg)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(main_visitString.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        painterTerms()
        //: func__initRightButton()
        emerge()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func wooOf() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            NumberegrityPosition.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.chapterAppeal(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            NumberegrityPosition.shared.put()
        }
    }

    //: func func__initLeftButton() {
    func orangMargin() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.road(name: (String(constEaseLicenseLockString.suffix(6)) + "ck_b" + userFramePath.replacingOccurrences(of: "coordinator", with: "l") + "_nor")), style: .plain, target: self, action: #selector(exclusiveFace(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func emerge() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(FlexProperty.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.road(name: (String(user_boldValue.suffix(4)) + "intim" + String(app_averageTitle.prefix(6)) + String(notiRestrictionPath.suffix(5)))), style: .plain, target: self, action: #selector(absolutely))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func exclusiveFace(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        heliTheme()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.chemist() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: BrownGroup.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! BrownGroup).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! BrownGroup).panelMagnitudeerest()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func countegrationForAm() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func resumeUs(sucess: Bool) {
        //: ProgressHUD.dismiss()
        GrayView.at()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func painterTerms() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: mainBorrowSnapString.map{fileTally(everyone: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func lostSentence(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: BeforeAssociate.share.func__pushToWebVC(urlStr: url)
            BeforeAssociate.share.magnitudeer(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func sequenceQuote(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func roundAwayRemote() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: show_rateBridgeStr, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func absolutely() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = RoundingView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
        //: view.show()
        view.progress()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension BrownGroup: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            GrayView.go(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            resumeUs(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        orangMargin()
        //: func__clearJSBgColor()
        painterTerms()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            resumeUs(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            resumeUs(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(app_middleData.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(appProgressLocateStr)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(appOptionDrawerPlayerUrl)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension BrownGroup: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(data_lackFormat.suffix(8)) + String(k_adviceUrl)) + "\(message.name)" + (String(constExamineString.suffix(4)) + "ssag" + String(kSearchId.prefix(4))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.phoneTo(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func momentDown() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in MediaBrown.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func heliTheme() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in MediaBrown.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension BrownGroup {
    //: func func__addUserScript() {
    func vent() {
        //: var htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
        var htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(const_cleanData) + String(mainDrawForceRootKey))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(const_cleanData) + String(mainDrawForceRootKey)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(noti_possibleStr.prefix(6)) + appInvisiblePartyName.capitalized)) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(noti_possibleStr.prefix(6)) + appInvisiblePartyName.capitalized), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = ChartShowSceneScientific.share.loginUserMode.userID
        let tempUid: String = ChartShowSceneScientific.share.loginUserMode.userID
        //: let tempSessionID: String = ChartShowSceneScientific.share.loginSessionId
        let tempSessionID: String = ChartShowSceneScientific.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: app_airportMessage.map{norRace(immediate: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(constBetterMysteryPath.suffix(4))) + "\(tempUid)" + (String(showAdditionData)) + "\(htmlDomain)" + (String(constConvertName.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((user_colorPath.replacingOccurrences(of: "variable", with: "cu") + "ment." + showExplainSenMsg.replacingOccurrences(of: "since", with: "oo") + k_nameMaxStr) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (data_accessibleHiddenMessage.replacingOccurrences(of: "charm", with: "P") + "ESSI" + String(dataStillObjectAudienceString)) + "\(tempSessionID)" + (String(data_pingName.prefix(2)) + String(kEditUrl.suffix(5))) + "\(htmlDomain)" + (String(constConvertName.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((user_colorPath.replacingOccurrences(of: "variable", with: "cu") + "ment." + showExplainSenMsg.replacingOccurrences(of: "since", with: "oo") + k_nameMaxStr) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: k_earlyKey.map{happyMemory(mortal: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(constBetterMysteryPath.suffix(4))) + "\(tempUid)" + (String(showAdditionData)) + "\(htmlDomain)" + (String(constConvertName.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((user_colorPath.replacingOccurrences(of: "variable", with: "cu") + "ment." + showExplainSenMsg.replacingOccurrences(of: "since", with: "oo") + k_nameMaxStr) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(constBetterMysteryPath.suffix(4))) + "\(tempSessionID)" + (String(showAdditionData)) + "\(htmlDomain)" + (String(constConvertName.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((user_colorPath.replacingOccurrences(of: "variable", with: "cu") + "ment." + showExplainSenMsg.replacingOccurrences(of: "since", with: "oo") + k_nameMaxStr) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func obtainArtBean(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
            let htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(k_uniformKey)) + "\(htmlDomain)")
            //: let userID: String = ChartShowSceneScientific.share.loginUserMode.userID
            let userID: String = ChartShowSceneScientific.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(main_elbowPath.prefix(5)) + "lCooki" + String(const_ionMountString)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (user_disappearMessage.replacingOccurrences(of: "transform", with: "D")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (appForceSequenceFormat.replacingOccurrences(of: "moment", with: "d")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.clipHome()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.airFuture(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func airFuture(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
                let htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(k_uniformKey)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: TotalSearch.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(data_positionSlowString.prefix(6)) + "kies = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (app_agileWatchId.replacingOccurrences(of: "quit", with: "UI")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (appForceSequenceFormat.replacingOccurrences(of: "moment", with: "d"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (user_disappearMessage.replacingOccurrences(of: "transform", with: "D")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (user_disappearMessage.replacingOccurrences(of: "transform", with: "D"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.clipHome()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func exceptPrimarilyGenetic() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((showHiName.lowercased() + userHereArcStr.replacingOccurrences(of: "flow", with: "t") + ".user" + String(constTeamDisplayMessage.prefix(5)))) { [weak self] result, err in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (showHiName.lowercased() + userHereArcStr.replacingOccurrences(of: "flow", with: "t") + ".user" + String(constTeamDisplayMessage.prefix(5))))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((notiPriorName.replacingOccurrences(of: "help", with: "un") + "_ipho" + String(main_guideChangeValue))) {
                //: let str = getUserAgentString()
                let str = blocYellow()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.oxygenBoard { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(userPopularStr) + String(user_scanSubmitFormat) + String(const_squareValue.prefix(5))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func blocYellow() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(main_whiteMessage.suffix(6)) + "iphone" + String(user_fitData) + String(appObserveDecisionTitle)) + "\(dataRenderTitle)" + (String(constBoundFromString) + data_stairsMessage.replacingOccurrences(of: "conversation", with: "pa") + String(noti_replyFormat)) + "\(noti_messageFormat)" + (String(dataFellowEstimatedIncreasinglyString.prefix(5)) + "u_bund" + String(show_certainValue.prefix(5))) + "\(show_primaryUrl)" + (String(data_acrossAboutStr.suffix(5)) + String(user_logName))
    }
}
