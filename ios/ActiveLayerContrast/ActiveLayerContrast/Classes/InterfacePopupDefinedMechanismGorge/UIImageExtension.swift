
//: Declare String Begin

/*: "bundle" :*/
fileprivate let noti_nameStr:String = "BUNDLE"

/*: "nav_back_black_nor" :*/
fileprivate let mainRecoverKey:String = "disclose shade open reward exnav_b"
fileprivate let show_hourLostPath:[Character] = ["_","n","o","r"]

/*: "icon_translation_go" :*/
fileprivate let main_cocaString:String = "ICON"
fileprivate let kRecoverFormat:String = "exio"

/*: "iv_me_vip" :*/
fileprivate let dataElementSubjectResolveUrl:String = "iv_me_vipinsert grade"

/*: "btn_dongtai_pinglun_nor" :*/
fileprivate let showReflectString:[Character] = ["b","t","n","_","d","o","n","g","t","a","i","_","p"]
fileprivate let main_keyPath:String = "INGL"
fileprivate let main_beyondStr:[Character] = ["u","n","_","n","o","r"]

/*: "btn_me_new_edit" :*/
fileprivate let main_everyoneSharePath:[Character] = ["b","t","n","_","m","e","_","n","e","w","_","e","d","i","t"]

/*: "btn_me_edit" :*/
fileprivate let showConstraintPath:String = "btn_me_sort decrease gift"
fileprivate let showBeginMsg:String = "petit"

/*: "icon_photo_giftbg" :*/
fileprivate let noti_originFormat:[Character] = ["i","c","o","n","_","p","h","o","t","o","_","g","i","f","t"]
fileprivate let kSampleGameMsg:[Character] = ["b","g"]

/*: "icon_gift_more_arrow" :*/
fileprivate let show_oxygenDateValue:[Character] = ["i","c","o","n","_","g","i","f","t","_","m","o","r","e","_","a","r","r","o","w"]

/*: "bg_talk_other" :*/
fileprivate let show_numberName:[Character] = ["b","g","_","t","a","l","k","_"]
fileprivate let noti_structureBoardTitle:[Character] = ["o","t","h","e","r"]

/*: "bg_talk_me" :*/
fileprivate let appHandleName:String = "extra single bag new versusbg_talk_"
fileprivate let constExactlyPeaKey:String = "morigin"

/*: "icon_talk_left_voive_1" :*/
fileprivate let noti_normallyUrl:[Character] = ["i","c","o","n","_"]
fileprivate let const_windowData:String = "talk_ladmin significant profile ermine"
fileprivate let constStagePath:[Character] = ["o","i","v","e","_","1"]

/*: "icon_talk_left_voive_2" :*/
fileprivate let k_retStr:[Character] = ["i","c","o","n","_"]
fileprivate let dataBrushStr:[Character] = ["t","a","l","k","_","l","e"]
fileprivate let notiFreezeValue:[Character] = ["f","t","_","v","o","i","v","e","_","2"]

/*: "icon_talk_left_voive_3" :*/
fileprivate let constWithdrawLessData:String = "icon_tmask collection today comply"
fileprivate let kFellowCountPath:String = "t_stop going profile"
fileprivate let kErrPath:String = "second save clean area interestvoive_3"

/*: "icon_talk_right_voive_1" :*/
fileprivate let userMentionTitle:String = "icon_tcalled report capture"
fileprivate let noti_presentationStartPath:String = "around empty recent enjoy thirdight_v"
fileprivate let dataEndFormat:String = "into option every menu fileoive_1"

/*: "icon_talk_right_voive_2" :*/
fileprivate let mainWaitName:String = "ICON"
fileprivate let main_subTriggerFactoryMsg:String = "_talkinterval stroke elbow"
fileprivate let noti_ageFormat:String = "super empty disable aboveht_v"

/*: "icon_talk_right_voive_3" :*/
fileprivate let data_serverTitle:[Character] = ["i","c","o","n","_","t","a"]
fileprivate let noti_impressiveShouldName:[Character] = ["l","k","_","r","i","g","h","t","_","v","o","i","v","e","_","3"]

/*: "icon_gift_num_bg" :*/
fileprivate let constReductionSignName:[Character] = ["i","c","o","n","_","g","i","f","t","_","n","u"]
fileprivate let mainRangeMessage:String = "person assert mix brushm_bg"

/*: "group_someoneatme" :*/
fileprivate let noti_efficiencyUrl:[Character] = ["g","r","o","u","p"]
fileprivate let app_sparString:String = "per sound drawer parent busy_someo"

/*: "btn_back_white" :*/
fileprivate let userParticleTableAgeMsg:String = "one marker all graphicbtn_bac"
fileprivate let main_combinedId:String = "k_whitebeside skin domain"

/*: "icon_VectorStroke" :*/
fileprivate let main_segmentName:[Character] = ["i","c","o","n","_","V","e","c","t","o","r","S","t"]
fileprivate let kLowMsg:String = "simultaneouslyke"

/*: "btnJQbEAB_etihw_kcab_shadow" :*/
fileprivate let user_absoluteCalledStr:[Character] = ["b","t","n","J","Q","b","E","A","B","_","e","t","i","h","w","_","k","c","a","b","_"]
fileprivate let main_bagMsg:[Character] = ["s","h","a","d","o","w"]

/*: "bg_talk_closed_tc_intimate" :*/
fileprivate let const_alwaysDiscountPath:[Character] = ["b","g","_"]
fileprivate let data_singleScriptProcessingId:[Character] = ["t","a","l","k","_","c","l","o","s","e","d","_","t","c","_","i","n","t","i","m","a","t","e"]

/*: "icon_talk_reply_voive" :*/
fileprivate let mainWritPurpleTeeTweenData:[Character] = ["i","c","o","n","_","t","a","l","k","_","r","e","p","l","y"]
fileprivate let main_revenueTitle:String = "reply properly skirt mask_voive"

/*: "ico_intimate_list_sm" :*/
fileprivate let const_priorityValue:String = "ico_hidden should turn complaint reveal"
fileprivate let kSmileId:String = "great price his paramate_l"
fileprivate let mainMinimumString:[Character] = ["i","s","t","_","s","m"]

/*: "female_bg_random" :*/
fileprivate let const_regionSampleOffId:[Character] = ["f","e","m","a","l","e","_","b","g","_","r","a","n","d","o"]
fileprivate let main_dataString:String = "entity"

/*: "party_mini_bg_userDetail" :*/
fileprivate let const_observerName:String = "pbusyty"
fileprivate let kContrastValue:String = "nothing stat could stringbg_user"
fileprivate let const_portraitAssetKey:[Character] = ["D","e","t","a","i","l"]

/*: "CIGaussianBlur" :*/
fileprivate let constRemarkMessage:String = "CIGaussound called"
fileprivate let main_aliveValue:[Character] = ["s","i","a","n","B","l","u","r"]

/*: "icon_mid_picture_default" :*/
fileprivate let dataPanelPath:[Character] = ["i","c","o","n","_"]
fileprivate let user_ehId:String = "mbarn"
fileprivate let const_postTitle:String = "mode recording sea typed_pict"
fileprivate let mainHungPath:String = "descientificault"

/*: "icon_big_picture_default" :*/
fileprivate let dataYouMsg:String = "mention global usericon_"
fileprivate let kAboutData:String = "pregionct"
fileprivate let appProsecutionYellowData:String = "DEFAULT"

/*: "icon_big_picture_square" :*/
fileprivate let noti_teamTitle:[Character] = ["i","c","o","n","_","b","i","g","_","p","i","c","t","u","r","e"]
fileprivate let show_levelStr:String = "recognize rate absolute_square"

/*: "gift_icon_default" :*/
fileprivate let constQualityPath:String = "gifexpose"
fileprivate let mainEqualMortalMessage:String = "opportunity mobile decide below_defaul"
fileprivate let user_extendName:String = "time"

/*: "icon_female_icon_default" :*/
fileprivate let dataChapterUrl:[Character] = ["i","c","o","n","_","f","e"]
fileprivate let const_normallyUrl:[Character] = ["m","a","l","e","_","i","c","o","n"]
fileprivate let showOthersUrl:String = "invitation drown cos living container_default"

/*: "icon_male_icon_default" :*/
fileprivate let appInnerConvertStr:String = "spend service break its heicon_m"
fileprivate let mainTriggerUrl:[Character] = ["_","d","e","f","a","u","l","t"]

/*: "icon_circle_pic_default" :*/
fileprivate let constAddressProgressNoteValue:[Character] = ["i","c","o","n","_","c","i"]
fileprivate let mainTakeData:String = "spar"
fileprivate let notiQuantityerruptUnlessEntryMessage:[Character] = ["c","l","e","_","p","i","c","_","d","e","f","a","u","l","t"]

/*: "ata:image/jpeg;base64, :*/
fileprivate let constThirdValue:String = "ata:imagindependent margin"
fileprivate let userAddName:String = "e/jpdescription resolve arena"
fileprivate let dataNetKey:String = "e64,day embarrass short receiver video"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UIImageExtension.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/25.
//

//: import Foundation
import Foundation
//: import SwiftUI
import SwiftUI
//: import UIKit
import UIKit

//: enum PodspecBundle {
enum ThroughTemp {
    //: static let bundle: Bundle = {
    static let bundle: Bundle = {
        //: if let bundleURL = Bundle.main.url(forResource: PodspecName, withExtension: "bundle") {
        if let bundleURL = Bundle.main.url(forResource: user_basicData, withExtension: (noti_nameStr.lowercased())) {
            //: return Bundle(url: bundleURL) ?? Bundle.main
            return Bundle(url: bundleURL) ?? Bundle.main
        }
        //: return Bundle.main
        return Bundle.main
        //: }()
    }()

    /// 需要镜像的图片资源
    //: static let flipsImages: Set<String> = {
    static let flipsImages: Set<String> = //: return ["nav_back_black_nor",
        [(String(mainRecoverKey.suffix(5)) + "ack_black" + String(show_hourLostPath)),
         //: "icon_translation_go",
         (main_cocaString.lowercased() + "_transl" + kRecoverFormat.replacingOccurrences(of: "ex", with: "at") + "n_go"),
         //: "iv_me_vip",
         (String(dataElementSubjectResolveUrl.prefix(9))),
         //: "btn_dongtai_pinglun_nor",
         (String(showReflectString) + main_keyPath.lowercased() + String(main_beyondStr)),
         //: "btn_me_new_edit",
         (String(main_everyoneSharePath)),
         //: "btn_me_edit",
         (String(showConstraintPath.prefix(7)) + showBeginMsg.replacingOccurrences(of: "pet", with: "ed")),
         //: "icon_photo_giftbg",
         (String(noti_originFormat) + String(kSampleGameMsg)),
         //: "icon_gift_more_arrow",
         (String(show_oxygenDateValue)),
         //: "bg_talk_other",
         (String(show_numberName) + String(noti_structureBoardTitle)),
         //: "bg_talk_me",
         (String(appHandleName.suffix(8)) + constExactlyPeaKey.replacingOccurrences(of: "origin", with: "e")),
         //: "icon_talk_left_voive_1",
         (String(noti_normallyUrl) + String(const_windowData.prefix(6)) + "eft_v" + String(constStagePath)),
         //: "icon_talk_left_voive_2",
         (String(k_retStr) + String(dataBrushStr) + String(notiFreezeValue)),
         //: "icon_talk_left_voive_3",
         (String(constWithdrawLessData.prefix(6)) + "alk_lef" + String(kFellowCountPath.prefix(2)) + String(kErrPath.suffix(7))),
         //: "icon_talk_right_voive_1",
         (String(userMentionTitle.prefix(6)) + "alk_r" + String(noti_presentationStartPath.suffix(6)) + String(dataEndFormat.suffix(6))),
         //: "icon_talk_right_voive_2",
         (mainWaitName.lowercased() + String(main_subTriggerFactoryMsg.prefix(5)) + "_rig" + String(noti_ageFormat.suffix(4)) + "oive_2"),
         //: "icon_talk_right_voive_3",
         (String(data_serverTitle) + String(noti_impressiveShouldName)),
         //: "icon_gift_num_bg",
         (String(constReductionSignName) + String(mainRangeMessage.suffix(4))),
         //: "group_someoneatme",
         (String(noti_efficiencyUrl) + String(app_sparString.suffix(6)) + "neatme"),
         //: "btn_back_white",
         (String(userParticleTableAgeMsg.suffix(7)) + String(main_combinedId.prefix(7))),
         //: "icon_VectorStroke",
         (String(main_segmentName) + kLowMsg.replacingOccurrences(of: "simultaneously", with: "ro")),
         //: "btnJQbEAB_etihw_kcab_shadow",
         (String(user_absoluteCalledStr) + String(main_bagMsg)),
         //: "bg_talk_closed_tc_intimate",
         (String(const_alwaysDiscountPath) + String(data_singleScriptProcessingId)),
         //: "icon_talk_reply_voive",
         (String(mainWritPurpleTeeTweenData) + String(main_revenueTitle.suffix(6))),
         //: "ico_intimate_list_sm",
         (String(const_priorityValue.prefix(4)) + "inti" + String(kSmileId.suffix(6)) + String(mainMinimumString)),
         //: "female_bg_random",
         (String(const_regionSampleOffId) + main_dataString.replacingOccurrences(of: "entity", with: "m")),
         //: "party_mini_bg_userDetail"]
         (const_observerName.replacingOccurrences(of: "busy", with: "ar") + "_mini_" + String(kContrastValue.suffix(7)) + String(const_portraitAssetKey))]
    //: }()
}

//: public extension UIImage {
public extension UIImage {
    //: static func imageFillColor(color: UIColor) -> UIImage {
    static func trainActual(color: UIColor) -> UIImage {
        //: let size = CGSize(width: 1, height: 1)
        let size = CGSize(width: 1, height: 1)
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let image = renderer.image { context in
        let image = renderer.image { context in
            //: context.cgContext.setFillColor(color.cgColor)
            context.cgContext.setFillColor(color.cgColor)
            //: context.fill(CGRect(x: 0, y: 0, width: 1, height: 1))
            context.fill(CGRect(x: 0, y: 0, width: 1, height: 1))
        }
        //: return image
        return image
    }

    //: static func imageFillColor(color: UIColor, size: CGSize) -> UIImage {
    static func fellowProduct(color: UIColor, size: CGSize) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: color.set()
            color.set()
            //: context.fill(CGRect(x: 0, y: 0, width: size.width, height: size.height))
            context.fill(CGRect(x: 0, y: 0, width: size.width, height: size.height))
        }
        //: return img
        return img
    }

    //: static func imageFillColor(color: UIColor, size: CGSize, radius: CGFloat) -> UIImage {
    static func totalent(color: UIColor, size: CGSize, radius: CGFloat) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: let path = UIBezierPath(roundedRect: CGRect(x: 0, y: 0, width: size.width, height: size.height), cornerRadius: radius)
            let path = UIBezierPath(roundedRect: CGRect(x: 0, y: 0, width: size.width, height: size.height), cornerRadius: radius)
            //: context.cgContext.setFillColor(color.cgColor)
            context.cgContext.setFillColor(color.cgColor)
            //: context.cgContext.addPath(path.cgPath)
            context.cgContext.addPath(path.cgPath)
            //: context.cgContext.fillPath()
            context.cgContext.fillPath()
        }
        //: return img
        return img
    }

    //: static func imageGradientColor(colors: [CGColor], size: CGSize) -> UIImage {
    static func managerMouse(colors: [CGColor], size: CGSize) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: let gradientLayer = CAGradientLayer()
            let gradientLayer = CAGradientLayer()
            //: gradientLayer.colors = colors
            gradientLayer.colors = colors
            //: gradientLayer.startPoint = CGPoint(x: 0, y: 0.5)
            gradientLayer.startPoint = CGPoint(x: 0, y: 0.5)
            //: gradientLayer.endPoint = CGPoint(x: 1.0, y: 0.5)
            gradientLayer.endPoint = CGPoint(x: 1.0, y: 0.5)
            //: gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            //: gradientLayer.render(in: context.cgContext)
            gradientLayer.render(in: context.cgContext)
        }
        //: return img
        return img
    }

    //: static func imageGradientTopToBottonColor(colors: [CGColor], size: CGSize) -> UIImage {
    static func boxIn(colors: [CGColor], size: CGSize) -> UIImage {
        //: let renderer = UIGraphicsImageRenderer(size: size)
        let renderer = UIGraphicsImageRenderer(size: size)
        //: let img = renderer.image { context in
        let img = renderer.image { context in
            //: let gradientLayer = CAGradientLayer()
            let gradientLayer = CAGradientLayer()
            //: gradientLayer.colors = colors
            gradientLayer.colors = colors
            //: gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            gradientLayer.frame = CGRect(x: 0, y: 0, width: ceil(size.width), height: ceil(size.height))
            //: gradientLayer.render(in: context.cgContext)
            gradientLayer.render(in: context.cgContext)
        }
        //: return img
        return img
    }

    /// 实现高斯模糊效果
    /// - Parameters:
    ///   - theImage: 传入图片
    ///   - blurValue: 模糊程度
    /// - Returns: 输出图片
    //: static func blur(theImage: UIImage, blurValue: CGFloat = 15.0) -> UIImage {
    static func confidentSpell(theImage: UIImage, blurValue: CGFloat = 15.0) -> UIImage {
        //: let context = CIContext(options: nil)
        let context = CIContext(options: nil)
        //: let inputImage = CIImage(cgImage: theImage.cgImage!)
        let inputImage = CIImage(cgImage: theImage.cgImage!)
        //: let filter = CIFilter(name: "CIGaussianBlur")
        let filter = CIFilter(name: (String(constRemarkMessage.prefix(6)) + String(main_aliveValue)))
        //: filter?.setValue(inputImage, forKey: kCIInputImageKey)
        filter?.setValue(inputImage, forKey: kCIInputImageKey)
        //: filter?.setValue(blurValue, forKey: kCIInputRadiusKey)
        filter?.setValue(blurValue, forKey: kCIInputRadiusKey)
        //: let result = filter?.value(forKey: kCIOutputImageKey) as! CIImage
        let result = filter?.value(forKey: kCIOutputImageKey) as! CIImage
        //: let cgImage = context.createCGImage(result, from: inputImage.extent)
        let cgImage = context.createCGImage(result, from: inputImage.extent)
        //: let returnImage = UIImage(cgImage: cgImage!)
        let returnImage = UIImage(cgImage: cgImage!)
        //: return returnImage
        return returnImage
    }

    //: @objc static func BundleImageNamed(name: String) -> UIImage {
    @objc static func road(name: String) -> UIImage {
        //: guard name.count > 0 else {
        guard name.count > 0 else {
            //: return UIImage()
            return UIImage()
        }
        //: if let image = SVGAEffectTool.default.getImage(name: name) {
        if let image = DocumentWhite.default.pion(name: name) {
            //: return processImage(image: image, name: name)
            return ported(image: image, name: name)
        }
        //: return UIImage()
        return UIImage()
    }

    /// 镜像图片
    //: private static func processImage(image: UIImage, name: String) -> UIImage {
    private static func ported(image: UIImage, name: String) -> UIImage {
        //: guard LanguageManager.shared.direction == .rightToLeft else {
        guard TitlePer.shared.direction == .rightToLeft else {
            //: return image
            return image
        }
        //: if PodspecBundle.flipsImages.contains(name) {
        if ThroughTemp.flipsImages.contains(name) {
            //: return image.imageFlippedForRightToLeftLayoutDirection()
            return image.imageFlippedForRightToLeftLayoutDirection()
        }
        //: return image
        return image
    }

    //: @objc static func placeImgSquare() -> UIImage {
    @objc static func franklyBounce() -> UIImage {
        //: return BundleImageNamed(name: "icon_mid_picture_default")
        return road(name: (String(dataPanelPath) + user_ehId.replacingOccurrences(of: "barn", with: "i") + String(const_postTitle.suffix(6)) + "ure_" + mainHungPath.replacingOccurrences(of: "scientific", with: "f")))
    }

    //: static func placeImgBanner() -> UIImage {
    static func outfitDoing() -> UIImage {
        //: return BundleImageNamed(name: "icon_big_picture_default")
        return road(name: (String(dataYouMsg.suffix(5)) + "big_" + kAboutData.replacingOccurrences(of: "region", with: "i") + "ure_" + appProsecutionYellowData.lowercased()))
    }

    //: @objc static func placeSquareBigImgBanner() -> UIImage {
    @objc static func alongPatter() -> UIImage {
        //: return BundleImageNamed(name: "icon_big_picture_square")
        return road(name: (String(noti_teamTitle) + String(show_levelStr.suffix(7))))
    }

    //: @objc static func giftPlaceHolderImage() -> UIImage {
    @objc static func rangeWithdraw() -> UIImage {
        //: return BundleImageNamed(name: "gift_icon_default")
        return road(name: (constQualityPath.replacingOccurrences(of: "expose", with: "t") + "_icon" + String(mainEqualMortalMessage.suffix(7)) + user_extendName.replacingOccurrences(of: "time", with: "t")))
    }

    //: static func placeHolderImage(sex: String) -> UIImage {
    static func holiday(sex: String) -> UIImage {
        //: if sex == Gender.female.rawValue {
        if sex == SharedOdd.female.rawValue {
            //: return BundleImageNamed(name: "icon_female_icon_default")
            return road(name: (String(dataChapterUrl) + String(const_normallyUrl) + String(showOthersUrl.suffix(8))))
        }
        //: return BundleImageNamed(name: "icon_male_icon_default")
        return road(name: (String(appInnerConvertStr.suffix(6)) + "ale_icon" + String(mainTriggerUrl)))
    }

    //: static func placeImgCircle() -> UIImage {
    static func betweenSophisticated() -> UIImage {
        //: return BundleImageNamed(name: "icon_circle_pic_default")
        return road(name: (String(constAddressProgressNoteValue) + mainTakeData.replacingOccurrences(of: "spar", with: "r") + String(notiQuantityerruptUnlessEntryMessage)))
    }

    //: @objc static func resizedInImageWithName(name: String) -> UIImage {
    @objc static func tactInvisible(name: String) -> UIImage {
        //: let ret = UIImage.BundleImageNamed(name: name)
        let ret = UIImage.road(name: name)
        //: let edge = UIEdgeInsets(top: 30, left: 10, bottom: 10, right: 15)
        let edge = UIEdgeInsets(top: 30, left: 10, bottom: 10, right: 15)
        //: return ret.resizableImage(withCapInsets: edge, resizingMode: .stretch)
        return ret.resizableImage(withCapInsets: edge, resizingMode: .stretch)
    }

    //: func resize(with newSize: CGSize, highQuality: Bool = false) -> UIImage? {
    func giftImplementContainerComprehensive(with newSize: CGSize, highQuality: Bool = false) -> UIImage? {
        //: let _newSize = highQuality ? CGSize(width: newSize.width * 2, height: newSize.height * 2) : newSize
        let _newSize = highQuality ? CGSize(width: newSize.width * 2, height: newSize.height * 2) : newSize
        //: var scaleFactor: CGFloat = 0.0
        var scaleFactor: CGFloat = 0.0
        //: if !size.equalTo(newSize) {
        if !size.equalTo(newSize) {
            //: let widthFactor: CGFloat = _newSize.width / size.width
            let widthFactor: CGFloat = _newSize.width / size.width
            //: let heightFactor: CGFloat = _newSize.height / size.height
            let heightFactor: CGFloat = _newSize.height / size.height
            //: if widthFactor < heightFactor {
            if widthFactor < heightFactor {
                //: scaleFactor = heightFactor
                scaleFactor = heightFactor // scale to fit height
                //: } else {
            } else {
                //: scaleFactor = widthFactor
                scaleFactor = widthFactor // scale to fit width
            }
        }

        //: scaleFactor = min(scaleFactor, 1.0)
        scaleFactor = min(scaleFactor, 1.0)
        //: let targetWidth = ceil(size.width * scaleFactor)
        let targetWidth = ceil(size.width * scaleFactor)
        //: let targetHeight = ceil(size.height * scaleFactor)
        let targetHeight = ceil(size.height * scaleFactor)

        //: let renderer = UIGraphicsImageRenderer(size: CGSize(width: targetWidth, height: targetHeight))
        let renderer = UIGraphicsImageRenderer(size: CGSize(width: targetWidth, height: targetHeight))
        //: let newImage = renderer.image { _ in
        let newImage = renderer.image { _ in
            //: draw(in: CGRect(x: 0, y: 0, width: targetWidth, height: targetHeight))
            draw(in: CGRect(x: 0, y: 0, width: targetWidth, height: targetHeight))
        }
        //: return newImage
        return newImage
    }

    //: @objc static func base64EncodeImageRestore(imageData: String) -> UIImage {
    @objc static func corruptionDetect(imageData: String) -> UIImage {
        //: let urlString = "ata:image/jpeg;base64,\(imageData)"
        let urlString = (String(constThirdValue.prefix(8)) + String(userAddName.prefix(4)) + "eg;bas" + String(dataNetKey.prefix(4))) + "\(imageData)"
        //: let url = URL(string: urlString)
        let url = URL(string: urlString)
        //: if let data = try? Data(contentsOf: url!) {
        if let data = try? Data(contentsOf: url!) {
            //: let image = UIImage(data: data)!
            let image = UIImage(data: data)!
            //: return image
            return image
            //: } else {
        } else {
            //: return UIImage()
            return UIImage()
        }
    }
}

//: class ImageWithoutRender: UIImage {
class RangeWrap: UIImage {
    //: override func withRenderingMode(_ renderingMode: UIImage.RenderingMode) -> UIImage {
    override func withRenderingMode(_ renderingMode: UIImage.RenderingMode) -> UIImage {
        //: return self
        return self
    }
}
