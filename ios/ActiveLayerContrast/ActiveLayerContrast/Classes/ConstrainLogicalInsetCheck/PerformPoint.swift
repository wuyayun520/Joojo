
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_bodyMessage:[UInt8] = [0x2a,0x2d,0x2a,0x37,0x6b,0x20,0x2c,0x27,0x26,0x31,0x79,0x6a,0x63,0x2b,0x22,0x30,0x63,0x2d,0x2c,0x37,0x63,0x21,0x26,0x26,0x2d,0x63,0x2a,0x2e,0x33,0x2f,0x26,0x2e,0x26,0x2d,0x37,0x26,0x27]

private func raceCeiling(near num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "uid" :*/
fileprivate let noti_paintShallName:[UInt8] = [0x9e,0x82,0x8f]

private func absolutelyAssistant(bucket num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "headPic" :*/
fileprivate let dataCompareData:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let k_rhoName:String = "nicknamfee"

/*: "birthday" :*/
fileprivate let userSpitFrameValue:String = "birboyh"
fileprivate let mainMacKindKey:[Character] = ["d","a","y"]

/*: "videoUrl" :*/
fileprivate let notiRhoId:String = "hold favorite directly pea agentvideoUr"
fileprivate let app_themeStairsKey:String = "highlight"

/*: "cover" :*/
fileprivate let main_statusMsg:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let noti_findTitle:String = "bound"
fileprivate let noti_columnMessage:[Character] = ["c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let user_chemistHeaveString:[UInt8] = [0x83,0x8d,0x84]

fileprivate func villageDirectly(tense num: UInt8) -> UInt8 {
    let value = Int(num) + 224
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let appPositionFormat:String = "missionfuse"
fileprivate let userPeopleFormat:String = "Callreduce permanent private"

/*: "data" :*/
fileprivate let appGalleryQuitTitle:[UInt8] = [0xf3,0xf6,0xe3,0xf6]

/*: "Call interruption" :*/
fileprivate let show_sophisticatedString:String = "roll exit grade profileCall"
fileprivate let notiPlayMsg:String = " intmeet crush empty"
fileprivate let main_spotTitle:String = "tiinterestedn"

/*: "stopCall" :*/
fileprivate let showFormStr:String = "firsto"
fileprivate let userMareValue:[Character] = ["p","C","a","l","l"]

/*: "logId" :*/
fileprivate let main_difficultId:[UInt8] = [0x4b,0x48,0x40,0x6e,0x43]

private func meRegular(favorite num: UInt8) -> UInt8 {
    return num ^ 39
}

/*: "cancelCall" :*/
fileprivate let user_typicalKey:String = "CANCEL"

/*: "No answer" :*/
fileprivate let main_maleCoverId:[Character] = ["N","o"," ","a","n","s","w","e"]
fileprivate let user_sineYaQuoteName:[Character] = ["r"]

/*: "duration" :*/
fileprivate let showObserveValue:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let notiContractorData:String = "She has secure condition link"
fileprivate let kPurplePath:String = "hung uppress great spine"

/*: "cancelSex" :*/
fileprivate let mainFranklyData:String = "cancelSexfuture assert mare spell"

/*: "He has hung up" :*/
fileprivate let showDateStr:String = "He has humale which"
fileprivate let userWatchUrl:[Character] = ["n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let show_arenaPath:[Character] = ["e","n"]
fileprivate let notiDestinationKey:String = "dTypefrankly rating light load secret"

/*: "stopSex" :*/
fileprivate let kDiveMsg:String = "stopSexregular for keep elbow"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let app_resistanceValue:[UInt8] = [0xe,0x29,0x34,0x32,0x21,0x21,0x2e,0x24,0x2e,0x22,0x29,0x33,0x67,0x20,0x28,0x2b,0x23,0x67,0x24,0x28,0x2e,0x29,0x34,0x6b,0x67,0x24,0x26,0x2b,0x2b,0x67,0x2f,0x26,0x34,0x67,0x22,0x29,0x23,0x22,0x23]

private func recognizeExecute(team num: UInt8) -> UInt8 {
    return num ^ 71
}

/*: "The other hung up the phone" :*/
fileprivate let notiQuantityervalMessage:String = "far tool strip outsideThe "
fileprivate let userMenuTitle:String = "my accuracy ping grasshung u"
fileprivate let data_feeName:[Character] = [" ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let const_sensorUpperRankFormat:String = "TYPE"

/*: "jumpUrl" :*/
fileprivate let showDrawId:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let noti_rankId:String = "shall"
fileprivate let showExistingPickData:String = "aacrossel"

/*: "Get gold coins" :*/
fileprivate let notiTierTitle:String = "Get gprovide tag tun true"
fileprivate let noti_propertyMsg:[Character] = ["o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let showExplainValue:[Character] = ["r","e","f","u","s","e","S","e"]
fileprivate let appPriorityFormat:String = "subject"

/*: "onAcceptCall" :*/
fileprivate let appHeartFormat:[Character] = ["o","n"]
fileprivate let notiFindTitle:String = "Acceplet process"

/*: "acceptCall" :*/
fileprivate let notiButtFormat:String = "accrestrictionp"

/*: "Calls are cancelled" :*/
fileprivate let user_problemString:String = "Calls called entry"
fileprivate let show_approvePerTitle:String = "NCELLED"

/*: "Continue matching with other users？" :*/
fileprivate let dataCharacterUrl:[UInt8] = [0x9f,0xbc,0xef,0x73,0x72,0x65,0x73,0x75,0x20,0x72,0x65,0x68,0x74,0x6f,0x20,0x68,0x74,0x69,0x77,0x20,0x67,0x6e,0x69,0x68,0x63,0x74,0x61,0x6d,0x20,0x65,0x75,0x6e,0x69,0x74,0x6e,0x6f,0x43]

/*: "Yes" :*/
fileprivate let mainStorageString:String = "pack system every commentYes"

/*: "The call has ended" :*/
fileprivate let appAboutMonthStr:String = "multi rocket low discoverThe c"
fileprivate let show_fromMsg:String = "s endehidden bot"
fileprivate let constArchitectureString:[Character] = ["d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PerformPoint.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let constStageUrl = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class PerformPoint: TrainBrush {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: GreatEnhanceTimingAgileModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ExtendEquipment? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: DivideStrokeStorage? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ShapeTranslateView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ToPresent.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToPresent.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToPresent.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_bodyMessage.map{raceCeiling(near: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MediaAfter
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.withinMarker()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            admin()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            parentSince(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.underCount()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        applicationPin()
        //: setupSubViewsConstraint()
        totalervalFollow()
        //: initNotificationObserver()
        miniEqually()
        //: req_callGetUserInfo()
        nextSection()
        //: timeoutRules()
        quoteFollowing()
        //: setUpBGMPlayer()
        nameMethod()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.sendOrientation()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
            if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = DetailView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.byWork = false
        ToPresent.shared.byWork = false
        //: TalkingSocketManager.shared.isCalling = false
        ToPresent.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.listOn()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: ActionReduce = {
        //: let v = TalkingBGVideoView()
        let v = ActionReduce()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.bottomDown(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: GlobalCouldView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = GlobalCouldView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ShouldDifficult = {
        //: let v = TalkingVideoWindowView()
        let v = ShouldDifficult()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = show_licenseTitle - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if TitlePer.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: show_orientationKey + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.erase()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: OriginalMaterial = {
        //: let m = TalkingVideoChatManager()
        let m = OriginalMaterial()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func nextSection() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        OriginalMaterial.doingtoTo(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: noti_paintShallName.map{absolutelyAssistant(bucket: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(dataCompareData))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(k_rhoName.replacingOccurrences(of: "fee", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(userSpitFrameValue.replacingOccurrences(of: "boy", with: "t") + String(mainMacKindKey))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(notiRhoId.suffix(7)) + app_themeStairsKey.replacingOccurrences(of: "highlight", with: "l"))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(main_statusMsg))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(noti_findTitle.replacingOccurrences(of: "bound", with: "lo") + String(noti_columnMessage))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.fullFor()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.media()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func media() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = SendBridge.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = SendBridge.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.belowProfile(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.handle(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func admin() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ContainPlate.tiff { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.bury(showMsg: kTextStr)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (appPositionFormat.replacingOccurrences(of: "mission", with: "re") + String(userPeopleFormat.prefix(4))), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: noti_paintShallName.map{absolutelyAssistant(bucket: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.counto()
                //: return
                return
            }

            //: self.startPreview()
            self.sendOrientation()
            //: self.stopBGM()
            self.boardFromRoman()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ExtendEquipment.brushWrite(type: .Response)
            //: self.startBGM()
            self.withinMarker()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func sendOrientation() {
        //: self.videoManager.startPreview()
        self.videoManager.mountAcross()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func listOn() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.boardFromRoman()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        KeepMethod.magnitudeoMerge()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ToPresent.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ToPresent.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ToPresent.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func counto(animated: Bool = true) {
        //: self.stopBGM()
        self.boardFromRoman()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func erase() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.loop()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.media()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func ignore() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.rewardOnMechanism()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.media()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.albumAcross()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func albumAcross() {
        //: guard ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = ShapeTranslateView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.alongUponDiscount()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func miniEqually() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(impressionRetain(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(substantialInstead(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: appAttributeData,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(duringDiscoverSpark(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: data_singleTitle,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        constStageUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func impressionRetain(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            counto()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func duringDiscoverSpark(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ToPresent.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ToPresent.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToPresent.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToPresent.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.event()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func substantialInstead(notification: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.media()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension PerformPoint: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.bury(showMsg: (String(show_sophisticatedString.suffix(4)) + String(notiPlayMsg.prefix(4)) + "errup" + main_spotTitle.replacingOccurrences(of: "interested", with: "o")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (showFormStr.replacingOccurrences(of: "first", with: "st") + String(userMareValue)), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (user_typicalKey.lowercased() + "Call"), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (appPositionFormat.replacingOccurrences(of: "mission", with: "re") + String(userPeopleFormat.prefix(4))), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: noti_paintShallName.map{absolutelyAssistant(bucket: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.before()
                //: TalkingSocketManager.shared.byWorkVC = nil
                ToPresent.shared.byWorkVC = nil
            }
            //: popCurrentViewController()
            counto()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func nameMethod() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? StopIndex.Call : StopIndex.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ExtendEquipment.brushWrite(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func withinMarker() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.belowCalled()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func boardFromRoman() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.indicator()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 超时定时器
    //: private func timeoutRules() {
    private func quoteFollowing() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_viewMsg : self.chatModel?.past()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            askPut()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.askPut()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func askPut() {
        //: guard !TalkingSocketManager.shared.byWork else { return }
        guard !ToPresent.shared.byWork else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.bury(showMsg: (String(main_maleCoverId) + String(user_sineYaQuoteName)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (user_typicalKey.lowercased() + "Call"), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (appPositionFormat.replacingOccurrences(of: "mission", with: "re") + String(userPeopleFormat.prefix(4))), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: noti_paintShallName.map{absolutelyAssistant(bucket: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ToPresent.shared.formLock(info: socketDict)
        //: popCurrentViewController()
        counto()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func enabled(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(showObserveValue))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.implementFromExample(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.nearby()
            //: createTalkRoom()
            ignore()
            //: self.stopBGM()
            self.boardFromRoman()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func parentSince(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.loop()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.bottomDown(show: false)
        //: self.stopBGM()
        self.boardFromRoman()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.nearby()
        //: self.createTalkRoom()
        self.ignore()
    }
}

// MARK: - ListenerShared【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension PerformPoint: ListenerShared {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func underRespective(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(notiContractorData.prefix(8)) + String(kPurplePath.prefix(7))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(mainFranklyData.prefix(9)))].stringValue == SharedOdd.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showDateStr.prefix(9)) + String(userWatchUrl)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bury(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.before()
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }

        //: popCurrentViewController()
        counto()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func featureReplacement(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        parentSince(logId: json[String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func robot(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        ImmediateThan.mage()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(show_arenaPath) + String(notiDestinationKey.prefix(5)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(notiContractorData.prefix(8)) + String(kPurplePath.prefix(7))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kDiveMsg.prefix(7)))].stringValue == SharedOdd.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(showDateStr.prefix(9)) + String(userWatchUrl)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue {
            if ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: app_resistanceValue.map{recognizeExecute(team: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(notiQuantityervalMessage.suffix(4)) + "other " + String(userMenuTitle.suffix(6)) + "p the" + String(data_feeName)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bury(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.before()
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.someRating()
        //: popCurrentViewController()
        counto()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func offFunctional(msg: String, data: [String: Any]) {
        //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(const_sensorUpperRankFormat.lowercased())] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(showDrawId))] as? String else { return }
            //: let httpsUrl = ChartShowSceneScientific.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = ChartShowSceneScientific.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = LocationMove()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: BeforeAssociate.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            BeforeAssociate.share.magnitudeer(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ImmediateThan.beanTransform(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (noti_rankId.replacingOccurrences(of: "shall", with: "C") + showExistingPickData.replacingOccurrences(of: "across", with: "nc")).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(notiTierTitle.prefix(5)) + "old c" + String(noti_propertyMsg)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ImmediateThan.mage()
                //: } rightBlock: {
            } rightBlock: {
                //: BeforeAssociate.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                BeforeAssociate.share.computeEstimated(clickEvent: k_windowCommandTitle, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func underDiscover(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.rebuildAcross(giftArr: giftArr)
    }
}

// MARK: - MayFactoryExclusive【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PerformPoint: MayFactoryExclusive {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func stackFor(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(notiContractorData.prefix(8)) + String(kPurplePath.prefix(7))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(showExplainValue) + appPriorityFormat.replacingOccurrences(of: "subject", with: "x"))].stringValue == SharedOdd.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showDateStr.prefix(9)) + String(userWatchUrl)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bury(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.before()
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }

        //: popCurrentViewController()
        counto()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func pathWith(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        parentSince(logId: json[String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - BoxFind【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension PerformPoint: BoxFind {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func weeklyImplement(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (appPositionFormat.replacingOccurrences(of: "mission", with: "re") + String(userPeopleFormat.prefix(4))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.bury(showMsg: (String(main_maleCoverId) + String(user_sineYaQuoteName)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.before()
                //: TalkingSocketManager.shared.byWorkVC = nil
                ToPresent.shared.byWorkVC = nil
            }
            //: popCurrentViewController()
            counto()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(appHeartFormat) + String(notiFindTitle.prefix(5)) + "tCall") && errorNo == 101 { // 接听通话时，余额不足
            //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return }
            //: BeforeAssociate.share.func__jumpToWebRecharge(sufficient: false)
            BeforeAssociate.share.computeEstimated(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(appHeartFormat) + String(notiFindTitle.prefix(5)) + "tCall") && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.bury(showMsg: msg)
            //: popCurrentViewController()
            counto()
        }
    }
}

// MARK: - PersistConstruct【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension PerformPoint: PersistConstruct {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func priceHero() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (notiButtFormat.replacingOccurrences(of: "restriction", with: "e") + "tCall"), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: noti_paintShallName.map{absolutelyAssistant(bucket: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func filterDown() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bury(showMsg: (String(user_problemString.prefix(6)) + "are ca" + show_approvePerTitle.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (user_typicalKey.lowercased() + "Call"), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bury(showMsg: (String(user_problemString.prefix(6)) + "are ca" + show_approvePerTitle.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (appPositionFormat.replacingOccurrences(of: "mission", with: "re") + String(userPeopleFormat.prefix(4))), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: noti_paintShallName.map{absolutelyAssistant(bucket: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                ImmediateThan.beanTransform(title: nil, message: String(bytes: dataCharacterUrl.reversed(), encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(mainStorageString.suffix(3))).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.todaySelect(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.todaySelect(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.bury(showMsg: (String(appAboutMonthStr.suffix(5)) + "all ha" + String(show_fromMsg.prefix(6)) + String(constArchitectureString)).localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (showFormStr.replacingOccurrences(of: "first", with: "st") + String(userMareValue)), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        counto()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func todaySelect(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.bury(showMsg: (String(appAboutMonthStr.suffix(5)) + "all ha" + String(show_fromMsg.prefix(6)) + String(constArchitectureString)).localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        ToPresent.shared.formLock(info: [String(bytes: user_chemistHeaveString.map{villageDirectly(tense: $0)}, encoding: .utf8)!: (showFormStr.replacingOccurrences(of: "first", with: "st") + String(userMareValue)), String(bytes: appGalleryQuitTitle.map{$0^151}, encoding: .utf8)!: [String(bytes: main_difficultId.map{meRegular(favorite: $0)}, encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is LengthViewController }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            counto()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: user_coverPath, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func transaction(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.albumState(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func particleService(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.bottomDown(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.valueSingle(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.media()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func anyHost() {
        //: TalkingSocketManager.shared.byWorkVC = self
        ToPresent.shared.byWorkVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = DivideStrokeStorage.weekScript()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.reasonInCustom()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.upgrade()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.chemist()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        counto()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func smart() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension PerformPoint {
    /// 添加视图
    //: private func setupSubviews() {
    private func applicationPin() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.chartOrFlush(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func totalervalFollow() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
