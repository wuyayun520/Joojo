
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let const_expectedData:[Character] = ["I","M","\u{9000}","出","登","录","失","败","，","c"]
fileprivate let noti_dynamicsReserveMsg:String = "respective string depthode:"

/*: , desc: :*/
fileprivate let show_noteKey:[Character] = [","," ","d","e","s","c",":"]

/*: "IM登录失败，code: :*/
fileprivate let show_warningKey:String = "IM\u{767b}录"
fileprivate let const_architectureMessage:String = "favorite long ad better whitede:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let showSisPingId:String = "IM获\u{53d6}"
fileprivate let data_classicMessage:String = "still living mystery strengthcode:"

/*: "crushBadge" :*/
fileprivate let userParticleMsg:[Character] = ["c","r","u","s","h","B","a","d"]
fileprivate let showDirectionMixKey:[Character] = ["g","e"]

/*: "badNumber" :*/
fileprivate let showEaseUrl:[UInt8] = [0xeb,0xea,0xed,0xd7,0xfe,0xf6,0xeb,0xee,0xfb]

fileprivate func distinctionSure(foundation num: UInt8) -> UInt8 {
    let value = Int(num) - 137
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let dataExtraUrl:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let kAdviceKey:String = "roomNumremain appeal join"

/*: "num" :*/
fileprivate let main_associatedData:String = "remarkm"

/*: "onlyOneRoomId" :*/
fileprivate let dataPairDisplayMedalFormat:[Character] = ["o","n","l","y","O","n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let data_fastToleranceStr:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let dataTiffString:String = "extrdented"

/*: "msgType" :*/
fileprivate let kBlackRangeStr:String = "project"
fileprivate let constHeatAbsolutePath:[Character] = ["s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let notiTakeTitle:[Character] = ["n","e","w","P","k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let data_systemOxygenProfessionalMsg:[Character] = ["M","F",":","C","h","a","t","M","e","n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let main_floorDomainMsg:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let showRoadMessage:[UInt8] = [0x76,0x75,0x73,0x70,0x75,0x6c,0x5a,0x7b,0x68,0x7b,0x7c,0x7a]

fileprivate func beginReduction(policy num: UInt8) -> UInt8 {
    let value = Int(num) - 7
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let notiResistanceKey:[UInt8] = [0xa4,0xbe,0x83,0xa8,0xba,0x98,0xbe,0xa8,0xbf]

private func weekNow(disclose num: UInt8) -> UInt8 {
    return num ^ 205
}

/*: "c2c_ :*/
fileprivate let user_easyString:String = "assert spend oi amongc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let data_implementMessageStr:[Character] = ["I","M","获","\u{53d6}","会","话","失","\u{8d25}","\u{ff0c}","c","o"]
fileprivate let main_memoEachData:[Character] = ["n","v","I","d",":"]

/*: , code: :*/
fileprivate let dataSingleSceneMsg:String = ", code:group before ready now hi"

/*: "IM插入消息失败，targetId: :*/
fileprivate let userStaffStr:String = "hung mix reduce go thereforIM插入消息"
fileprivate let k_keepUrl:String = "split markgetId"
fileprivate let dataTimeMsg:[Character] = [":"]

/*: "changeDirection" :*/
fileprivate let kSecondEhString:String = "chabetweeng"
fileprivate let notiAfterBackgroundMessage:String = "boactio"
fileprivate let appAspectName:String = "item"

/*: "rYMsgType" :*/
fileprivate let dataMpManData:String = "way temporary addressrYMsgT"
fileprivate let constStepMessage:String = "yattributee"

/*: "GJ:CallCustom" :*/
fileprivate let k_tableFemaleString:String = "strip sound noun drink beforeGJ:Ca"
fileprivate let noti_alwaysCorruptionPath:String = "llCuremote writing repeat written"

/*: "IM获取会话列表失败，code: :*/
fileprivate let userRequireUrl:String = "IM\u{83b7}\u{53d6}"
fileprivate let kArenaData:[Character] = ["o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let showShallMessage:String = "follow withdrawmsgInfo"

/*: "needFold" :*/
fileprivate let data_suspendYearName:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let noti_valueData:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let user_databaseFormat:[Character] = ["I","M","删","除","会"]
fileprivate let appPhonePath:[Character] = ["话","失"]
fileprivate let k_plainStr:String = "\u{8d25}\u{ff0c}convID:"

/*: "IM置顶会话失败，convId: :*/
fileprivate let constTriggerInvisibleFormat:[Character] = ["I","M","置","顶","会"]
fileprivate let notiMapMessage:String = "话失败\u{ff0c}co"

/*: "SDK 正在连接到服务器" :*/
fileprivate let noti_sustainableName:String = "SDK 正在investigate sequence color"
fileprivate let dataSecurityShapeValue:String = "no"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let show_sentenceName:[Character] = ["S","D","K"," ","\u{5df2}"]
fileprivate let kItemAssessmentPath:String = "\u{7ecf}成功连"

/*: "IM连接服务器失败，code: :*/
fileprivate let showMomentumString:[Character] = ["I","M","连","接","服","务","器","失","败","，"]
fileprivate let notiRestrictionPath:[Character] = ["c","o","d","e",":"]

/*: , error: :*/
fileprivate let noti_greenData:String = "phase grain, error:"

/*: "IM账号被挤下线。" :*/
fileprivate let appMePath:[Character] = ["I","M","账","\u{53f7}","被","\u{6324}","下","线"]
fileprivate let notiReasonTitle:[Character] = ["。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let show_instanceTitle:[UInt8] = []

fileprivate func outerFinish(equipment num: UInt8) -> UInt8 {
    let value = Int(num) + 14
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let kDetailedMessage:String = "home make bringOfflin"
fileprivate let constMagnitudeensityIsolateId:String = "sensoricatio"
fileprivate let dataThatString:[Character] = ["n"]

/*: "Cancel" :*/
fileprivate let appHeatMessage:String = "Cancelicon grade plus abs"

/*: "Re Login" :*/
fileprivate let userCompositionMessage:[Character] = ["R","e"," ","L","o","g","i"]
fileprivate let const_businessMsg:String = "rebuild"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InputDrawing.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol OddAway: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func towardField(data: [DistinctionLocation])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func belowAssociated(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func submit(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum MultipleConsumption {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class InputDrawing: NSObject {
@objcMembers public class InputDrawing: NSObject {
    //: public static let shared = InputDrawing()
    public static let shared = InputDrawing()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [DistinctionLocation] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [DistinctionLocation] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [DistinctionLocation] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [DistinctionLocation] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [DistinctionLocation] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: DistinctionLocation]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.environment()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func info() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(dataSpotData, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func environment() {
        //: func__removeAllDelegate()
        scopeStyleYellowMaterial()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.service()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func service() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = DistinctionLocation.globalToday(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = DistinctionLocation.globalToday(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func user() -> [DistinctionLocation] {
        // 审核模式，在消息列表移除官方客服
        //: if ChartShowSceneScientific.share.appStatus == AppSkinStatus.special.rawValue {
        if ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == CharacteristicSine.reflectOff() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension InputDrawing {
extension InputDrawing {
    //: func func__removeAllDelegate() {
    func scopeStyleYellowMaterial() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func divideVisit(_ delegate: OddAway) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func constraintWith(_ delegate: OddAway) {
        //: self.func__invokeDelegate {
        self.hero {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func hero(_ invocation: (OddAway) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! OddAway)
        }
    }
}

// MARK: - IM登录态管理

//: extension InputDrawing {
extension InputDrawing {
    /// 退出登录IM
    //: func func__LogingOut() {
    func arrayWould() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            KitTolerance.evenIndex(msg: (String(const_expectedData) + String(noti_dynamicsReserveMsg.suffix(4))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.environment()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func city(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = ChartShowSceneScientific.share.appUserConfigMode.uSig
        let uSigStr = ChartShowSceneScientific.share.appUserConfigMode.uSig
        //: let uidStr = ChartShowSceneScientific.share.loginUserMode.userID
        let uidStr = ChartShowSceneScientific.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.anVidEx()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            KitTolerance.evenIndex(msg: (show_warningKey + "失败，co" + String(const_architectureMessage.suffix(3))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func crush() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || ChartShowSceneScientific.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || ChartShowSceneScientific.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.bury(showMsg: show_plainHalfName)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension InputDrawing {
extension InputDrawing {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func anVidEx() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            KitTolerance.evenIndex(msg: (showSisPingId + "未读消息\u{603b}数失败，" + String(data_classicMessage.suffix(5))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func regulateHeap() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        NameProjection.ask { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(userParticleMsg) + String(showDirectionMixKey))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kModeDownString, object: self, userInfo: [String(bytes: showEaseUrl.map{distinctionSure(foundation: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func popular(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        NameProjection.ceilingAlive { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(dataExtraUrl))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(kAdviceKey.prefix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = DistinctionLocation.globalToday(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(main_associatedData.replacingOccurrences(of: "remark", with: "nu"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(dataPairDisplayMedalFormat))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension InputDrawing: V2TIMAdvancedMsgListener {
extension InputDrawing: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(data_fastToleranceStr)))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            RestrictionAction.doUser().receive(msg: msg)
            //: return
            return
        }
        //: if msg.sender == ChartShowSceneScientific.share.loginUserMode.userID { return }
        if msg.sender == ChartShowSceneScientific.share.loginUserMode.userID { return }

        //: let dic = CompareOddSecond.parseTXMessageData(data: msg.customElem.data)
        let dic = CompareOddSecond.activity(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(dataTiffString.replacingOccurrences(of: "dented", with: "a"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ItemModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: showMomentumName, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(kBlackRangeStr.replacingOccurrences(of: "project", with: "m") + String(constHeatAbsolutePath))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(notiTakeTitle)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: constVoiceId, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if CompareOddSecond.checkVoiceMessage(message: msg) {
            if CompareOddSecond.presentation(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = AgainstName.terms(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                MoveBar.enableFrom(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.originHeapTween(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SignificantLight.shared.processorSmart(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SignificantLight.shared.belowMobile(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(data_systemOxygenProfessionalMsg)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ElementUnder.share.uponSchedule(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: FactoryWriteTranslateFound.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: FactoryWriteTranslateFound.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != CharacteristicSine.beforeVent() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.hero { $0.submit?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            BlackSignAlongSaveTake.shared.report(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: main_floorDomainMsg.reversed(), encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: showRoadMessage.map{beginReduction(policy: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: notiResistanceKey.map{weekNow(disclose: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: userFlexStr,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            TranslateScreen.execute(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            FactoryWriteTranslateFound.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: k_robotString, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: EmbraceShadow, isPrivete: Bool) {
    @objc public func observe(targetId: String, msgCellData: EmbraceShadow, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.pickWith(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(user_easyString.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.tableIndex(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.taSource(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                KitTolerance.evenIndex(msg: (String(data_implementMessageStr) + String(main_memoEachData)) + "\(convId)" + (String(dataSingleSceneMsg.prefix(7))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func pickWith(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = ChartShowSceneScientific.share.loginUserMode.userID
        let senderStr = ChartShowSceneScientific.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
//                KitTolerance.evenIndex(msg: (String(userStaffStr.suffix(6)) + "\u{5931}\u{8d25}，tar" + String(k_keepUrl.suffix(5)) + String(dataTimeMsg)) + "\(targetId)" + (String(dataSingleSceneMsg.prefix(7))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func originHeapTween(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(kSecondEhString.replacingOccurrences(of: "between", with: "n") + "eDir" + notiAfterBackgroundMessage.replacingOccurrences(of: "boa", with: "e") + appAspectName.replacingOccurrences(of: "item", with: "n"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && BeforeAssociate.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(dataMpManData.suffix(6)) + constStepMessage.replacingOccurrences(of: "attribute", with: "p"))].stringValue == (String(k_tableFemaleString.suffix(5)) + String(noti_alwaysCorruptionPath.prefix(4)) + "stom") && isSelf && BeforeAssociate.share.mare()!.isKind(of: RunViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: const_paintId) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: const_paintId)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension InputDrawing: V2TIMConversationListener {
extension InputDrawing: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.tableIndex(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.taSource(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.tableIndex(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.taSource(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: ChartShowSceneScientific.share.unreadMessageNum = Int(totalUnreadCount)
        ChartShowSceneScientific.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.hero {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.belowAssociated?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func taSource(conVersationList: [DistinctionLocation]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if TranslateScreen.density().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                TranslateScreen.density().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.hero {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.towardField?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func pauseFromCharmBrush(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [DistinctionLocation] = []

        // V2TIMConversation->DistinctionLocation
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = DistinctionLocation(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.op(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [DistinctionLocation] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = OutsideTierRadio.factory(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.level(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension InputDrawing {
extension InputDrawing {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func speaker(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.pauseFromCharmBrush(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.taSource(conVersationList: arr as? [DistinctionLocation] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            KitTolerance.evenIndex(msg: (userRequireUrl + "会\u{8bdd}列表\u{5931}败，c" + String(kArenaData)) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func remain(targetId: String) -> DistinctionLocation? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func tableIndex(convList: [V2TIMConversation]) -> [DistinctionLocation] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [DistinctionLocation] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = DistinctionLocation(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.op(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.level(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func level(convModel: DistinctionLocation, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.service()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: MultipleConsumption = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - ChartShowSceneScientific.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - ChartShowSceneScientific.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = TranslateScreen.drownBlindAnonymousEnhanceSetProvide(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = CompareOddSecond.parseTXMessageData(data: elemData)
            let dict = CompareOddSecond.activity(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(dataTiffString.replacingOccurrences(of: "dented", with: "a"))][(String(showShallMessage.suffix(7)))][(String(data_suspendYearName))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.warningEnable(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.warningEnable(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func warningEnable(convList: [DistinctionLocation], dataType: MultipleConsumption) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func someBegin(targetId: String, dataType: MultipleConsumption) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(user_easyString.suffix(4))) + "\(targetId)" : (String(noti_valueData)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            TranslateScreen.minService(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(ChartShowSceneScientific.share.loginUserMode.userID, toUid: targetId)
            MoveBar.easeDown(ChartShowSceneScientific.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.anVidEx()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            KitTolerance.evenIndex(msg: (String(user_databaseFormat) + String(appPhonePath) + k_plainStr) + "\(convID)" + (String(dataSingleSceneMsg.prefix(7))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func cityOf(convModel: DistinctionLocation, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            KitTolerance.evenIndex(msg: (String(constTriggerInvisibleFormat) + notiMapMessage + "nvId:") + "\(convModel.conversationID)" + (String(dataSingleSceneMsg.prefix(7))) + "\(code)" + (String(show_noteKey)) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.warningEnable(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.warningEnable(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension InputDrawing: V2TIMSDKListener {
extension InputDrawing: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(noti_sustainableName.prefix(6)) + "连接到服" + dataSecurityShapeValue.replacingOccurrences(of: "no", with: "务器")))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(show_sentenceName) + kItemAssessmentPath + "接到服务\u{5668}"))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        KitTolerance.evenIndex(msg: (String(showMomentumString) + String(notiRestrictionPath)) + "\(code)" + (String(noti_greenData.suffix(8))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        KitTolerance.evenIndex(msg: (String(appMePath) + String(notiReasonTitle)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        ToPresent.shared.everyAccelerateTransform(userLoginOut: true)
        //: InputDrawing.shared.isConnection = false
        InputDrawing.shared.isConnection = false

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: show_instanceTitle.map{outerFinish(equipment: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = MissionField()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if TotalSearch.share.interfaceLang == ChainAgile.ar.rawValue || TotalSearch.share.interfaceLang == ChainAgile.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            TotalSearch.share.interfaceLang == ChainAgile.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.pass(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        ImmediateThan.beanTransform()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        ImmediateThan.beanTransform(title: (String(kDetailedMessage.suffix(6)) + "e Not" + constMagnitudeensityIsolateId.replacingOccurrences(of: "sensor", with: "if") + String(dataThatString)).localized, message: msg, leftBtnTitle: (String(appHeatMessage.prefix(6))).localized, rightBtnTitle: (String(userCompositionMessage) + const_businessMsg.replacingOccurrences(of: "rebuild", with: "n")).localized, leftBlock: {
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            SelectedReload.actual { t in
                //: ProgressHUD.dismiss()
                GrayView.at()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: const_dimensionFrameMsg, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            SelectedReload.enabletoGreen { t in
                //: if t {
                if t {
                    //: InputDrawing.shared.func__LogingIn { _ in
                    InputDrawing.shared.city { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if CaptureUsed.mutualAcross().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(CaptureUsed.mutualAcross().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    ToPresent.shared.childDaily()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
