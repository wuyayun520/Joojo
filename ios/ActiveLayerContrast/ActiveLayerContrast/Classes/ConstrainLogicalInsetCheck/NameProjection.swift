
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let noti_indicationId:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","g","i","f","t","L","i"]
fileprivate let noti_receiverOnceStr:String = "ST"

/*: "type" :*/
fileprivate let main_whyName:[UInt8] = [0xa7,0xaa,0xa3,0xb6]

private func oiAutomat(could num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let const_accountLeaveData:String = "mf/chatRlab thin submit contact withdraw"
fileprivate let app_halfKey:[Character] = ["o","o","m","/","g"]
fileprivate let dataInserterStr:String = "opportunity within left regular challengeiftList"

/*: "live/giftList" :*/
fileprivate let mainReasonMotivePath:[Character] = ["l","i","v","e","/","g","i","f","t","L","i","s","t"]

/*: "IM/getMessageList" :*/
fileprivate let showExactlyKey:String = "IM/getMeslike classic"
fileprivate let kEagerOiStr:[Character] = ["s"]
fileprivate let show_availableBurnMessage:String = "span measureageList"

/*: "mf/business/sendMsg" :*/
fileprivate let kGrinData:[Character] = ["m","f","/","b","u"]
fileprivate let notiTabFormat:String = "sfactoress"
fileprivate let data_domainTitle:[Character] = ["/","s","e","n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let show_personId:[Character] = ["m","f","/","c","h"]
fileprivate let mainDirectlyStr:[Character] = ["a","t","R","o","o","m","/","s"]
fileprivate let mainBadUrl:[Character] = ["e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let show_somethingStr:[Character] = ["m","f","/","b","u","s"]
fileprivate let mainRefKey:String = "whon"
fileprivate let showFlexibilityString:String = "target calendar old from staffess/s"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let dataUniquePath:String = "mf/chagain spark warn require"
fileprivate let appUsExceptionName:String = "can maintain turnm/se"
fileprivate let noti_farData:String = "spell"

/*: "live/sendGift" :*/
fileprivate let k_deadlinePath:[Character] = ["l","i","v","e","/","s","e","n","d","G","i","f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let mainMotiveUrl:String = "super consent upper kindmf/use"
fileprivate let user_sinkStructureMessage:String = "subsequent intro uponChatInfo"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let noti_shouldUrl:String = "IM/gday then orientation lightly notice"
fileprivate let noti_platPath:String = "sageUaround increasingly then elbow"
fileprivate let dataRateSpringValue:String = "past nativefoList"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let kMigrationMsg:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o"]
fileprivate let mainDimensionWedKey:[Character] = ["L","i"]
fileprivate let app_feeFormat:[Character] = ["k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let dataThirdPreviousString:[Character] = ["m","f","/","c","r","u"]
fileprivate let showGrassTitle:String = "line exist whosh/ignore"

/*: "mf/crush/like" :*/
fileprivate let k_renderMendBlueKey:[Character] = ["m","f","/","c","r","u","s"]
fileprivate let userIndicatorStr:String = "contain scenario eventh/like"

/*: "mf/user/getRelation" :*/
fileprivate let app_canvasKey:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","R","e","l"]
fileprivate let userCeilingMarkKey:[Character] = ["a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let show_titleMessage:[UInt8] = [0xfd,0xfb,0xed,0xfa,0xb7,0xe9,0xfc,0xfc,0xed,0xf6,0xfc,0xf1,0xf7,0xf6]

fileprivate func perDrop(reward num: UInt8) -> UInt8 {
    let value = Int(num) - 136
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/removeAttention" :*/
fileprivate let k_platValue:String = "existing inside visual ting resolveuser/rem"
fileprivate let k_containName:String = "mare alloveA"

/*: "follow Success" :*/
fileprivate let user_additionalTitle:[Character] = ["f","o","l","l","o","w"," ","S"]
fileprivate let data_resolutionEasterFormat:String = "formatcess"

/*: "unfollow Success" :*/
fileprivate let noti_sunDrawMsg:[Character] = ["u","n","f","o","l","l","o","w"]
fileprivate let main_bagEveryTitle:String = "rag stage Suc"

/*: "translate/msg" :*/
fileprivate let user_satisfyUrl:String = "trinfrastructuren"
fileprivate let notiExclusivePath:String = "sea great present bosse/msg"

/*: "msgId" :*/
fileprivate let dataShallNativeTitle:[UInt8] = [0x2f,0x31,0x25,0xb,0x26]

/*: "targetLng" :*/
fileprivate let main_relationYetMessage:[UInt8] = [0x34,0x21,0x32,0x27,0x25,0x34,0xc,0x2e,0x27]

private func parentLeave(large num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let const_privacyStandardName:String = "mf/gmode hung fatal team"
fileprivate let show_monthSpaceArrayStr:String = "etAutoheight spec"
fileprivate let show_networkOurMessage:String = "view aspect clock smalltContent"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let userDeadlineData:String = "mf/gralong cart re distribution"
fileprivate let app_countMediaProcedureTitle:String = "over top account readingtInt"
fileprivate let dataHusbandMsg:String = "presidencyoto"

/*: "toUid" :*/
fileprivate let k_awakeMessage:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "mf/chatRoom/info" :*/
fileprivate let kGuideName:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let user_bagKey:[Character] = ["m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let user_mixString:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let noti_sampleFormat:[Character] = ["/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let noti_ratePath:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let showOnlyEnableId:[UInt8] = [0xd2,0xd9,0xd9,0xd8,0xf9,0xc4,0xc8,0xce,0xdd,0xf5,0xd2,0xda,0xd3]

/*: "mf/chatRoom/check" :*/
fileprivate let notiDrownPath:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","c","h","e","c","k"]

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let app_agreementKey:String = "dismiss inmate resumemf/cha"
fileprivate let appTroubleFormat:String = "distance vent child grain us/joinOrQ"
fileprivate let constEvenMsg:[Character] = ["u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let showNowViewMessage:String = "month"
fileprivate let userHappyQualityId:String = "mouse sentence interrupt exception place/chatR"
fileprivate let const_carrierTitle:String = "membedented"

/*: "mf/user/queryStatus" :*/
fileprivate let noti_raceMsg:[Character] = ["m","f","/","u","s","e","r","/","q","u"]
fileprivate let mainEconomicalDialogData:String = "ERY"
fileprivate let noti_eventTitle:[Character] = ["S","t","a","t","u","s"]

/*: "toUids" :*/
fileprivate let appCommonFormat:[UInt8] = [0x73,0x64,0x69,0x55,0x6f,0x74]

/*: "call/getCallTab" :*/
fileprivate let dataThatMsg:String = "call/gback decision"
fileprivate let const_countPath:[Character] = ["T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let show_impactProvideStr:[Character] = ["m","f","/","u","s","e","r","/","r","e","m","a"]
fileprivate let show_providerPresentationStr:[Character] = ["r","k"]

/*: "content" :*/
fileprivate let noti_businessName:[UInt8] = [0x67,0x6b,0x6a,0x70,0x61,0x6a,0x70]

private func ragAgainst(once num: UInt8) -> UInt8 {
    return num ^ 4
}

/*: "top" :*/
fileprivate let noti_providerMsg:[UInt8] = [0xf5,0xee,0xf1]

private func reportPortrait(nominee num: UInt8) -> UInt8 {
    return num ^ 129
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NameProjection.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class NameProjection: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [KeepArchitecture]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [KeepArchitecture]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func launchTo(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(noti_indicationId) + noti_receiverOnceStr.lowercased())
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: main_whyName.map{oiAutomat(could: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func mainOff(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(const_accountLeaveData.prefix(8)) + String(app_halfKey) + String(dataInserterStr.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func everyReady(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(mainReasonMotivePath))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func ask(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(showExactlyKey.prefix(9)) + String(kEagerOiStr) + String(show_availableBurnMessage.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func searchM(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(kGrinData) + notiTabFormat.replacingOccurrences(of: "factor", with: "in") + String(data_domainTitle))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func mend(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(show_personId) + String(mainDirectlyStr) + String(mainBadUrl))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func notEdit(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(show_somethingStr) + mainRefKey.replacingOccurrences(of: "who", with: "i") + String(showFlexibilityString.suffix(5)) + "endGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func towardHiAround(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(dataUniquePath.prefix(5)) + "atRoo" + String(appUsExceptionName.suffix(4)) + "ndGif" + noti_farData.replacingOccurrences(of: "spell", with: "t"))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func scriptBy(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(k_deadlinePath))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func whoAccept(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(mainMotiveUrl.suffix(6)) + "r/get" + String(user_sinkStructureMessage.suffix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func anHide(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(noti_shouldUrl.prefix(4)) + "etMes" + String(noti_platPath.prefix(5)) + "serIn" + String(dataRateSpringValue.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<KeepArchitecture>.deserialize(from: result as? Array) {
            if let datas = Array<KeepArchitecture>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [KeepArchitecture])
                completion(succeed, datas as! [KeepArchitecture])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func clear(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        constOriginId.constraintPanel(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func overResumeFilter(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(kMigrationMsg) + String(mainDimensionWedKey) + String(app_feeFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func talkValue(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(dataThirdPreviousString) + String(showGrassTitle.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func searcherLimited(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(k_renderMendBlueKey) + String(userIndicatorStr.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func modify(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(app_canvasKey) + String(userCeilingMarkKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func clipAcrossManual(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: show_titleMessage.map{perDrop(reward: $0)}, encoding: .utf8)! : (String(k_platValue.suffix(8)) + String(k_containName.suffix(4)) + "ttention")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                    kAdjustBecomeValue.allInsidePosition(showMsg: (String(user_additionalTitle) + data_resolutionEasterFormat.replacingOccurrences(of: "format", with: "uc")).localized)
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    kAdjustBecomeValue.allInsidePosition(showMsg: (String(noti_sunDrawMsg) + String(main_bagEveryTitle.suffix(4)) + "cess").localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                kAdjustBecomeValue.bury(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func translateCur(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (user_satisfyUrl.replacingOccurrences(of: "infrastructure", with: "a") + "slat" + String(notiExclusivePath.suffix(5)))
        //: reqModel.params = ["msgId": msgId, "targetLng": MacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: dataShallNativeTitle.map{$0^66}, encoding: .utf8)!: msgId, String(bytes: main_relationYetMessage.map{parentLeave(large: $0)}, encoding: .utf8)!: CharacteristicSine.enableCondemnMore(), String(bytes: main_whyName.map{oiAutomat(could: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func petOp(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(const_privacyStandardName.prefix(4)) + "reet/g" + String(show_monthSpaceArrayStr.prefix(6)) + "Gree" + String(show_networkOurMessage.suffix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func enableHard(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(userDeadlineData.prefix(5)) + "eet/ge" + String(app_countMediaProcedureTitle.suffix(4)) + "imateP" + dataHusbandMsg.replacingOccurrences(of: "presidency", with: "h"))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: k_awakeMessage.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func ceilingAlive(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(kGuideName) + String(user_bagKey))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func usMale(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(user_mixString) + String(noti_sampleFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func structure(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: noti_ratePath.reversed(), encoding: .utf8)!: roomId, String(bytes: showOnlyEnableId.map{$0^188}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(notiDrownPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func boardLine(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(app_agreementKey.suffix(6)) + "tRoom" + String(appTroubleFormat.suffix(8)) + String(constEvenMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func strategyAcrossCritical(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (showNowViewMessage.replacingOccurrences(of: "month", with: "mf") + String(userHappyQualityId.suffix(6)) + "oom/" + const_carrierTitle.replacingOccurrences(of: "dented", with: "rs"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func specifyCalculate(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(noti_raceMsg) + mainEconomicalDialogData.lowercased() + String(noti_eventTitle))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: appCommonFormat.reversed(), encoding: .utf8)!: toUids, String(bytes: main_whyName.map{oiAutomat(could: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func characteristicTo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(dataThatMsg.prefix(6)) + "etCall" + String(const_countPath))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func splitBy(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(show_impactProvideStr) + String(show_providerPresentationStr))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: noti_businessName.map{ragAgainst(once: $0)}, encoding: .utf8)!: content, String(bytes: noti_providerMsg.map{reportPortrait(nominee: $0)}, encoding: .utf8)!: isTop, String(bytes: k_awakeMessage.reversed(), encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
