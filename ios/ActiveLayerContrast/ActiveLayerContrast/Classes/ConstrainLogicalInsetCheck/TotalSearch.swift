
//: Declare String Begin

/*: "http://" :*/
fileprivate let notiCommunicateString:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let userHiddenUrl:[Character] = ["h","t","t","p","s"]
fileprivate let appRemainFormat:String = ":mergemerge"

/*: "http:// :*/
fileprivate let userSupporterConstructString:String = "http://properly behind quality"

/*: "serverSelctIndex =  :*/
fileprivate let notiMinimizeKey:[Character] = ["s","e","r","v","e","r","S","e","l","c","t"]
fileprivate let data_numberString:[Character] = ["I","n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let main_needPath:[Character] = ["s","e","r","v","e","r"]
fileprivate let data_partnerKey:String = "please rounding the instead transactionUrlS"

/*: "app. :*/
fileprivate let appCameraOccasionData:String = "want future detailed tweenapp."

/*: .com" :*/
fileprivate let main_aboutUrl:String = "orang rate.com"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let k_batteryKey:String = "currently speedmf/ch"
fileprivate let main_adminMarkString:String = "om/seaccount sentence control launch"
fileprivate let constTitleureLeadingStr:String = "FT"

/*: "recharge/applePayNotify" :*/
fileprivate let showPreviousFormat:String = "reccapable"
fileprivate let mainBeauTitle:String = "plePbeau ta heat button robot"
fileprivate let k_mediaPermissionPath:String = "tspready"

/*: "user/appleLogin" :*/
fileprivate let showPointData:String = "user/apspecial average"
fileprivate let kInmateTriggerMsg:String = "pleLogininto temp way day"

/*: "IM/getMyBlackList" :*/
fileprivate let notiDeployStr:String = "gray spend repeat sink interactionIM/get"
fileprivate let data_pureBedCorrectStr:String = "ackLisspec sustainable reason fore"
fileprivate let notiTranslateArrowToTitle:[Character] = ["t"]

/*: "mf/business/sendMsg" :*/
fileprivate let user_symbolMessage:String = "weekly thread tag consentmf/bus"
fileprivate let app_phaseChangeTitle:[Character] = ["i","n","e","s","s","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let appSunFactorMaleId:[Character] = ["m","f","/","b","u","s","i"]
fileprivate let notiRouteSnapTitle:String = "nearns"
fileprivate let show_portKitMsg:String = "dGiftgreen as multiple transaction nature"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let main_messagePerceptKey:String = "already everymf/cha"
fileprivate let user_bringName:[Character] = ["/","s","e","n","d","M","s","g"]

/*: "GJ21FB" :*/
fileprivate let notiFlexibilityMsg:String = "gj21fb"

/*: "完成了检测,请求状态正常" :*/
fileprivate let showWeekPoseUrl:[Character] = ["完","成","\u{4e86}","检","测",",","请"]
fileprivate let mainNumbCapableId:[Character] = ["求","状","\u{6001}","正","常"]

/*: "接口请求失败" :*/
fileprivate let show_evaluateStartTitle:String = "接口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let user_orangOldValue:[Character] = ["完","成","了","检","测",",","需","要","更","换","s","e","r","v","e","r"," "]
fileprivate let dataAverageKey:String = "commitl"

/*: "HTTPProxy" :*/
fileprivate let data_locateName:String = "httpp"
fileprivate let user_memoBlueInserterMsg:String = "axy"

/*: "__SCOPED__" :*/
fileprivate let data_ofFormat:String = "space blue__SCOPE"
fileprivate let dataFactoryFormat:[Character] = ["D","_","_"]

/*: "tap" :*/
fileprivate let main_livingStr:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let noti_ageValue:String = "tbasic"

/*: "ipsec" :*/
fileprivate let app_borrowStr:String = "initesec"

/*: "ppp" :*/
fileprivate let constErrorStr:String = "itemp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let data_gemName:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="]
fileprivate let data_vidPublishId:String = "consumption"

/*: "https:// :*/
fileprivate let k_consumeMaintainFormat:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let user_transactionDataMsg:[UInt8] = [0x2e,0x3d,0x36,0x2b,0x27,0x27,0x2c,0x2d,0x34,0x2d,0x3c,0x2d,0x1b,0x29,0x35,0x2d,0xb,0x37,0x37,0x33,0x31,0x2d,0x3b,0x1f,0x31,0x3c,0x30,0x1d,0x3a,0x34,0xe8,0x5,0xe8]

fileprivate func vidWant(contain num: UInt8) -> UInt8 {
    let value = Int(num) - 200
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let data_miniCorrectKey:String = "mergeD"

/*: "PHPSESSID" :*/
fileprivate let userIconUrl:String = "PrecentSE"
fileprivate let const_libraryMessageKey:String = "currencycurrencyID"

/*: "func__saveAllCookies =  :*/
fileprivate let user_mentionPicFormat:String = "futhere"
fileprivate let k_liveWorkData:String = "eAllCoprivacy explore strip re she"

/*: "func__saveAllCookies  error" :*/
fileprivate let show_boldBringStr:String = "needunc"
fileprivate let mainPrivacyDivePath:String = "veAllCoplate tool"
fileprivate let appLimitedDebateKey:String = "oktiffes"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TotalSearch.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ChainAgile: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class TotalSearch: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = TotalSearch()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        missConfident()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(notiCommunicateString))) || newValue.hasPrefix((String(userHiddenUrl) + appRemainFormat.replacingOccurrences(of: "merge", with: "/"))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if noti_visitMenuName && !appCombineProcessorValue {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(userSupporterConstructString.prefix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(userSupporterConstructString.prefix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    elbowWithProcessing(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func missConfident() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = show_modelHideFormat.integer(forKey: showUndertakeString)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(notiMinimizeKey) + String(data_numberString)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = associateOff()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiCommunicateString))
        //: if Environment_Formal && !Encryption_Request {
        if noti_visitMenuName && !appCombineProcessorValue {
            //: serverPrefix = "http://"
            serverPrefix = (String(notiCommunicateString))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(main_needPath) + String(data_partnerKey.suffix(4)) + "tr = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func associateOff() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func speedNominee(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(appCameraOccasionData.suffix(4))) + "\(data_graphicUrl)" + (String(main_aboutUrl.suffix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(k_batteryKey.suffix(5)) + "atRo" + String(main_adminMarkString.prefix(5)) + "ndGi" + constTitleureLeadingStr.lowercased()),
         //: "recharge/applePayNotify",
         (showPreviousFormat.replacingOccurrences(of: "capable", with: "h") + "arge/ap" + String(mainBeauTitle.prefix(4)) + "ayNo" + k_mediaPermissionPath.replacingOccurrences(of: "spread", with: "if")),
         //: "user/appleLogin"] + slowPathArr1
         (String(showPointData.prefix(7)) + String(kInmateTriggerMsg.prefix(8)))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(notiDeployStr.suffix(6)) + "MyBl" + String(data_pureBedCorrectStr.prefix(6)) + String(notiTranslateArrowToTitle)),
         //: "mf/business/sendMsg",
         (String(user_symbolMessage.suffix(6)) + String(app_phaseChangeTitle)),
         //: "mf/business/sendGift",
         (String(appSunFactorMaleId) + notiRouteSnapTitle.replacingOccurrences(of: "earn", with: "es") + "/sen" + String(show_portKitMsg.prefix(5))),
         //: "mf/chatRoom/sendMsg"]
         (String(main_messagePerceptKey.suffix(6)) + "tRoom" + String(user_bringName))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MacroDefine.getSystemLangCode()
        let lang = CharacteristicSine.enableCondemnMore()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .screenFor(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (notiFlexibilityMsg.uppercased()) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension TotalSearch {
    //: func checkServerUrl() {
    func flushOn() {
        //: if ChartShowSceneScientific.share.networkStatus != .Unavailable {
        if ChartShowSceneScientific.share.networkStatus != .Unavailable {
            //: func_requestHost()
            husband()
            //: } else {
        } else {
            //: func_starCodeTime()
            sternPrimarilyListener()
        }
    }

    //: func func_starCodeTime() {
    func sternPrimarilyListener() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] num in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.husband()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func asNotice() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func husband() {
        //: if ChartShowSceneScientific.share.networkStatus == .Unavailable {
        if ChartShowSceneScientific.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        asNotice()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        PopMessage.sum { [weak self] succeed, result, errorModel in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(showWeekPoseUrl) + String(mainNumbCapableId)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (show_evaluateStartTitle.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.sternPrimarilyListener()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(user_orangOldValue) + dataAverageKey.replacingOccurrences(of: "commit", with: "ur")))
                    //: self.func__changeHostUrl()
                    self.duringChannel()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func duringChannel() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        asNotice()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        show_modelHideFormat.set(serverSelctIndex, forKey: showUndertakeString)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiCommunicateString))
        //: if Environment_Formal && !Encryption_Request {
        if noti_visitMenuName, !appCombineProcessorValue {
            //: serverPrefix = "http://"
            serverPrefix = (String(notiCommunicateString))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension TotalSearch {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func grab() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(data_locateName.uppercased() + user_memoBlueInserterMsg.replacingOccurrences(of: "a", with: "ro"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func mouseWithinEditor() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(data_ofFormat.suffix(7)) + String(dataFactoryFormat))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(main_livingStr))) || keyStr.contains((noti_ageValue.replacingOccurrences(of: "basic", with: "un"))) || keyStr.contains((app_borrowStr.replacingOccurrences(of: "nite", with: "p"))) || keyStr.contains((constErrorStr.replacingOccurrences(of: "item", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension TotalSearch {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func elbowWithProcessing(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(data_gemName) + data_vidPublishId.replacingOccurrences(of: "consumption", with: " ")) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = infrastructureNet()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
        var urlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(notiCommunicateString)), with: (String(userHiddenUrl) + appRemainFormat.replacingOccurrences(of: "merge", with: "/")))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(userHiddenUrl) + appRemainFormat.replacingOccurrences(of: "merge", with: "/"))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(k_consumeMaintainFormat)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            compareReading(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        quitMain()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func compareReading(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: user_transactionDataMsg.map{vidWant(contain: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func infrastructureNet() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiCommunicateString))
        //: if Environment_Formal && !Encryption_Request {
        if noti_visitMenuName, !appCombineProcessorValue {
            //: serverPrefix = "https://"
            serverPrefix = (String(userHiddenUrl) + appRemainFormat.replacingOccurrences(of: "merge", with: "/"))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(appCameraOccasionData.suffix(4))) + "\(data_graphicUrl)" + (String(main_aboutUrl.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func pointMethod(note: Notification) {
        //: if ChartShowSceneScientific.share.loginUid.isEmptyString {
        if ChartShowSceneScientific.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            disclose()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            implyDaily()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func disclose() {
        //: var htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
        var htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))), h5Cookie.keys.contains((data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))), h5Cookie.keys.contains((userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func implyDaily() {
        //: let htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
        let htmlDomain: String = ChartShowSceneScientific.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == ChartShowSceneScientific.share.loginUserMode.userID {
            if cookie.name == (data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI")), cookie.value == ChartShowSceneScientific.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(data_miniCorrectKey.replacingOccurrences(of: "merge", with: "UI"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(userIconUrl.replacingOccurrences(of: "recent", with: "HP") + const_libraryMessageKey.replacingOccurrences(of: "currency", with: "S"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func quitMain() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (user_mentionPicFormat.replacingOccurrences(of: "there", with: "nc") + "__sav" + String(k_liveWorkData.prefix(6)) + "okies = ") + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            show_modelHideFormat.set(cookieData, forKey: const_customString)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (show_boldBringStr.replacingOccurrences(of: "need", with: "f") + "__sa" + String(mainPrivacyDivePath.prefix(7)) + appLimitedDebateKey.replacingOccurrences(of: "tiff", with: "i") + "  error"))
        }
    }

    //: func func__setAppCookies() {
    func roundSignal() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = show_modelHideFormat.object(forKey: const_customString)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func lastDrawer() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        show_modelHideFormat.removeObject(forKey: const_customString)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
