
//: Declare String Begin

/*: ; build: :*/
fileprivate let k_orangeData:[Character] = [";"," ","b","u","i","l","d",":"]

/*: ; iOS  :*/
fileprivate let main_coordinatorTitle:String = "tween music; iOS "

/*: "audio" :*/
fileprivate let dataGradeDoingMessage:[Character] = ["a","u","d","i","o"]

/*: .wav" :*/
fileprivate let notiPropertyData:String = ".wavmy object for crop"

/*: "audio/wav" :*/
fileprivate let main_parentChainString:String = "gate propertyaudio/wav"

/*: .jpg" :*/
fileprivate let constDropPath:String = "then outer core.jpg"

/*: "image/jpeg" :*/
fileprivate let user_applicationUrl:String = "imbass"
fileprivate let show_manageOutputName:String = "bot thumb infoe/jpeg"

/*: "://app." :*/
fileprivate let mainDrawerStr:String = "://apvar valid general"
fileprivate let data_boxPath:String = "p.gold object"

/*: "data" :*/
fileprivate let constDrinkId:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let kViewId:String = ":nullpage pi"

/*: "Request failed, Try again later" :*/
fileprivate let main_equallyTitle:[UInt8] = [0xdc,0xef,0xfb,0xff,0xef,0xfd,0xfe,0xaa,0xf0,0xeb,0xf3,0xf6,0xef,0xee,0xb6,0xaa,0xde,0xfc,0x3,0xaa,0xeb,0xf1,0xeb,0xf3,0xf8,0xaa,0xf6,0xeb,0xfe,0xef,0xfc]

fileprivate func missMissing(business num: UInt8) -> UInt8 {
    let value = Int(num) - 138
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "json error" :*/
fileprivate let mainChartBuryUrl:String = "json part load pause meeting chapter"
fileprivate let const_pullUrl:String = "ERROR"

/*: "platform=iphone&version= :*/
fileprivate let show_alongsideHoldUrl:String = "PLATF"
fileprivate let showSuccessfulData:String = "spar"
fileprivate let appPanelMessage:String = "decide nim contrast onee&ve"

/*: &packageId= :*/
fileprivate let main_relationPath:[Character] = ["&","p","a","c","k","a"]
fileprivate let dataLastInstanceStr:String = "plus be motivegeId="

/*: &bundleId= :*/
fileprivate let showAppearValue:String = "&bundleIdremaining owner turn characteristic spine"
fileprivate let noti_providerTitle:String = "keep"

/*: &lang= :*/
fileprivate let mainConnectKey:[Character] = ["&","l","a","n","g","="]

/*: "/route/slowindex" :*/
fileprivate let showFactorData:[Character] = ["/","r","o","u","t","e","/","s","l","o","w","i","n","d"]
fileprivate let app_leastMechanismMsg:String = "econ"

/*: "/route/index" :*/
fileprivate let main_movementStr:String = "base bling large nous/route"
fileprivate let user_plusMsg:String = "eshot"

/*: "?path= :*/
fileprivate let appSenThinStr:String = "button holder reader?path="

/*: "&GJNonceStr= :*/
fileprivate let k_libStatTitle:String = "busy below vid&GJNo"
fileprivate let constBlackFormat:String = "="

/*: "&params= :*/
fileprivate let k_isolateStr:[Character] = ["&","p","a","r","a","m","s","="]

/*: "token" :*/
fileprivate let noti_resistanceMsg:String = "terrken"

/*: "请求成功 :*/
fileprivate let constSpecifyFormat:String = "请求成\u{529f}"

/*: "AAAAAAAA" :*/
fileprivate let mainCharacterKey:String = "implementimplement"
fileprivate let appRemTensionStr:String = "nownownownownownow"

/*: "下载完成 -  :*/
fileprivate let const_heapStr:String = "value载完成 -"
fileprivate let app_automaticallyPath:String = "player"

/*: "/Documents/" :*/
fileprivate let dataInformationMessage:String = "/Docuplat own currently"

/*: ".mp4" :*/
fileprivate let notiTickData:String = "nature cancel.mp4"

/*: "移动地址: :*/
fileprivate let appHeadTitle:String = "移动地址:"

/*: "后台任务下载回来" :*/
fileprivate let kBlocGeneticMessage:String = "后台\u{4efb}务下载"
fileprivate let app_answerAllId:[Character] = ["回","\u{6765}"]

/*: "下载成功 : :*/
fileprivate let mainOiString:String = "\u{4e0b}"
fileprivate let user_manualSubstantialMsg:[Character] = ["\u{8f7d}","成","\u{529f}"," ",":"]

/*: "Net Error" :*/
fileprivate let userConvertUrl:String = "Net Errorintro info disabled"

/*: "下载失败 : :*/
fileprivate let app_unableValue:String = "下\u{8f7d}失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LiteException.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let user_grainHandleMsg = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let const_sData = "\(show_clickId)/\(appOperationPath) (\(show_primaryUrl)" + (String(k_orangeData)) + "\(dataEntityId)" + (String(main_coordinatorTitle.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: SearcherNatural?) -> Void

//: let GJ = TalkingRequestTool.init()
let constOriginId = LiteException()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class LiteException: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func product(model: StrategyArray, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.nextAndAspect() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.sufficientWith(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.temporaryBlack(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func sufficientWith(model: StrategyArray, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.nous(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.rehab(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    doLess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: show_plainHalfName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bury(showMsg: show_plainHalfName)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(dataGradeDoingMessage)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.betweenBling())+\(noti_messageFormat)" + (String(notiPropertyData.prefix(4))), mimeType: (String(main_parentChainString.suffix(9))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(constDropPath.suffix(4))), mimeType: (user_applicationUrl.replacingOccurrences(of: "bass", with: "ag") + String(show_manageOutputName.suffix(6))))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    doLess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: show_plainHalfName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bury(showMsg: show_plainHalfName)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func temporaryBlack(model: StrategyArray, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.nous(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.rehab(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: appCombineProcessorValue ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = user_grainHandleMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    doLess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: show_plainHalfName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bury(showMsg: show_plainHalfName)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = user_grainHandleMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    doLess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: show_plainHalfName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bury(showMsg: show_plainHalfName)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func doLess(model: StrategyArray, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            TotalSearch.share.elbowWithProcessing(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if appCombineProcessorValue, model.requestServer.contains((String(mainDrawerStr.prefix(5)) + String(data_boxPath.prefix(2)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.circleUniform(key: TotalSearch.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(constDrinkId)) + "\"" + (String(kViewId.prefix(5))), with: "" + "\"" + (String(constDrinkId)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !appCombineProcessorValue {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(kViewId.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<TimingResult>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == TitleureRunUser.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, SearcherNatural(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    bury(showMsg: responseModel.msg ?? String(bytes: main_equallyTitle.map{missMissing(business: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case TitleureRunUser.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: const_dimensionFrameMsg, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: (String(mainChartBuryUrl.prefix(5)) + const_pullUrl.lowercased()).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func nous(model: StrategyArray) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (show_alongsideHoldUrl.lowercased() + "orm=ipho" + showSuccessfulData.replacingOccurrences(of: "spar", with: "n") + String(appPanelMessage.suffix(4)) + "rsion=") + "\(dataRenderTitle)" + (String(main_relationPath) + String(dataLastInstanceStr.suffix(5))) + "\(noti_messageFormat)" + (String(showAppearValue.prefix(9)) + noti_providerTitle.replacingOccurrences(of: "keep", with: "=")) + "\(show_primaryUrl)" + (String(mainConnectKey)) + "\(TotalSearch.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if appCombineProcessorValue, serverUrl.contains((String(mainDrawerStr.prefix(5)) + String(data_boxPath.prefix(2)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if TotalSearch.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(showFactorData) + app_leastMechanismMsg.replacingOccurrences(of: "con", with: "x")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(main_movementStr.suffix(6)) + "/ind" + user_plusMsg.replacingOccurrences(of: "shot", with: "x"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(appSenThinStr.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(k_libStatTitle.suffix(5)) + "nceStr" + constBlackFormat.capitalized) + "\(TotalSearch.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.enableConnectionEnormous(key: TotalSearch.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(k_isolateStr)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func rehab(model: StrategyArray) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(const_sData)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (noti_resistanceMsg.replacingOccurrences(of: "err", with: "o")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func customInsideFreeze(model: StrategyArray) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.nous(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { data, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (constSpecifyFormat) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (mainCharacterKey.replacingOccurrences(of: "implement", with: "A") + appRemTensionStr.replacingOccurrences(of: "now", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (const_heapStr.replacingOccurrences(of: "value", with: "下") + app_automaticallyPath.replacingOccurrences(of: "player", with: " ")) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(dataInformationMessage.prefix(5)) + "ments/") + NSDate.betweenBling() + (String(notiTickData.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (appHeadTitle.capitalized) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (kBlocGeneticMessage + String(app_answerAllId)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = TemporaryGrain.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func constraintPanel(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: notiSaltFinishId.customForWrite(), with: data_graphicUrl)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (mainOiString + String(user_manualSubstantialMsg)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, SearcherNatural(errorCode: TitleureRunUser.Normal.rawValue, errorMsg: (String(userConvertUrl.prefix(9))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: (String(userConvertUrl.prefix(9))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (app_unableValue) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func confident(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: notiSaltFinishId.customForWrite(), with: data_graphicUrl)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (mainOiString + String(user_manualSubstantialMsg)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, SearcherNatural(errorCode: TitleureRunUser.Normal.rawValue, errorMsg: (String(userConvertUrl.prefix(9))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, SearcherNatural(errorCode: TitleureRunUser.NetError.rawValue, errorMsg: (String(userConvertUrl.prefix(9))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (app_unableValue) + "\(errMsg)")
            }
        }
    }
}
