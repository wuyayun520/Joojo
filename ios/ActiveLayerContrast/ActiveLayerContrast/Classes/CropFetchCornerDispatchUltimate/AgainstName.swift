
//: Declare String Begin

/*: "msgType" :*/
fileprivate let main_personalValue:[Character] = ["m","s","g","T","y","p","e"]

/*: "tips" :*/
fileprivate let notiHangingProperlyId:String = "TIPS"

/*: "extra" :*/
fileprivate let constScriptCreateMsg:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let show_closedDarkRetPath:String = "team always sure warnmsgIn"
fileprivate let noti_writtenFormat:[Character] = ["f","o"]

/*: "user" :*/
fileprivate let mainHeadUrl:String = "umeetr"

/*: "uid" :*/
fileprivate let dataMereExtendTitle:String = "UID"

/*: "nickname" :*/
fileprivate let noti_beUrl:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "headPic" :*/
fileprivate let k_undertakeStr:[UInt8] = [0x53,0x5e,0x5a,0x5f,0x6b,0x52,0x58]

private func statueDimension(crop num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "age" :*/
fileprivate let constBelowString:[UInt8] = [0x9,0xf,0xd]

private func tagNumb(change num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "gift" :*/
fileprivate let notiCompareString:String = "GIFT"

/*: "isGroup" :*/
fileprivate let userMaleMsg:String = "isGroupautomatic man catch go"

/*: "WantGift" :*/
fileprivate let kHarassmentTitle:String = "bullet add columnWantGif"
fileprivate let k_fileData:String = "stern"

/*: "txt" :*/
fileprivate let notiMomentId:String = "ilkt"

/*: "img" :*/
fileprivate let app_noticeStr:String = "filterg"

/*: "video" :*/
fileprivate let data_twoKey:String = "VIDEO"

/*: "audio" :*/
fileprivate let mainDirectUrl:String = "audiinvisible"

/*: "messageType" :*/
fileprivate let kScanLocationId:[Character] = ["m","e","s","s","a","g","e"]
fileprivate let data_perceptNomineeMsg:String = "Typehead explicit"

/*: "contentType" :*/
fileprivate let app_databaseMessage:String = "copresenttepresent"
fileprivate let noti_revealMessage:[Character] = ["e"]

/*: "ImgMsg" :*/
fileprivate let data_processorLineString:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let kMouseUrl:[Character] = ["i","m"]
fileprivate let mainEveryoneId:String = "ageUrimere popular model"

/*: "type" :*/
fileprivate let user_linkUrl:String = "typspread"

/*: "pic" :*/
fileprivate let main_turnStr:String = "picommunicate"

/*: "isBurn" :*/
fileprivate let mainRateValue:String = "typical homeisBurn"

/*: "giftId" :*/
fileprivate let kBossItemStr:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let kDeviceRatingFormat:String = "pentryrentryms"

/*: "audioLength" :*/
fileprivate let userPoseId:String = "audioLya discount coordinate emerge"

/*: "len" :*/
fileprivate let notiSessionKey:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let appHandleGenderUrl:[Character] = ["a","u","d","i","o","S"]
fileprivate let showAfterStr:String = "andbseventh"

/*: "senderId" :*/
fileprivate let showLiveContactKey:String = "stern element supporter burnsenderId"

/*: "AudioMsg" :*/
fileprivate let showSingleFoundId:[Character] = ["A","u","d","i","o","M"]
fileprivate let notiCountegrationUrl:String = "senjoy"

/*: "content" :*/
fileprivate let showProviderKeyStr:[Character] = ["c","o","n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let userInspectRetPrecisUrl:[Character] = ["T","x","t","M","s","g"]

/*: "message" :*/
fileprivate let appClubStr:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let userRelatedId:String = "quota"
fileprivate let main_grainValue:[Character] = ["I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let dataLocalString:String = "portedot"
fileprivate let mainResolveFormat:String = "eMsgInfoalone status chapter delivery"

/*: "replayId" :*/
fileprivate let constDialogFormat:String = "privacy localreplayI"
fileprivate let mainWayShId:String = "per"

/*: "rYMsgType" :*/
fileprivate let noti_noCleanId:String = "rYMsgreflect suspend rounding ceiling restriction"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let app_beMsg:[Character] = ["G","J",":","S","e","s","s",":","A","u"]
fileprivate let mainMarkId:[Character] = ["t","o","G","r","e","e","t",":","T","x","t","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let constCycleRoadVillageUrl:String = "CHANGE"
fileprivate let userDeliveryTeeMessage:[Character] = ["D","i","r","e","c","t","i","o","n"]

/*: "system" :*/
fileprivate let noti_sinkMessage:String = "sscreentem"

/*: "title" :*/
fileprivate let mainHolderString:[Character] = ["t","i","t","l","e"]

/*: "icon" :*/
fileprivate let userVisibleKey:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let dataShString:String = "akitkit"

/*: "icon_chats_pcr" :*/
fileprivate let notiRagId:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p"]
fileprivate let k_tooUnknownFormat:String = "csegment"

/*: "status" :*/
fileprivate let user_upperPath:[Character] = ["s","t","a","t","u","s"]

/*: "msgIncome" :*/
fileprivate let constRestrictionKey:String = "deputy approve targetmsgInco"
fileprivate let dataGraphTitle:String = "mcomply"

/*: "msgId" :*/
fileprivate let kRedPhasePath:String = "msgIdcurrency normal now condemn"

/*: "ContextLevel" :*/
fileprivate let userReduceAutomatTallyUrl:String = "err interestTMsgTr"
fileprivate let show_evenCurrentlyFormat:String = "integrationnslintegration"

/*: "TMsgTranslatedContent" :*/
fileprivate let data_missionMsg:String = "TMsgTrevolution assessment team expose"
fileprivate let user_resumeValue:String = "man fire cur immediate weightedCont"
fileprivate let main_radioUrl:String = "enequally"

/*: "formatType" :*/
fileprivate let constHeadPath:String = "foruniform"

/*: "plain" :*/
fileprivate let show_constructValue:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let showCounteractionGoFormat:String = "sophisticatedag"
fileprivate let userPopularExpressionUrl:String = "eDatabeat case round drink"

/*: "VideoMsg" :*/
fileprivate let userGiveMsg:String = "Videoflexibility void"
fileprivate let notiSpellData:String = "Msgdue total"

/*: "match" :*/
fileprivate let noti_laterBarId:[Character] = ["m","a","t","c","h"]

/*: "userIntro" :*/
fileprivate let constFeatureFlowTotalerestUrl:[Character] = ["u","s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let user_filterFormat:[Character] = ["t","a","r","g","e"]
fileprivate let userStackPath:String = "app library functional quittId"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let mainDoingCaptureMessage:String = "rollroll"
fileprivate let dataQueryerString:String = "person road for contractordd HH:m"
fileprivate let dataWatchValue:String = "sentence sea implementm:ss"

/*: "sendTime" :*/
fileprivate let mainRefuseData:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let notiEnjoyString:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let noti_publishStr:String = "auposition"

/*: "audioUri" :*/
fileprivate let main_bedMsg:String = "fit"
fileprivate let k_emptyMsg:[Character] = ["u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AgainstName.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum WouldWhite: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - AgainstName

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class AgainstName: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func missionReplace() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EmbraceShadow? {
    public class func easy(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EmbraceShadow? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = policyPan(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: InputDrawing.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            InputDrawing.shared.observe(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EmbraceShadow? {
    public class func policyPan(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EmbraceShadow? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(main_personalValue))] = (notiHangingProperlyId.lowercased())
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(constScriptCreateMsg)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = equallyInclude(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RegulateThan(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ItemModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? UndertakeModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(mainHeadUrl.replacingOccurrences(of: "meet", with: "se"))] = [
            //: "uid": ChartShowSceneScientific.share.loginUserMode.userID,
            (dataMereExtendTitle.lowercased()): ChartShowSceneScientific.share.loginUserMode.userID,
            //: "nickname": ChartShowSceneScientific.share.loginUserMode.nickname,
            String(bytes: noti_beUrl.reversed(), encoding: .utf8)!: ChartShowSceneScientific.share.loginUserMode.nickname,
            //: "headPic": ChartShowSceneScientific.share.loginUserMode.headPic,
            String(bytes: k_undertakeStr.map{statueDimension(crop: $0)}, encoding: .utf8)!: ChartShowSceneScientific.share.loginUserMode.headPic,
            //: "age": ChartShowSceneScientific.share.loginUserMode.age,
            String(bytes: constBelowString.map{tagNumb(change: $0)}, encoding: .utf8)!: ChartShowSceneScientific.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(constScriptCreateMsg)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = equallyInclude(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(main_personalValue))] as? String == (notiCompareString.lowercased()) {
            //: var data: EmbraceShadow
            var data: EmbraceShadow
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(userMaleMsg.prefix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = BesideParticle(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = FeatureOccasion(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = FeatureOccasion(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(main_personalValue))] as? String == (String(kHarassmentTitle.suffix(7)) + k_fileData.replacingOccurrences(of: "stern", with: "t")) {
            //: let data = VisitPrepare(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = VisitPrepare(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(main_personalValue))] as? String == (notiMomentId.replacingOccurrences(of: "ilk", with: "tx")) {
            //: var data: EmbraceShadow
            var data: EmbraceShadow
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(userMaleMsg.prefix(7)))] as? String == "1" { // 群聊
                //: data = CoordinatorBasicOff(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = CoordinatorBasicOff(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = VariableChain(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = VariableChain(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(main_personalValue))] as? String == (app_noticeStr.replacingOccurrences(of: "filter", with: "im")) {
            //: let data = WantText(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = WantText(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(main_personalValue))] as? String == (data_twoKey.lowercased()) {
            //: let data = RoundingAsset(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RoundingAsset(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(main_personalValue))] as? String == (mainDirectUrl.replacingOccurrences(of: "invisible", with: "o")) {
            //: let data = SustainableCenter(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SustainableCenter(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(notiCompareString.lowercased())] != nil && (msgInfo[(String(kScanLocationId) + String(data_perceptNomineeMsg.prefix(4)))] as? Int) == 4 {
            //: let data = VariableChain(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = VariableChain(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ItemModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: EmbraceShadow) -> EmbraceShadow? {
    public class func distance(withData cellData: EmbraceShadow) -> EmbraceShadow? {
        // 图片
        //: if let reCellData = cellData as? WantText {
        if let reCellData = cellData as? WantText {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(MacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(CharacteristicSine.contextUsedFlexBring())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(app_databaseMessage.replacingOccurrences(of: "present", with: "n") + "tTyp" + String(noti_revealMessage))] = (String(data_processorLineString))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(main_personalValue))] = (app_noticeStr.replacingOccurrences(of: "filter", with: "im"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(kMouseUrl) + String(mainEveryoneId.prefix(6)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(user_linkUrl.replacingOccurrences(of: "spread", with: "e"))] = (String(data_processorLineString))
            //: params["pic"] = filePath
            params[(main_turnStr.replacingOccurrences(of: "communicate", with: "c"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(mainRateValue.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(mainRateValue.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(kBossItemStr))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(kBossItemStr))] = "0"
            }

            //: dic["params"] = params
            dic[(kDeviceRatingFormat.replacingOccurrences(of: "entry", with: "a"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = policyPan(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? RoundingAsset {
        if let reCellData = cellData as? RoundingAsset {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? SustainableCenter {
        if let reCellData = cellData as? SustainableCenter {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = TranslateScreen.alwaysA(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(userPoseId.prefix(6)) + "ength")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(notiSessionKey))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(appHandleGenderUrl) + showAfterStr.replacingOccurrences(of: "seventh", with: "ox"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(appHandleGenderUrl) + showAfterStr.replacingOccurrences(of: "seventh", with: "ox"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(main_personalValue))] = (mainDirectUrl.replacingOccurrences(of: "invisible", with: "o"))
            //: msgInfo["senderId"] = ChartShowSceneScientific.share.loginUserMode.userID
            msgInfo[(String(showLiveContactKey.suffix(8)))] = ChartShowSceneScientific.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(user_linkUrl.replacingOccurrences(of: "spread", with: "e"))] = (String(showSingleFoundId) + notiCountegrationUrl.replacingOccurrences(of: "enjoy", with: "g"))
            //: dic["params"] = params
            dic[(kDeviceRatingFormat.replacingOccurrences(of: "entry", with: "a"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = policyPan(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? VariableChain {
        if let reCellData = cellData as? VariableChain {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(showProviderKeyStr))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(main_personalValue))] = (notiMomentId.replacingOccurrences(of: "ilk", with: "tx"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(user_linkUrl.replacingOccurrences(of: "spread", with: "e"))] = (String(userInspectRetPrecisUrl))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(appClubStr))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.yield() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(userRelatedId.replacingOccurrences(of: "ta", with: "t") + "eMsg" + String(main_grainValue))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(dataLocalString.replacingOccurrences(of: "ported", with: "qu") + String(mainResolveFormat.prefix(8)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(String(constDialogFormat.suffix(7)) + mainWayShId.replacingOccurrences(of: "per", with: "d"))] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(String(constDialogFormat.suffix(7)) + mainWayShId.replacingOccurrences(of: "per", with: "d"))] = replayId
            }

            //: dic["params"] = params
            dic[(kDeviceRatingFormat.replacingOccurrences(of: "entry", with: "a"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = policyPan(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EmbraceShadow? {
    public class func need(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EmbraceShadow? {
        //: var cellData: EmbraceShadow?
        var cellData: EmbraceShadow?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(constScriptCreateMsg))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(mainHeadUrl.replacingOccurrences(of: "meet", with: "se"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ItemModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == ChartShowSceneScientific.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == ChartShowSceneScientific.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == ChartShowSceneScientific.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == ChartShowSceneScientific.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(noti_noCleanId.prefix(5)) + "Type")] as? String == (String(app_beMsg) + String(mainMarkId)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(constCycleRoadVillageUrl.lowercased() + String(userDeliveryTeeMessage))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = measureTension(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(main_personalValue))] as? String == (noti_sinkMessage.replacingOccurrences(of: "screen", with: "ys")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ItemModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? UndertakeModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RegulateThan(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(showProviderKeyStr))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = WantText(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = WantText(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = RoundingAsset(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RoundingAsset(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = BesideParticle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = FeatureOccasion(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FeatureOccasion(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = valueEnable(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(constScriptCreateMsg))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? PhoneModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = VisitPrepare(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = VisitPrepare(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = SustainableCenter(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SustainableCenter(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? SustainableCenter {
            if let audioCellData = cellData as? SustainableCenter {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = recommend(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = MoveBar.speakerDiscount(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = MoveBar.enableFrom(terms(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BasicCollection(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(mainHolderString))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(showProviderKeyStr))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(app_noticeStr.replacingOccurrences(of: "filter", with: "im"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CapacityDelivery(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = CoordinatorBasicOff(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CoordinatorBasicOff(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = VariableChain(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = VariableChain(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? ActiveModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(userVisibleKey))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: ChartShowSceneScientific.share.loginUserMode.headPic ?? ""
                        ChartShowSceneScientific.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = PhoneModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(showProviderKeyStr))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(constScriptCreateMsg))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = RoleOptionModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (dataShString.replacingOccurrences(of: "kit", with: "l"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(notiRagId) + k_tooUnknownFormat.replacingOccurrences(of: "segment", with: "r"))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.requireOpen(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(String(user_upperPath))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(constRestrictionKey.suffix(7)) + dataGraphTitle.replacingOccurrences(of: "comply", with: "e"))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(kScanLocationId) + String(data_perceptNomineeMsg.prefix(4)))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(kRedPhasePath.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(userReduceAutomatTallyUrl.suffix(6)) + show_evenCurrentlyFormat.replacingOccurrences(of: "integration", with: "a") + "teStatus")] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = ContextLevel(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(data_missionMsg.prefix(6)) + "anslat" + String(user_resumeValue.suffix(6)) + main_radioUrl.replacingOccurrences(of: "equally", with: "t"))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = CurrencyModel.kit()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = CurrencyModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = KitEaseModel.response()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = KitEaseModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = LockModel.entityTo()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func measureTension(_ extraDic: [String: Any]) -> WouldWhite {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(constScriptCreateMsg))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ItemModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(constHeadPath.replacingOccurrences(of: "uniform", with: "m") + "atType")] as? String == (String(show_constructValue)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(kMouseUrl) + String(mainEveryoneId.prefix(6)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(showCounteractionGoFormat.replacingOccurrences(of: "sophisticated", with: "im") + String(userPopularExpressionUrl.prefix(5)))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(userGiveMsg.prefix(5)) + String(notiSpellData.prefix(3))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = smallDestroy(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(noti_laterBarId))] as? Int) == 1 || (msgInfo?[(String(noti_laterBarId))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(main_personalValue))] as? String == (notiHangingProperlyId.lowercased()) || (dic?[(String(kScanLocationId) + String(data_perceptNomineeMsg.prefix(4)))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(constHeadPath.replacingOccurrences(of: "uniform", with: "m") + "atType")] as? String == "mf") && ((dic?[(String(kScanLocationId) + String(data_perceptNomineeMsg.prefix(4)))] as? Int) == 15 || (dic?[(String(kScanLocationId) + String(data_perceptNomineeMsg.prefix(4)))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(main_personalValue))] as? String == (String(constFeatureFlowTotalerestUrl)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(main_personalValue))] as? String == (String(kHarassmentTitle.suffix(7)) + k_fileData.replacingOccurrences(of: "stern", with: "t")) || (msgInfo?[(String(kScanLocationId) + String(data_perceptNomineeMsg.prefix(4)))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func terms(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.requireOpen(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(constScriptCreateMsg))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(show_closedDarkRetPath.suffix(5)) + String(noti_writtenFormat))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(mainHeadUrl.replacingOccurrences(of: "meet", with: "se"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(showLiveContactKey.suffix(8)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == ChartShowSceneScientific.share.loginUserMode.userID {
        if senderId == ChartShowSceneScientific.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(showLiveContactKey.suffix(8)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(user_filterFormat) + String(userStackPath.suffix(3)))] = message.userID ?? (user?[(String(user_filterFormat) + String(userStackPath.suffix(3)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(showLiveContactKey.suffix(8)))] = "\(msgInfo?[(dataMereExtendTitle.lowercased())] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(user_filterFormat) + String(userStackPath.suffix(3)))] = "\(msgInfo?[(dataMereExtendTitle.lowercased())] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(kRedPhasePath.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(kRedPhasePath.prefix(5)))] = "\(msgInfo?[(String(kRedPhasePath.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.requireOpen(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(kRedPhasePath.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(kRedPhasePath.prefix(5)))] = "\(customDic[(String(kRedPhasePath.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(kRedPhasePath.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(appHandleGenderUrl) + showAfterStr.replacingOccurrences(of: "seventh", with: "ox"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(appHandleGenderUrl) + showAfterStr.replacingOccurrences(of: "seventh", with: "ox"))] = "\(msgInfo?[(String(appHandleGenderUrl) + showAfterStr.replacingOccurrences(of: "seventh", with: "ox"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(userPoseId.prefix(6)) + "ength")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(userPoseId.prefix(6)) + "ength")] = "\(msgInfo?[(String(userPoseId.prefix(6)) + "ength")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (mainDoingCaptureMessage.replacingOccurrences(of: "roll", with: "yy") + "-MM-" + String(dataQueryerString.suffix(7)) + String(dataWatchValue.suffix(4)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(mainRefuseData))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == ChartShowSceneScientific.share.loginUserMode.userID ? "1" : "0"
        info?[(String(notiEnjoyString))] = senderId == ChartShowSceneScientific.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func recommend(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = terms(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(kRedPhasePath.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func valueEnable(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func equallyInclude(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func smallDestroy(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(main_personalValue))] as? String, msgType == (mainDirectUrl.replacingOccurrences(of: "invisible", with: "o")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(app_databaseMessage.replacingOccurrences(of: "present", with: "n") + "tTyp" + String(noti_revealMessage))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(showSingleFoundId) + notiCountegrationUrl.replacingOccurrences(of: "enjoy", with: "g"))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(noti_publishStr.replacingOccurrences(of: "position", with: "di") + "oData")] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(main_bedMsg.replacingOccurrences(of: "fit", with: "a") + String(k_emptyMsg))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
