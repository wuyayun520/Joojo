
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_speakerAccessibleUpdatelyValue:[UInt8] = [0x52,0x57,0x52,0x5d,0x11,0x4c,0x58,0x4d,0x4e,0x5b,0x23,0x12,0x9,0x51,0x4a,0x5c,0x9,0x57,0x58,0x5d,0x9,0x4b,0x4e,0x4e,0x57,0x9,0x52,0x56,0x59,0x55,0x4e,0x56,0x4e,0x57,0x5d,0x4e,0x4d]

fileprivate func holdOrange(table num: UInt8) -> UInt8 {
    let value = Int(num) + 23
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let constPastPlatMessage:[UInt8] = [0xa2,0xbe,0xb3]

/*: "headPic" :*/
fileprivate let userPlainUrl:[Character] = ["h","e","a","d","P","i"]
fileprivate let show_dialogFormat:String = "C"

/*: "nickname" :*/
fileprivate let notiLastName:String = "nicautomaticam"
fileprivate let k_ticPartString:String = "disable"

/*: "birthday" :*/
fileprivate let k_arrayPath:String = "bitranslationthd"
fileprivate let k_alongKey:[Character] = ["a","y"]

/*: "videoUrl" :*/
fileprivate let const_flexGirlData:String = "videoUrpion send production raw shark"
fileprivate let user_visitUniversalPath:[Character] = ["l"]

/*: "location" :*/
fileprivate let mainTailEnhanceMessage:String = "LOCATI"
fileprivate let kGamePath:String = "okeep"

/*: "cmd" :*/
fileprivate let userImpressionMessage:[UInt8] = [0x88,0x86,0x8f]

private func samePrimaryMissing(fit num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "refuseCall" :*/
fileprivate let k_manageStr:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let app_readingData:[UInt8] = [0xb9,0xbc,0xa9,0xbc]

private func couldMeeting(strength num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "Call interruption" :*/
fileprivate let constCorruptionSmileId:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i"]
fileprivate let user_challengeString:String = "operate"

/*: "stopCall" :*/
fileprivate let app_easeFormat:String = "title pickstopC"
fileprivate let kInsideMsg:[Character] = ["a","l","l"]

/*: "logId" :*/
fileprivate let dataRemarkAmStageUrl:[UInt8] = [0x3e,0x3d,0x35,0x1b,0x36]

private func optionEven(automat num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "cancelCall" :*/
fileprivate let appTingBehindString:String = "CANCE"

/*: "No answer" :*/
fileprivate let show_stepFoundOnicialGenderUrl:String = "No ansrelation than to flow medal"
fileprivate let showAlwaysString:[Character] = ["w","e","r"]

/*: "duration" :*/
fileprivate let data_enableEnoughFormat:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let showBlindKingString:[Character] = ["S","h","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let main_satisfyKey:String = "barn emerge remcancelSex"

/*: "He has hung up" :*/
fileprivate let const_literalValue:String = "He has huvariable accept"
fileprivate let show_tunStr:String = "close processng up"

/*: "endType" :*/
fileprivate let mainWhiteMsg:String = "endTypescale ask attention"

/*: "stopSex" :*/
fileprivate let k_compositionPortString:String = "stopSeach discount package pi we"
fileprivate let show_topTitle:String = "econstant"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let appChildMaterialValue:[UInt8] = [0x6a,0x4d,0x50,0x56,0x45,0x45,0x4a,0x40,0x4a,0x46,0x4d,0x57,0x3,0x44,0x4c,0x4f,0x47,0x3,0x40,0x4c,0x4a,0x4d,0x50,0xf,0x3,0x40,0x42,0x4f,0x4f,0x3,0x4b,0x42,0x50,0x3,0x46,0x4d,0x47,0x46,0x47]

private func tunSibling(accept num: UInt8) -> UInt8 {
    return num ^ 35
}

/*: "The other hung up the phone" :*/
fileprivate let app_batteryString:String = "The oi input hold map rank"
fileprivate let mainHostTeeStr:String = "live each promise nobody hung up"
fileprivate let userBudgeName:String = " the cur character"

/*: "type" :*/
fileprivate let appDistanceKey:String = "explainype"

/*: "jumpUrl" :*/
fileprivate let show_identificationValue:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let notiEnableeId:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let k_composeInstructionData:[Character] = ["G","e","t"," ","g","o","l","d"," ","c","o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let userBeanName:String = "automaticallyfu"

/*: "onAcceptCall" :*/
fileprivate let app_throughFutureAppointedStr:String = "administrative existingonAccept"
fileprivate let appPadString:String = "Callrequest movie quantity spring"

/*: "acceptCall" :*/
fileprivate let app_texturePath:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let user_subjectFormat:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let notiFlagFunPath:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," "]
fileprivate let const_processingAllowStr:[Character] = ["e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TechniqueOld.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let app_mustId = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class TechniqueOld: TrainBrush {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: GreatEnhanceTimingAgileModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ExtendEquipment? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: StateView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ToPresent.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToPresent.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToPresent.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_speakerAccessibleUpdatelyValue.map{holdOrange(table: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MediaAfter
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.quitBasic()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            remind()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.magnitudeerruptCancel()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        childLikeShareImmediate()
        //: setupSubViewsConstraint()
        bagDismiss()
        //: initNotificationObserver()
        tab()
        //: req_callGetUserInfo()
        flag()
        //: timeoutRules()
        deploy()
        //: resumeTalk()
        pastSh()
        //: setUpBGMPlayer()
        belowDown()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
            if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = DetailView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.byWork = false
        ToPresent.shared.byWork = false
        //: TalkingSocketManager.shared.isCalling = false
        ToPresent.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.stateOn()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: WriteEventView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = WriteEventView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: MenuArc = {
        //: let m = TalkingVoiceChatManager()
        let m = MenuArc()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func flag() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        OriginalMaterial.doingtoTo(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: constPastPlatMessage.map{$0^215}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(userPlainUrl) + show_dialogFormat.lowercased())].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(notiLastName.replacingOccurrences(of: "automatic", with: "kn") + k_ticPartString.replacingOccurrences(of: "disable", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_arrayPath.replacingOccurrences(of: "translation", with: "r") + String(k_alongKey))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(const_flexGirlData.prefix(7)) + String(user_visitUniversalPath))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(mainTailEnhanceMessage.lowercased() + kGamePath.replacingOccurrences(of: "keep", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.present()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func remind() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ContainPlate.filter(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.bury(showMsg: kTextStr)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(k_manageStr)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: constPastPlatMessage.map{$0^215}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.save()
                //: return
                return
            }

            //: self.stopBGM()
            self.brownCalculate()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ExtendEquipment.brushWrite(type: .Response)
            //: self.startBGM()
            self.quitBasic()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func stateOn() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.brownCalculate()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ToPresent.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ToPresent.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ToPresent.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func save(animated: Bool = true) {
        //: self.stopBGM()
        self.brownCalculate()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func magnitudeerval() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.include(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func pastSh() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            magnitudeerval()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func tab() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(exclusive(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(delicateLocation(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: data_singleTitle,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        app_mustId.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func exclusive(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            save()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func delicateLocation(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ToPresent.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ToPresent.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ToPresent.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ToPresent.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.bitBy(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension TechniqueOld: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.bury(showMsg: (String(constCorruptionSmileId) + user_challengeString.replacingOccurrences(of: "operate", with: "on")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(app_easeFormat.suffix(5)) + String(kInsideMsg)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (appTingBehindString.lowercased() + "lCall"), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(k_manageStr)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: constPastPlatMessage.map{$0^215}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.before()
                //: TalkingSocketManager.shared.byWorkVC = nil
                ToPresent.shared.byWorkVC = nil
            }
            //: popCurrentViewController()
            save()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.theM(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func belowDown() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? StopIndex.Call : StopIndex.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ExtendEquipment.brushWrite(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func quitBasic() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.belowCalled()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func brownCalculate() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.indicator()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 超时定时器
    //: private func timeoutRules() {
    private func deploy() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? user_viewMsg : self.chatModel?.past()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            startFor()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.startFor()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func startFor() {
        //: guard !TalkingSocketManager.shared.byWork else { return }
        guard !ToPresent.shared.byWork else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.bury(showMsg: (String(show_stepFoundOnicialGenderUrl.prefix(6)) + String(showAlwaysString)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (appTingBehindString.lowercased() + "lCall"), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(k_manageStr)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: constPastPlatMessage.map{$0^215}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ToPresent.shared.formLock(info: socketDict)
        //: popCurrentViewController()
        save()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func remove(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(data_enableEnoughFormat))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.stepWithQuit(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.popularSplit()
            //: createTalkRoom()
            magnitudeerval()
            //: self.stopBGM()
            self.brownCalculate()
        }
    }
}

// MARK: - ListenerShared【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension TechniqueOld: ListenerShared {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func underRespective(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showBlindKingString)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(main_satisfyKey.suffix(9)))].stringValue == SharedOdd.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_literalValue.prefix(9)) + String(show_tunStr.suffix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bury(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.before()
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }

        //: popCurrentViewController()
        save()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func featureReplacement(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.brownCalculate()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.popularSplit()
        //: self.createTalkRoom()
        self.magnitudeerval()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func robot(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(mainWhiteMsg.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(showBlindKingString)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(k_compositionPortString.prefix(5)) + show_topTitle.replacingOccurrences(of: "constant", with: "x"))].stringValue == SharedOdd.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(const_literalValue.prefix(9)) + String(show_tunStr.suffix(5))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue {
            if ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: appChildMaterialValue.map{tunSibling(accept: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(app_batteryString.prefix(4)) + "other" + String(mainHostTeeStr.suffix(8)) + String(userBudgeName.prefix(5)) + "phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bury(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.before()
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.styleSuccessfully()
        //: self.popCurrentViewController()
        self.save()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: show_rateBridgeStr, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func offFunctional(msg: String, data: [String: Any]) {
        //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(appDistanceKey.replacingOccurrences(of: "explain", with: "t"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(show_identificationValue))] as? String else { return }
            //: let httpsUrl = ChartShowSceneScientific.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = ChartShowSceneScientific.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = LocationMove()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: BeforeAssociate.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            BeforeAssociate.share.magnitudeer(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ImmediateThan.beanTransform(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(notiEnableeId)).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(k_composeInstructionData)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ImmediateThan.mage()
                //: } rightBlock: {
            } rightBlock: {
                //: BeforeAssociate.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                BeforeAssociate.share.computeEstimated(clickEvent: k_windowCommandTitle, sufficient: false)
            }
        }
    }
}

// MARK: - MayFactoryExclusive【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TechniqueOld: MayFactoryExclusive {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func stackFor(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showBlindKingString)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(userBeanName.replacingOccurrences(of: "automatically", with: "re") + "seSex")].stringValue == SharedOdd.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_literalValue.prefix(9)) + String(show_tunStr.suffix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bury(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.before()
            //: TalkingSocketManager.shared.byWorkVC = nil
            ToPresent.shared.byWorkVC = nil
        }
        //: popCurrentViewController()
        save()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func pathWith(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.brownCalculate()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.popularSplit()
        //: self.createTalkRoom()
        self.magnitudeerval()
    }
}

// MARK: - BoxFind【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension TechniqueOld: BoxFind {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func weeklyImplement(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_manageStr)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.bury(showMsg: (String(show_stepFoundOnicialGenderUrl.prefix(6)) + String(showAlwaysString)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.before()
                //: TalkingSocketManager.shared.byWorkVC = nil
                ToPresent.shared.byWorkVC = nil
            }
            //: self.popCurrentViewController()
            self.save()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(app_throughFutureAppointedStr.suffix(8)) + String(appPadString.prefix(4))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return }
            //: BeforeAssociate.share.func__jumpToWebRecharge(sufficient: false)
            BeforeAssociate.share.computeEstimated(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(app_throughFutureAppointedStr.suffix(8)) + String(appPadString.prefix(4))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.bury(showMsg: msg)
            //: self.popCurrentViewController()
            self.save()
        }
    }
}

// MARK: - HeadIcon【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension TechniqueOld: HeadIcon {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func upSignificant() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(app_texturePath)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: constPastPlatMessage.map{$0^215}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func exceptionSustainable() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bury(showMsg: (String(user_subjectFormat)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (appTingBehindString.lowercased() + "lCall"), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bury(showMsg: (String(user_subjectFormat)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(k_manageStr)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: constPastPlatMessage.map{$0^215}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.bury(showMsg: (String(notiFlagFunPath) + String(const_processingAllowStr)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ToPresent.shared.formLock(info: [String(bytes: userImpressionMessage.map{samePrimaryMissing(fit: $0)}, encoding: .utf8)!: (String(app_easeFormat.suffix(5)) + String(kInsideMsg)), String(bytes: app_readingData.map{couldMeeting(strength: $0)}, encoding: .utf8)!: [String(bytes: dataRemarkAmStageUrl.map{optionEven(automat: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.styleSuccessfully()
        //: self.popCurrentViewController()
        self.save()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func maximum(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.architectureFrame(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func message(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.discount(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func substantialConversation() {
        //: TalkingSocketManager.shared.byWorkVC = self
        ToPresent.shared.byWorkVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = StateView.displayConnectionMicModel()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.chemist()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.beforePositionMe()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.through(talkingTime)
        //: popCurrentViewController()
        save()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func restrictionQuick(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.through(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func application(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.front(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension TechniqueOld {
    /// 添加视图
    //: private func setupSubviews() {
    private func childLikeShareImmediate() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func bagDismiss() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
