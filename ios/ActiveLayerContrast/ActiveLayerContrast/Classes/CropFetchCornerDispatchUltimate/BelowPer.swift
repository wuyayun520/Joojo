
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let k_unlessValue:String = "mwatch"
fileprivate let app_offerPath:[Character] = ["/","m","o","m","e","n","t","/","l","i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let userHungTitle:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let appProjectionUrl:String = "/momentsspeaker task staff instance"

/*: "mf/moment/like" :*/
fileprivate let main_joinPath:String = "choosef"
fileprivate let dataUntilUrl:[Character] = ["/","m","o","m","e","n","t","/","l","i","k","e"]

/*: "mf/crush/send" :*/
fileprivate let userUniformThatName:[Character] = ["m","f","/","c","r","u","s","h","/","s"]
fileprivate let k_propertyStr:[Character] = ["e","n","d"]

/*: "mf/moment/view" :*/
fileprivate let mainShGesturePath:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let notiScheduleTitle:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","l","i","s","t"]

/*: "mf/momentReply/save" :*/
fileprivate let app_becomeData:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let show_frameworkString:String = "tRdiscover three document clip literal"
fileprivate let noti_allowName:String = "snap coordinator agreementeply/"

/*: "mf/moment/videoSign" :*/
fileprivate let main_oneUrl:String = "mpresentation"
fileprivate let app_stripAgentKey:String = "panel recording/mome"
fileprivate let mainTowardMsg:String = "equipment keep substantialdeoSign"

/*: "dynamicPic.zip" :*/
fileprivate let constBringCapableMsg:String = "dyplus"
fileprivate let dataWindowUrl:String = "product make doamicP"

/*: "pic" :*/
fileprivate let dataDomainTimeData:[UInt8] = [0xa5,0xbc,0xb6]

/*: "content" :*/
fileprivate let app_logValue:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let app_heSignificantString:String = "before phase deadline channelmf/mo"
fileprivate let main_prepareGrabSelectKey:[Character] = ["e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let k_renderCenterTitle:[UInt8] = [0xee,0x13,0x0,0xa,0x7,0x11,0x6,0xbe,0xf1,0x13,0x1,0x1,0x3,0x11,0x11,0x4,0x13,0xa,0xca,0xbe,0xe5,0xd,0xbe,0x12,0xd,0xbe,0xeb,0x17,0xbe,0xe2,0x17,0xc,0xff,0xb,0x7,0x1,0x11,0xbe,0x12,0xd,0xbe,0x14,0x7,0x3,0x15]

fileprivate func blingCur(music num: UInt8) -> UInt8 {
    let value = Int(num) + 98
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let showSubstantialValue:String = "table basic hiddenmf/mo"
fileprivate let k_heartString:String = "veVideoallow personal smile"

/*: .png" :*/
fileprivate let kEnterPath:String = ".pngmain will form ting status"

/*: "dynamicPic" :*/
fileprivate let constAnalyzeStr:String = "bityn"
fileprivate let main_infoEmergeMsg:[Character] = ["a","m","i","c","P","i","c"]

/*: "freeImage" :*/
fileprivate let const_tipModifyFormat:[Character] = ["f","r"]
fileprivate let appNorMessage:[Character] = ["e","e","I","m","a","g","e"]

/*: "mf/momentReply/replylist" :*/
fileprivate let kGuideFeedTitle:String = "mf/maddition tier male curve"
fileprivate let notiDistinctionKey:[Character] = ["R"]
fileprivate let constForeShakeMsg:[Character] = ["e","p","l","y","/","r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let app_clearlyChangeFormat:[Character] = ["m","f","/","m","o","m","e","n","t","/","d","e","l","e"]
fileprivate let mainIncreasePath:String = "discovere"

/*: "mf/momentReply/delete" :*/
fileprivate let kModifyFormat:String = "mf/mocheck behavior receive required"
fileprivate let main_failureKey:[Character] = ["m","e","n","t","R","e","p","l","y","/","d","e","l","e","t","e"]

/*: "mf/moment/pin" :*/
fileprivate let kStatusAskConvertFormat:String = "mf/moyes heavy"

/*: "mf/moment/remindNum" :*/
fileprivate let notiHoneyData:[Character] = ["m","f","/","m","o","m","e","n","t","/","r"]
fileprivate let data_textureGreenValue:String = "emindNumsmart protection"

/*: "mf/moment/remindList" :*/
fileprivate let noti_analysisValue:[Character] = ["m","f","/","m","o"]
fileprivate let noti_mediumString:String = "ME"
fileprivate let userDocumentMessage:String = "eh threshold sophisticated lover cannt/rem"

/*: "page" :*/
fileprivate let mainCombinedTitle:[UInt8] = [0x65,0x67,0x61,0x70]

/*: "cos/getCredential" :*/
fileprivate let dataFragmentChapterFormat:String = "trade spacecos/"
fileprivate let main_integrityKey:String = "portfolio turn explicit egggetCr"
fileprivate let const_introValue:String = "TIAL"

/*: "type" :*/
fileprivate let showTingFormat:String = "tybettere"

/*: "user/reportUserInfo" :*/
fileprivate let data_capableData:String = "user/rinstruction group toward"
fileprivate let userBehaviorName:[Character] = ["e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let userMicPath:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let dataWritingTitle:[Character] = ["d","e","v","i","c","e","I","d"]

/*: "deviceName" :*/
fileprivate let const_tunPath:String = "deblocc"

/*: "iphone" :*/
fileprivate let user_nativeId:String = "iphoremote"

/*: "fileUrl" :*/
fileprivate let const_recommendName:String = "fileUdisabled accept appointment sale"
fileprivate let data_increasinglyValue:[Character] = ["r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BelowPer.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class BelowPer: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func distanceKit(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (k_unlessValue.replacingOccurrences(of: "watch", with: "f") + String(app_offerPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func combineOld(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(userHungTitle) + String(appProjectionUrl.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func dismissMain(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (main_joinPath.replacingOccurrences(of: "choose", with: "m") + String(dataUntilUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func last(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(userUniformThatName) + String(k_propertyStr))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func alongAvailable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(mainShGesturePath))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func shapeTo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(notiScheduleTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func drag(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(app_becomeData) + String(show_frameworkString.prefix(2)) + String(noti_allowName.suffix(5)) + "save")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func associated(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (main_oneUrl.replacingOccurrences(of: "presentation", with: "f") + String(app_stripAgentKey.suffix(5)) + "nt/vi" + String(mainTowardMsg.suffix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func clickBar(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        resistanceMale()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            orientation(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((constBringCapableMsg.replacingOccurrences(of: "plus", with: "n") + String(dataWindowUrl.suffix(5)) + "ic.zip"))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: dataDomainTimeData.map{$0^213}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(app_logValue))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = StrategyArray()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(app_heSignificantString.suffix(5)) + "ment/sav" + String(main_prepareGrabSelectKey))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                constOriginId.product(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.allInsidePosition(showMsg: String(bytes: k_renderCenterTitle.map{blingCur(music: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.resistanceMale()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func doingRefresh(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(showSubstantialValue.suffix(5)) + "ment/sa" + String(k_heartString.prefix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func orientation(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.yellow()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(kEnterPath.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((constAnalyzeStr.replacingOccurrences(of: "bit", with: "d") + String(main_infoEmergeMsg)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func resistanceMale() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(const_tipModifyFormat) + String(appNorMessage)))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func window(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(kGuideFeedTitle.prefix(4)) + "oment" + String(notiDistinctionKey) + String(constForeShakeMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func disabled(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(app_clearlyChangeFormat) + mainIncreasePath.replacingOccurrences(of: "discover", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func exceptRed(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(kModifyFormat.prefix(5)) + String(main_failureKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func perform(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(kStatusAskConvertFormat.prefix(5)) + "ment/pin")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func always(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(notiHoneyData) + String(data_textureGreenValue.prefix(8)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func checkerUpon(page: Int, completion: @escaping (_ data: [AmongSecond]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(noti_analysisValue) + noti_mediumString.lowercased() + String(userDocumentMessage.suffix(6)) + "indList")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: mainCombinedTitle.reversed(), encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [AmongSecond] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<AmongSecond>.deserialize(from: arr as? Array) as? [AmongSecond] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func choice(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(dataFragmentChapterFormat.suffix(4)) + String(main_integrityKey.suffix(5)) + "eden" + const_introValue.lowercased())
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(showTingFormat.replacingOccurrences(of: "better", with: "p")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func exception(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = StrategyArray()
        //: reqModel.requestServer = ChartShowSceneScientific.share.appConfigMode.reportDomain
        reqModel.requestServer = ChartShowSceneScientific.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(data_capableData.prefix(6)) + "eportUs" + String(userBehaviorName))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = ChartShowSceneScientific.share.loginUserMode.userID
        params[(String(userMicPath))] = ChartShowSceneScientific.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(dataWritingTitle))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(const_tunPath.replacingOccurrences(of: "bloc", with: "vi") + "eName")] = (user_nativeId.replacingOccurrences(of: "remote", with: "ne"))
        //: params["type"] = 1
        params[(showTingFormat.replacingOccurrences(of: "better", with: "p"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(const_recommendName.prefix(5)) + String(data_increasinglyValue))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
