
//: Declare String Begin

/*: "cmd" :*/
fileprivate let show_takeRomanTitle:[UInt8] = [0x48,0x52,0x49]

fileprivate func themeSimilar(object num: UInt8) -> UInt8 {
    let value = Int(num) - 229
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let mainAgainstStr:[Character] = ["c","a","n","c","e","l","C","a","l"]
fileprivate let showRejectIlkFormat:String = "L"

/*: "data" :*/
fileprivate let showNumbId:[UInt8] = [0x52,0x4f,0x62,0x4f]

fileprivate func fixedAbove(increasingly num: UInt8) -> UInt8 {
    let value = Int(num) + 18
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let dataNousId:String = "section gender tab section restorelogId"

/*: "#322870" :*/
fileprivate let notiEnhanceWhiteMessage:String = "#impressive"
fileprivate let kCornerFindTitle:[Character] = ["2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let dataSinkPath:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i","d"]
fileprivate let noti_recommendData:String = "vid button noneeo c"
fileprivate let constRowData:[Character] = ["h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let const_oiPleaseMessage:[Character] = ["i","c","o","n","_","c","l","o","s","e","_","n"]
fileprivate let mainContainAdviceString:String = "OR"

/*: "icon_views_number" :*/
fileprivate let user_reasonValue:[Character] = ["i","c","o","n","_","v","i","e","w","s","_","n"]
fileprivate let dataHeFormat:[Character] = ["u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let app_simultaneouslyTitle:[Character] = ["%","@"," ","c","h"]
fileprivate let k_takeMessage:String = "ATTING"

/*: "icon_lounge" :*/
fileprivate let mainSpellRemindTitle:String = "icon_lbury till momentum process mutt"
fileprivate let showHangingPath:[Character] = ["o","u","n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let kMarkerBorrowStr:String = "icon_player tip constant"
fileprivate let show_ownerHeavyValue:[Character] = ["_","c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let noti_ignorePureUrl:[Character] = ["v","i","p","_","b","t","n","_","t","o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let user_liteFormat:String = "btn_nearwoman false husband warning safe"
fileprivate let main_quantityString:String = "by_sealive mechanism"
fileprivate let main_increasinglyComprehensiveName:String = "element coalition to limited_nor"

/*: "%@ Coins/min" :*/
fileprivate let app_shareCommandPath:String = "%@ Coiversion privacy constraint lower"
fileprivate let appMicTitle:[Character] = ["n","s","/","m","i","n"]

/*: "img_match_connecting" :*/
fileprivate let main_installPortedFormat:String = "create image clubimg_mat"
fileprivate let show_chartMsg:String = "cportfolio"
fileprivate let constSineAirPath:[Character] = ["_","c","o","n","n","e","c","t","i","n","g"]

/*: "#9F80FF" :*/
fileprivate let show_revealString:[Character] = ["#","9","F","8","0","F"]
fileprivate let user_meetingEconomicalId:[Character] = ["F"]

/*: "get json error" :*/
fileprivate let showQuerySternStr:String = "cut with single norget js"
fileprivate let k_recognizeMsg:[Character] = ["r","r","o","r"]

/*: "matchId" :*/
fileprivate let data_messageKey:[Character] = ["m","a","t","c","h","I"]
fileprivate let app_routePath:String = "enormous"

/*: "errno" :*/
fileprivate let appWindowTitle:[UInt8] = [0xea,0xf7,0xf7,0xf3,0xf4]

fileprivate func examineerBling(professional num: UInt8) -> UInt8 {
    let value = Int(num) - 133
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let user_globalEndString:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let constInputStr:String = "Matchchoose magnitude cover word"
fileprivate let userAwakeName:String = "ing nowfire heli"

/*: "userTouch" :*/
fileprivate let showStretchProgramAriaTitle:[Character] = ["u","s","e","r","T","o"]
fileprivate let mainMarkPath:String = "uyourselfh"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let main_whiteData:String = "mf/frexpression publish whole"
fileprivate let show_portfolioMessage:String = "shadow thread operateeeCa"
fileprivate let userQuickValidFormat:String = "etVideview resolve appointment"

/*: "fromType" :*/
fileprivate let user_nobodyPopGlassFormat:[UInt8] = [0x10,0x4,0x19,0x1b,0x22,0xf,0x6,0x13]

private func greenHandle(primarily num: UInt8) -> UInt8 {
    return num ^ 118
}

/*: "You've been barred from receiving calls" :*/
fileprivate let mainCriticalPath:[UInt8] = [0xe8,0xde,0xc4,0x96,0xc7,0xd4,0x91,0xd3,0xd4,0xd4,0xdf,0x91,0xd3,0xd0,0xc3,0xc3,0xd4,0xd5,0x91,0xd7,0xc3,0xde,0xdc,0x91,0xc3,0xd4,0xd2,0xd4,0xd8,0xc7,0xd8,0xdf,0xd6,0x91,0xd2,0xd0,0xdd,0xdd,0xc2]

/*: "source" :*/
fileprivate let notiSpecialPath:[UInt8] = [0x1,0x1d,0x7,0x0,0x11,0x17]

private func columnBulletOval(architecture num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "type" :*/
fileprivate let main_screenValue:[UInt8] = [0x9d,0x90,0x99,0x8c]

private func mortalPublisher(yellow num: UInt8) -> UInt8 {
    return num ^ 233
}

/*: "uid" :*/
fileprivate let main_factorPeopleString:[UInt8] = [0x9,0x15,0x18]

private func pathFreeze(select num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "fromFreeCall" :*/
fileprivate let const_carrierTitle:String = "assistantom"
fileprivate let showLeastStr:[Character] = ["l"]

/*: "requestCall" :*/
fileprivate let const_tactName:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let userCustomerKey:[Character] = ["o","n","V","i","d","e","o","M","a","t","c"]
fileprivate let k_helpFormat:String = "since"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let user_herMsg:[UInt8] = [0xca,0xd0,0xe4,0xe5,0xfe,0xfc,0xf0,0xe5,0xf8,0xf2,0xb1,0xe7,0xf8,0xf5,0xf4,0xfe,0xcc,0xb1,0xc3,0xf4,0xf2,0xf4,0xf8,0xe7,0xf4,0xb1,0xe7,0xf8,0xf5,0xf4,0xfe,0xb1,0xf2,0xf0,0xfd,0xfd,0xb1,0xf8,0xff,0xf2,0xfe,0xfc,0xf8,0xff,0xf6]

private func fastOrangeGallery(gift num: UInt8) -> UInt8 {
    return num ^ 145
}

/*: "isRandomVCPush" :*/
fileprivate let dataAutomatId:[Character] = ["i","s","R","a","n","d","o","m","V","C"]
fileprivate let app_possibleData:String = "base female momentum tabPush"

/*: "refuseCall" :*/
fileprivate let data_heapValue:String = "REFUSE"

/*: "onCancelCall" :*/
fileprivate let k_linkPath:[Character] = ["o","n","C","a","n","c","e"]
fileprivate let user_upperUrl:String = "lCallhappy change examine area both"

/*: "retryAfter" :*/
fileprivate let mainMaxUrl:[Character] = ["r","e","t","r","y","A","f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let main_aName:String = "onRcurve mask"
fileprivate let const_beautyManUrl:String = "illegalquillegal"

/*: "failType" :*/
fileprivate let noti_visitMessage:String = "failTypebroad why husband mention ex"

/*: "callWaitingTime" :*/
fileprivate let constCapPath:String = "message second begincallW"
fileprivate let userDataStr:String = "lacktin"
fileprivate let notiEagerValue:[Character] = ["g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let dataRehabName:String = "less join memorytoUid"

/*: "Free 1 min" :*/
fileprivate let app_clickYesterdayStr:[Character] = ["F","r","e","e"," ","1"," ","m","i"]
fileprivate let mainWhenRegulateTitle:String = "automat"

/*: "%@/min" :*/
fileprivate let appGoldSlowPath:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BorderHold.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class BorderHold: TrainBrush {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.that()
            }
        }
        //: self.resetCamera()
        self.doingSlowBridge()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        netReflect()
        //: setupSubViewsConstraint()
        performPromise()
        //: bindInteraction()
        spotIndex()
        //: videoManager.startPreview(isMatch: true)
        videoManager.mountAcross(isMatch: true)
        //: setPlayer(isRandon: false)
        weekly(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            theDrawing()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            ToPresent.shared.formLock(info: [String(bytes: show_takeRomanTitle.map{themeSimilar(object: $0)}, encoding: .utf8)!: (String(mainAgainstStr) + showRejectIlkFormat.lowercased()), String(bytes: showNumbId.map{fixedAbove(increasingly: $0)}, encoding: .utf8)!: [(String(dataNousId.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        respectiveRebuild()
        //: if TalkingSocketManager.shared.byWork == false {
        if ToPresent.shared.byWork == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ToPresent.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ToPresent.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ToPresent.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ToPresent.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: OriginalMaterial = {
        //: let m = TalkingVideoChatManager()
        let m = OriginalMaterial()
        //: let info = TalkingVideoChatModel()
        let info = GreatEnhanceTimingAgileModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .sharkHighlightMaintain()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(theDrawing), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: show_licenseTitle + 1, height: dataChainValue + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (notiEnhanceWhiteMessage.replacingOccurrences(of: "impressive", with: "3") + String(kCornerFindTitle)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(dataSinkPath) + String(noti_recommendData.suffix(4)) + String(constRowData)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.road(name: (String(const_oiPleaseMessage) + mainContainAdviceString.lowercased())), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(easeTension), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: KeepPurple = {
        //: let btn = TalkingButton()
        let btn = KeepPurple()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.road(name: (String(user_reasonValue) + String(dataHeFormat))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.road(name: (String(user_reasonValue) + String(dataHeFormat))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.bagUniform(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(app_simultaneouslyTitle) + k_takeMessage.lowercased()).skipLive(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.road(name: (String(mainSpellRemindTitle.prefix(6)) + String(showHangingPath)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.road(name: (String(kMarkerBorrowStr.prefix(5)) + "rank" + String(show_ownerHeavyValue)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.bagUniform(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.road(name: (String(noti_ignorePureUrl)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: KeepPurple = {
        //: let btn = TalkingButton()
        let btn = KeepPurple()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(underAttention), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.road(name: (String(user_liteFormat.prefix(8)) + String(main_quantityString.prefix(5)) + "ttings" + String(main_increasinglyComprehensiveName.suffix(4)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.road(name: (String(kMarkerBorrowStr.prefix(5)) + "rank" + String(show_ownerHeavyValue)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .clickForRace()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(app_shareCommandPath.prefix(6)) + String(appMicTitle)).skipLive(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.bagUniform(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: CriticalEffect = //: return CriticalEffect(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.road(name: (String(main_installPortedFormat.suffix(7)) + show_chartMsg.replacingOccurrences(of: "portfolio", with: "h") + String(constSineAirPath)))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(show_revealString) + String(user_meetingEconomicalId)))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension BorderHold {
    //: private func setPlayer(isRandon: Bool) {
    private func weekly(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = DocumentWhite.default.countensity(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(showQuerySternStr.suffix(6)) + "on e" + String(k_recognizeMsg)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func two(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(data_messageKey) + app_routePath.replacingOccurrences(of: "enormous", with: "d")): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: show_takeRomanTitle.map{themeSimilar(object: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: appWindowTitle.map{examineerBling(professional: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        BlocCompare.history(params: params) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.tar()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(data_messageKey) + app_routePath.replacingOccurrences(of: "enormous", with: "d"))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func coverLost() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        BlocCompare.instanceSpecifyInfoBecomeThreeAdvertising { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(user_globalEndString))].intValue
            //: self.refreshVideoCallPrice()
            self.that()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension BorderHold {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func underAttention() {
        //: BeforeAssociate.share.func__pushToSubscribeAlert()
        BeforeAssociate.share.corruption()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func easeTension() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        mode()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        data_arrayMsg.automat(eventID: app_boxPath)

        //: if ChartShowSceneScientific.share.appConfigMode.displayMatchV4PopUp, ChartShowSceneScientific.share.loginUserMode.freeCallTimes > 0,
        if ChartShowSceneScientific.share.appConfigMode.displayMatchV4PopUp, ChartShowSceneScientific.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.change()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        chemist()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func theDrawing() {
        //: guard TalkingSocketManager.shared.byWork == false else {
        guard ToPresent.shared.byWork == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bury(showMsg: noti_scientificCurrencyValue)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        voice()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(constInputStr.prefix(5)) + String(userAwakeName.prefix(7))).localized
        //: setPlayer(isRandon: true)
        weekly(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if ChartShowSceneScientific.share.loginUserMode.randomVideoType == 2 && ChartShowSceneScientific.share.loginUserMode.robotVideoNum > 0 {
        if ChartShowSceneScientific.share.loginUserMode.randomVideoType == 2, ChartShowSceneScientific.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            data_arrayMsg.automat(eventID: constDrawerName)
            //: req_getVideoInfo()
            outBecome()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            data_arrayMsg.automat(eventID: data_originStr)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            ToPresent.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            ToPresent.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            ToPresent.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            ToPresent.shared.errorDelegate = self
            //: restart_randomVideo()
            eachWithoutSecure()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func eachWithoutSecure(cmd: String = (String(showStretchProgramAriaTitle) + mainMarkPath.replacingOccurrences(of: "yourself", with: "c")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        respectiveRebuild()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        two(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func tar() {
        // 停止定时器
        //: destroyWaitingTimer()
        respectiveRebuild()
        //: showCloseBtn()
        mode()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(dataSinkPath) + String(noti_recommendData.suffix(4)) + String(constRowData)).localized
        //: setPlayer(isRandon: false)
        weekly(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.byWork == false {
        if ToPresent.shared.byWork == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ToPresent.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ToPresent.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ToPresent.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ToPresent.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        that()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func respectiveRebuild() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func outBecome() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = StrategyArray()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(main_whiteData.prefix(5)) + String(show_portfolioMessage.suffix(4)) + "ll/g" + String(userQuickValidFormat.prefix(6)) + "oInfo")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: user_nobodyPopGlassFormat.map{greenHandle(primarily: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constOriginId.product(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.tar()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: ChartShowSceneScientific.share.loginUserMode.randomVideoType = 1
                    ChartShowSceneScientific.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.theDrawing()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.bury(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = KitFeedback.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = ExecuteOrange()
                //: player.setMute(bEnable: true)
                player.object(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.week(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                GraphicEquipment.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                ContainPlate.tiff { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.informationDown()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func informationDown() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        data_arrayMsg.automat(eventID: k_dailyString)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard GraphicEquipment.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.bury(showMsg: String(bytes: mainCriticalPath.map{$0^177}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            GraphicEquipment.shared.nearWhenWindowRaceFenderItem(type: 1) { succeed, result, errorModel in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.tar()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = PetSection()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.chemist()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.tar()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func voice() {
        //: if ChartShowSceneScientific.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if ChartShowSceneScientific.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(ChartShowSceneScientific.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(ChartShowSceneScientific.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func mode() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func doingSlowBridge() {
        //: guard TalkingSocketManager.shared.byWork == false else {
        guard ToPresent.shared.byWork == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.mountAcross()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func failure() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.theDrawing()
        }
    }
}

// MARK: - ContentSession

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension BorderHold: ContentSession {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func tillSumerval(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = KitModel.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(data_messageKey) + app_routePath.replacingOccurrences(of: "enormous", with: "d")): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: notiSpecialPath.map{columnBulletOval(architecture: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: main_screenValue.map{mortalPublisher(yellow: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: main_factorPeopleString.map{pathFreeze(select: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = BottomReload.randow
        //: if ChartShowSceneScientific.share.loginUserMode.freeCallTimes > 0 && ChartShowSceneScientific.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if ChartShowSceneScientific.share.loginUserMode.freeCallTimes > 0 && ChartShowSceneScientific.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (const_carrierTitle.replacingOccurrences(of: "assistant", with: "fr") + "FreeCal" + String(showLeastStr)))
            //: TalkingSocketManager.shared.isFreeCall = true
            ToPresent.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: show_takeRomanTitle.map{themeSimilar(object: $0)}, encoding: .utf8)!: (String(const_tactName)), String(bytes: showNumbId.map{fixedAbove(increasingly: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        ToPresent.shared.formLock(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            ToPresent.shared.formLock(info: [String(bytes: show_takeRomanTitle.map{themeSimilar(object: $0)}, encoding: .utf8)!: (String(mainAgainstStr) + showRejectIlkFormat.lowercased()), String(bytes: showNumbId.map{fixedAbove(increasingly: $0)}, encoding: .utf8)!: [(String(dataNousId.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.eachWithoutSecure(cmd: (String(userCustomerKey) + k_helpFormat.replacingOccurrences(of: "since", with: "h")))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func liveBySelect(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(dataNousId.suffix(5)))].intValue
    }
}

// MARK: - ListenerShared【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension BorderHold: ListenerShared {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func underRespective(data: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func robot(data: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func offFunctional(msg: String, data: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func featureReplacement(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            TemporaryGrain.examine(body: String(bytes: user_herMsg.map{fastOrangeGallery(gift: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = KitModel.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            factoryAngle(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        tar()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.permissionMatch()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(dataAutomatId) + String(app_possibleData.suffix(4)))] = true
        //: ChartShowSceneScientific.share.start1v1TalkCall(info: newData, situation: .Calling)
        ChartShowSceneScientific.share.scaleContext(info: newData, situation: .Calling)
    }
}

// MARK: - MayFactoryExclusive【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension BorderHold: MayFactoryExclusive {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func stackFor(data: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        eachWithoutSecure(cmd: (data_heapValue.lowercased() + "Call"))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func pathWith(data: [String: Any]) {}
}

// MARK: - BoxFind

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension BorderHold: BoxFind {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func weeklyImplement(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(k_linkPath) + String(user_upperUrl.prefix(5))) { // 取消通话异常
            //: stop_randomVideo()
            tar()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(userCustomerKey) + k_helpFormat.replacingOccurrences(of: "since", with: "h")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            tar()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(data_messageKey) + app_routePath.replacingOccurrences(of: "enormous", with: "d"))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.bury(showMsg: msg)
            //: uploadFailTime(type: 2)
            coverLink(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(userCustomerKey) + k_helpFormat.replacingOccurrences(of: "since", with: "h")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(mainMaxUrl))].intValue), block: { [weak self] timer in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(data_messageKey) + app_routePath.replacingOccurrences(of: "enormous", with: "d"))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.eachWithoutSecure(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(main_aName.prefix(3)) + const_beautyManUrl.replacingOccurrences(of: "illegal", with: "e") + "stCall"), errorNo == YellowReliability.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            tar()
            //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return }
            //: BeforeAssociate.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            BeforeAssociate.share.computeEstimated(clickEvent: k_reasonLastValue, sufficient: false)
            //: uploadFailTime(type: 1)
            coverLink(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        eachWithoutSecure(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension BorderHold {
    //: func uploadFailTime(type: Int) {
    func coverLink(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(noti_visitMessage.prefix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(constCapPath.suffix(5)) + userDataStr.replacingOccurrences(of: "lack", with: "ai") + String(notiEagerValue))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        data_arrayMsg.lightly(eventID: constBlocString, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func factoryAngle(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(constCapPath.suffix(5)) + userDataStr.replacingOccurrences(of: "lack", with: "ai") + String(notiEagerValue))] = time
        //: parm["toUid"] = toUid
        parm[(String(dataRehabName.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        data_arrayMsg.lightly(eventID: noti_channelUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension BorderHold {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func that() {
        //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !ChartShowSceneScientific.share.loginUserMode.loungePlus
        vipIcon.isHidden = !ChartShowSceneScientific.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = BottomReload.randow
        //: if ChartShowSceneScientific.share.loginUserMode.freeCallTimes > 0 && ChartShowSceneScientific.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if ChartShowSceneScientific.share.loginUserMode.freeCallTimes > 0, ChartShowSceneScientific.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if ChartShowSceneScientific.share.appUserConfigMode.callBtnStyle == 1 {
            if ChartShowSceneScientific.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(app_clickYesterdayStr) + mainWhenRegulateTitle.replacingOccurrences(of: "automat", with: "n")).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = ChartShowSceneScientific.share.loginUserMode.loungePlus ? vipFee:fee
            let price = ChartShowSceneScientific.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(appGoldSlowPath)).skipLive(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(appGoldSlowPath)).skipLive(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pass(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = ChartShowSceneScientific.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = ChartShowSceneScientific.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = ChartShowSceneScientific.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = ChartShowSceneScientific.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !ChartShowSceneScientific.share.loginUserMode.loungePlus
            priceStackView.isHidden = !ChartShowSceneScientific.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func netReflect() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func performPromise() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(show_orientationKey + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func spotIndex() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(that),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: const_hiddenCommonMsg,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(coverLost),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: app_fillMinValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(failure), name: user_coverPath, object: nil)
    }
}
