
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let app_pauseName:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let mainPhotoJobMessage:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4c,0x4c,0x41,0x43,0x54,0x53,0x45,0x55,0x51,0x45,0x52,0x5f,0x54,0x41,0x48,0x43,0x4f,0x45,0x44,0x49,0x56,0x5f,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let appDataPartnerId:[UInt8] = [0x88,0x7b,0x79,0x7f,0x8c,0x7b,0x95,0x7c,0x82,0x85,0x77,0x8a,0x89,0x79,0x88,0x7b,0x7b,0x84,0x95,0x84,0x85,0x8a,0x7f,0x7c,0x7f,0x79,0x77,0x8a,0x7f,0x85,0x84]

fileprivate func packageHeave(cell num: UInt8) -> UInt8 {
    let value = Int(num) - 54
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET 连接成功." :*/
fileprivate let mainDifficultFormat:String = "socke"
fileprivate let kBirthdayData:[Character] = ["成","\u{529f}","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let showCleanKey:String = "SOholder"
fileprivate let userHighlightData:String = "conversation interrupt typical drawer hold接失败, er"
fileprivate let data_movieThemeName:String = "home permanentror:"

/*: "SOCKET 失败 :*/
fileprivate let showWhenOxygenFormat:String = "SOCmage"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let notiBackgroundMagnitudeerestUrl:String = "次, 切recommend"
fileprivate let mainJudgeString:String = "random price union include submitket域名"
fileprivate let mainHiUrl:String = "重\u{65b0}尝试，地址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let mainExamineTitle:[Character] = ["S","O","C","K","E","T"," ","\u{5df2}"]
fileprivate let k_nobodyAverageFormat:[Character] = ["关","闭",","," ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let const_pressureExposeString:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: /?sid= :*/
fileprivate let notiGrinFormat:String = "mine effect modify/?sid="

/*: &uid= :*/
fileprivate let mainPicValue:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let appDoingceValue:String = "file deadline interaction medal&plat"
fileprivate let appRedId:String = "forjump"
fileprivate let noti_insertName:String = "ne&vetip charm lab some"

/*: &packageId= :*/
fileprivate let k_averageAlongId:[Character] = ["&","p","a","c","k","a","g","e","I","d"]
fileprivate let showBadPath:[Character] = ["="]

/*: "1v1server. :*/
fileprivate let const_menuRegulateString:String = "1v1seanonymous end i sure"
fileprivate let const_hurryGoValue:[Character] = ["."]

/*: .com:7766" :*/
fileprivate let k_languageStr:[Character] = [".","c","o","m",":","7","7","6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let app_botMessage:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1"]
fileprivate let k_searchMsg:String = "94:7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let app_transferName:[UInt8] = [0x28,0x34,0x38,0x30,0x3e,0x2f,0x5b,0x9c,0xc6,0xea,0x9c,0xc0,0xe7,0x9,0x1e,0x1a,0x18,0x13,0x1a,0x19,0x12,0x17,0x12,0xf,0x2,0x38,0x13,0x1a,0x15,0x1c,0x1e,0x1f,0x9c,0xe0,0xea,0x9e,0xeb,0xd7,0x94,0xc7,0xf7,0x9d,0xed,0xd6,0x9c,0xc6,0xea,0x9e,0xfe,0xc8,0x92,0xec,0xd6,0x55]

private func memberArray(interrupt num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "cmd" :*/
fileprivate let data_decreaseFormat:String = "combinedd"

/*: "ping" :*/
fileprivate let user_altogetherName:String = "pinanima"

/*: "callHeartbeat" :*/
fileprivate let app_tickStr:String = "rating load tab blue yacallHe"
fileprivate let showMaximumKey:String = "adocumentbeat"

/*: "recievedDate" :*/
fileprivate let kBeId:[Character] = ["r","e","c","i","e","v"]
fileprivate let notiSinceQueryerUrl:String = "chart"
fileprivate let show_broadDonIncludeFormat:String = "dDateshade floor"

/*: "liveHeartBeat" :*/
fileprivate let constFormData:String = "shall appeal dialog manager emptyliveH"
fileprivate let k_onlyPerceptUrl:String = "EAR"
fileprivate let noti_oddUrl:String = "agent save expected futuretBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let mainCertainTitle:[Character] = ["🔗"," ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let app_particleDrawingData:String = "road"
fileprivate let mainLibraryKey:String = "he usupporter edit shot failure right"
fileprivate let notiRelationKey:String = "ogs outmake take"

/*: "SOCKET  :*/
fileprivate let main_healthyMsg:String = "SOCKET double natural public"

/*: 重连. 重试次数： :*/
fileprivate let k_symbolIndicatorFormat:String = "重连. \u{91cd}"
fileprivate let dataAlbumGreenFormat:String = "\u{8bd5}次\u{6570}："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let constYouFormat:[Character] = ["S","O","C","K","E","T"," ","连","接","正","\u{5e38}"," ","不","需","\u{8981}","\u{91cd}","新","\u{8fde}"]
fileprivate let constNativeKey:[Character] = ["接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let notiPortId:[Character] = ["S","O","C","K","E","T"," ","正","\u{5728}","连","\u{63a5}"," ","不"]
fileprivate let k_threePath:[Character] = ["需","要","重","新","连","接","."]

/*: "&resume=1" :*/
fileprivate let mainCameraGiveStr:String = "except evolution&resum"
fileprivate let k_mentionMessage:[Character] = ["e","=","1"]

/*: "&lang= :*/
fileprivate let k_toolName:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let userPlayerAdministrativeData:String = "SOCjoin"
fileprivate let showInvitationMessage:String = "T third art boss might"
fileprivate let notiFireMessage:String = "\u{5f00}始连\u{63a5},"

/*: "onNetDisabled" :*/
fileprivate let app_geneticUrl:[Character] = ["o","n","N","e","t","D","i","s","a"]
fileprivate let notiHurryName:[Character] = ["b","l","e","d"]

/*: "errno" :*/
fileprivate let const_followRedUrl:String = "ERRNO"

/*: "data" :*/
fileprivate let show_heaveFormat:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let mainMemoryImplementMsg:String = "SOCtotal"
fileprivate let const_awakeWithConValue:String = "T 连接保\u{6d3b}"
fileprivate let main_rejectUrl:String = "object publicing."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let constHeavyAssessmentMsg:[UInt8] = [0x45,0x59,0x55,0x5d,0x53,0x42,0x36,0x62,0x64,0x62,0x75,0xf2,0xa9,0x8b,0xf0,0xa2,0xad,0x75,0x7b,0x72,0x2c,0x36,0x75,0x77,0x7a,0x7a,0x5e,0x73,0x77,0x64,0x62,0x74,0x73,0x77,0x62,0x38]

private func applicationHello(point num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let showHisUrl:[UInt8] = [0x1,0x1d,0x11,0x19,0x17,0x6,0x72,0x3e,0x3b,0x24,0x37,0xb6,0xed,0xcf,0xb4,0xe6,0xe9,0x31,0x3f,0x36,0x68,0x72,0x3e,0x3b,0x24,0x37,0x1a,0x37,0x33,0x20,0x26,0x10,0x37,0x33,0x26,0x7c]

private func sparBanIndicator(grab num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "onP" :*/
fileprivate let noti_unknownSpellData:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let constHappyMessage:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let const_startUrl:[Character] = ["🔗"," ","处","理","消","息"]
fileprivate let mainDetailOpId:String = "pea"
fileprivate let data_absolutelyFormat:[Character] = ["c","m","d","N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let constHeapFormat:[Character] = [","," ","e","r","r","N","o",":"]

/*: , msg: :*/
fileprivate let notiExclusivePath:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let showRemindUrl:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let dataSucceedCloudUrl:[Character] = ["S","O","C","K","E","T"," ","接","收","异","常","消","息","：","c","m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let k_burySinkKey:[Character] = ["o","n","R","e","c","e","i","v","e"]
fileprivate let showFoundationTitle:String = "dGiftrole remaining gray"

/*: "onVideoMatch" :*/
fileprivate let userBossDarkMessage:String = "onVideolisten type available panel trade"
fileprivate let app_hiddenMsg:String = "have adjust tolerance fitMatch"

/*: "onRequestCall" :*/
fileprivate let appAdminString:String = "available leave big ontoonRequ"
fileprivate let data_futureMessage:String = "estCallsuspend up try"

/*: "onCancelCall" :*/
fileprivate let show_altogetherHoneyStr:[Character] = ["o","n","C","a","n"]
fileprivate let mainAtCartFormat:String = "for explain painter rhocelCall"

/*: "onRefuseCall" :*/
fileprivate let appStartKey:String = "ok"
fileprivate let k_remainingTitle:[Character] = ["n","R","e","f","u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let k_coordinateMsg:String = "word till deadlineonAccep"
fileprivate let app_infoFormat:String = "price present equally medium arenatCall"

/*: "onStopCall" :*/
fileprivate let constEnableString:String = "onStopCpause memory play"
fileprivate let kLocalPath:[Character] = ["a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let user_distributionDepthKey:String = "freeCagender enormous ermine need presidency"
fileprivate let app_favoritePath:[Character] = ["e","s"]

/*: "mfCoin" :*/
fileprivate let user_frontString:String = "install"
fileprivate let kEqualData:String = "fCoinsign heave storage edit"

/*: "onMissCoin" :*/
fileprivate let k_expectedId:String = "onMiseventh production"

/*: "acceptCall" :*/
fileprivate let kReliabilityPath:String = "aloselose"
fileprivate let data_poString:String = "eptCallback we transaction transaction ago"

/*: "stopCall" :*/
fileprivate let app_submitMessage:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let showLoopMessage:[Character] = ["r","e","q","u","e","s","t"]
fileprivate let const_domainMobileData:String = "Calltranslation ref"

/*: "[Invite you to a video call...]" :*/
fileprivate let dataScenarioId:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let user_memberUrl:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let user_inmateFavoriteTitle:[UInt8] = [0xe1,0xf3,0xd4,0xcc,0xd3,0xce,0xdf,0x9a,0xc3,0xd5,0xcf,0x9a,0xce,0xd5,0x9a,0xdb,0x9a,0xcc,0xd5,0xd3,0xd9,0xdf,0x9a,0xd9,0xdb,0xd6,0xd6,0x94,0x94,0x94,0xe7]

private func uniqueMaterial(restore num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "uid" :*/
fileprivate let main_commitHolidayMsg:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let mainPeerKingFormat:[Character] = ["n","i","c","k","n","a"]
fileprivate let showUpperId:String = "architecturee"

/*: "headPic" :*/
fileprivate let userRejectValue:String = "critical location male suspend satisfyheadPic"

/*: "onCallResume" :*/
fileprivate let show_meetingString:String = "onCalltarget holiday security"
fileprivate let app_totalStr:[Character] = ["R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let kSensorMsg:String = "canceher"

/*: "refuseCall" :*/
fileprivate let notiMaximumMirrorTitle:[Character] = ["r","e","f","u","s","e","C","a"]
fileprivate let data_platformString:String = "LL"

/*: "onSendProgramGift" :*/
fileprivate let app_resourceMsg:[Character] = ["o","n","S","e","n","d","P","r","o"]
fileprivate let appSpecialName:String = "practicalr"
fileprivate let data_upKey:[Character] = ["a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let userRefName:String = "characteristic burn false redonGa"

/*: "broadStatus" :*/
fileprivate let noti_spaceId:[Character] = ["b","r","o","a","d","S","t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let k_viaCountegrationPath:[Character] = ["o","n","C","a","l","l","C","h","a","t","M","s","g"]

/*: "onUserBlock" :*/
fileprivate let showComposeMatchLeadingData:String = "onUssea current"
fileprivate let const_designDynamicsPeopleUrl:String = "erBlockaverage mage handle"

/*: "isRecharge" :*/
fileprivate let showPracticalCapacityTitle:[Character] = ["i","s"]
fileprivate let dataOuterName:[Character] = ["R","e","c","h","a","r","g","e"]

/*: "source" :*/
fileprivate let user_clickDraftPath:String = "SOURC"
fileprivate let app_shadeName:[Character] = ["e"]

/*: "invitePop" :*/
fileprivate let constStandardValue:String = "pause people tag application signinvitePop"

/*: "videoPop" :*/
fileprivate let userSameMsg:String = "VID"
fileprivate let user_gemPath:String = "eoPopout custom stream associate"

/*: "coin" :*/
fileprivate let appEasyName:String = "peroin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ToPresent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let const_averagePostString = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let app_floorUrl = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let show_arraySingleId = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let appPoolData = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let dataLimitPath = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let showCrucialUrl = (String(app_pauseName))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let showWhiteParticleUrl = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let k_implementationData = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let appInfoHundredMessage = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let dataSinceId = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kMovieValue = NSNotification.Name(rawValue: String(bytes: mainPhotoJobMessage.reversed(), encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let k_advancedUndertakeId = NSNotification.Name(rawValue: String(bytes: appDataPartnerId.map{packageHeave(cell: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol BoxFind: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func weeklyImplement(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ContentSession: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func tillSumerval(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func liveBySelect(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ListenerShared: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func featureReplacement(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func robot(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func underRespective(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func offFunctional(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol MayFactoryExclusive: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func stackFor(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func pathWith(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class ToPresent: NSObject {
    //: enum SocketRetryType: String {
    enum FrameCurrent: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var byWorkVC: AnyObject?
    var byWorkVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TitlePer.shared.direction == .rightToLeft ? 0 : show_licenseTitle,
        //: y: StatusBarHeight+190
        y: show_orientationKey + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: TitlePer.shared.direction == .rightToLeft ? 0 : show_licenseTitle,
        //: y: StatusBarHeight+190
        y: show_orientationKey + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: BoxFind? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ContentSession? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ListenerShared? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: MayFactoryExclusive? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var byWork = false
    @objc dynamic var byWork = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = ToPresent()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(orientationOrFuture(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(current),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension ToPresent: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        KitTolerance.evenIndex(msg: (mainDifficultFormat.uppercased() + "T 连接" + String(kBirthdayData)))
        //: sendPing()
        attributeSignature()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: data_singleTitle,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: showCrucialUrl)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        resBottom()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        KitTolerance.evenIndex(msg: (showCleanKey.replacingOccurrences(of: "holder", with: "CK") + "ET 连" + String(userHighlightData.suffix(7)) + String(data_movieThemeName.suffix(4))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = ChartShowSceneScientific.share.reachability?.connection, status != .unavailable else {
        guard let status = ChartShowSceneScientific.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == appPoolData {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = darkWho(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            KitTolerance.evenIndex(msg: (showWhenOxygenFormat.replacingOccurrences(of: "mage", with: "K") + "ET 失败") + "\(appPoolData)" + (notiBackgroundMagnitudeerestUrl.replacingOccurrences(of: "recommend", with: "换") + "过Soc" + String(mainJudgeString.suffix(5)) + mainHiUrl) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.upPool(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.upFinish(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        KitTolerance.evenIndex(msg: (String(mainExamineTitle) + String(k_nobodyAverageFormat)) + "\(code)" + (String(const_pressureExposeString)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.everyAccelerateTransform(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == dataSinceId {
            //: return
            return
        }
        //: if let status = ChartShowSceneScientific.share.reachability?.connection, status != .unavailable {
        if let status = ChartShowSceneScientific.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.upPool(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func darkWho(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = ChartShowSceneScientific.share.appConfigMode.ws
        self.linkAddrArr = ChartShowSceneScientific.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = couldListen()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: showCrucialUrl)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = ChartShowSceneScientific.share.loginUid
        let uid = ChartShowSceneScientific.share.loginUid
        //: let sid = ChartShowSceneScientific.share.loginSessionId
        let sid = ChartShowSceneScientific.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(notiGrinFormat.suffix(6))) + "\(sid)" + (String(mainPicValue)) + "\(uid)" + (String(appDoingceValue.suffix(5)) + appRedId.replacingOccurrences(of: "jump", with: "m") + "=ipho" + String(noti_insertName.prefix(5)) + "rsion=") + "\(dataRenderTitle)" + (String(k_averageAlongId) + String(showBadPath)) + "\(noti_messageFormat)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func couldListen() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return noti_visitMenuName ? [(String(const_menuRegulateString.prefix(5)) + "rver" + String(const_hurryGoValue)) + "\(data_graphicUrl)" + (String(k_languageStr))] : [(String(app_botMessage) + k_searchMsg.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension ToPresent {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func orientationOrFuture(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            KitTolerance.evenIndex(msg: String(bytes: app_transferName.map{memberArray(interrupt: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.everyAccelerateTransform(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.upPool(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func current() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.upPool(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.charmDeploy()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension ToPresent {
    /// socket保活心跳包
    //: private func sendPing() {
    private func attributeSignature() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: const_averagePostString, block: { timer in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.formLock(info: [(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm")): (user_altogetherName.replacingOccurrences(of: "anima", with: "g"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func linkEventPer() {
        //: endTalkingPing()
        voiceSum()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: app_floorUrl, block: { timer in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.byWork || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.byWorkVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.byWorkVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.byWork || (self.chemist()?.isKind(of: PerformPoint.self) == false && self.chemist()?.isKind(of: TechniqueOld.self) == false) && (self.byWorkVC?.isKind(of: PerformPoint.self) == false && self.byWorkVC?.isKind(of: TechniqueOld.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.formLock(info: [(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm")): (String(app_tickStr.suffix(6)) + showMaximumKey.replacingOccurrences(of: "document", with: "rt"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func voiceSum() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func charmDeploy() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(kBeId) + notiSinceQueryerUrl.replacingOccurrences(of: "chart", with: "e") + String(show_broadDonIncludeFormat.prefix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < show_petLicenseMaximumValue {
                //: ChartShowSceneScientific.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                ChartShowSceneScientific.share.coca(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension ToPresent {
    /// 开启直播心跳包
    //: func startLivePing() {
    func arcByFrame() {
        //: endLivePing()
        belowCanvas()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: show_arraySingleId, block: { timer in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.formLock(info: [(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm")): (String(constFormData.suffix(5)) + k_onlyPerceptUrl.lowercased() + String(noti_oddUrl.suffix(5)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func belowCanvas() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension ToPresent {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func resBottom() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = ActivityFieldFirst()
        //: let keyWindow = MacroDefine.getWindow()
        let keyWindow = CharacteristicSine.asCap()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.byWork
        self.containerView?.isHidden = !self.byWork
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func broadContainer() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension ToPresent {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func everyAccelerateTransform(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(mainCertainTitle)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: dataSinceId, reason: (app_particleDrawingData.replacingOccurrences(of: "road", with: "T") + String(mainLibraryKey.prefix(4)) + "ser l" + String(notiRelationKey.prefix(7))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        voiceSum()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            broadContainer()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func childDaily() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = ToPresent.shared.darkWho(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.upPool(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func upPool(timeInterval: TimeInterval, type: FrameCurrent) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            KitTolerance.evenIndex(msg: (String(main_healthyMsg.prefix(7))) + "\(type.rawValue)" + (k_symbolIndicatorFormat + dataAlbumGreenFormat) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.resolveAcross(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func resolveAcross(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                KitTolerance.evenIndex(msg: (String(constYouFormat) + String(constNativeKey)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                KitTolerance.evenIndex(msg: (String(notiPortId) + String(k_threePath)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.byWork {
        if self.byWork {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(mainCameraGiveStr.suffix(6)) + String(k_mentionMessage)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(k_toolName)) + "\(TotalSearch.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        everyAccelerateTransform(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = dataLimitPath
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        KitTolerance.evenIndex(msg: (userPlayerAdministrativeData.replacingOccurrences(of: "join", with: "KE") + String(showInvitationMessage.prefix(2)) + notiFireMessage + " 地址：") + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func formLock(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if ChartShowSceneScientific.share.reachability?.connection == nil || ChartShowSceneScientific.share.reachability?.connection == .unavailable {
        if ChartShowSceneScientific.share.reachability?.connection == nil || ChartShowSceneScientific.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm")): (String(app_geneticUrl) + String(notiHurryName)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (const_followRedUrl.lowercased()): "\(appInfoHundredMessage)",
                                       //: "data": [:]]
                                       (String(show_heaveFormat)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            upFinish(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm"))] as? String == (user_altogetherName.replacingOccurrences(of: "anima", with: "g")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                KitTolerance.evenIndex(msg: (mainMemoryImplementMsg.replacingOccurrences(of: "total", with: "KE") + const_awakeWithConValue + "cmd: p" + String(main_rejectUrl.suffix(4))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm"))] as? String == (String(app_tickStr.suffix(6)) + showMaximumKey.replacingOccurrences(of: "document", with: "rt")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                KitTolerance.evenIndex(msg: String(bytes: constHeavyAssessmentMsg.map{applicationHello(point: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm"))] as? String == (String(constFormData.suffix(5)) + k_onlyPerceptUrl.lowercased() + String(noti_oddUrl.suffix(5))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                KitTolerance.evenIndex(msg: String(bytes: showHisUrl.map{sparBanIndicator(grab: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm"))] as? String == (user_altogetherName.replacingOccurrences(of: "anima", with: "g")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm")): (String(noti_unknownSpellData)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (const_followRedUrl.lowercased()): "\(showWhiteParticleUrl)",
                                           //: "data": [:]]
                                           (String(show_heaveFormat)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                upFinish(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.upPool(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func upFinish(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(data_decreaseFormat.replacingOccurrences(of: "combined", with: "cm"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(const_followRedUrl.lowercased())].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(constHappyMessage))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(show_heaveFormat))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(const_startUrl) + mainDetailOpId.replacingOccurrences(of: "pea", with: "：") + String(data_absolutelyFormat)) + "\(cmdName)" + (String(constHeapFormat)) + "\(errorNo)" + (String(notiExclusivePath)) + "\(msg)" + (String(showRemindUrl)) + "\(data)")

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            KitTolerance.evenIndex(msg: (String(dataSucceedCloudUrl)) + "\(cmdName)" + (String(constHeapFormat)) + "\(errorNo)" + (String(notiExclusivePath)) + "\(msg)" + (String(showRemindUrl)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: ChartShowSceneScientific.share.loginUserMode.freeCallTimes = 0
                ChartShowSceneScientific.share.loginUserMode.freeCallTimes = 0
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.weeklyImplement(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(k_burySinkKey) + String(showFoundationTitle.prefix(5))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: k_reasonData, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(userBossDarkMessage.prefix(7)) + String(app_hiddenMsg.suffix(5))) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.tillSumerval(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(appAdminString.suffix(6)) + String(data_futureMessage.prefix(7))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.liveBySelect(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(show_altogetherHoneyStr) + String(mainAtCartFormat.suffix(7))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (appStartKey.replacingOccurrences(of: "ok", with: "o") + String(k_remainingTitle)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(k_coordinateMsg.suffix(7)) + String(app_infoFormat.suffix(5))) { // 接收方发送acceptCall回执
            //: self.byWork = true
            self.byWork = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            linkEventPer()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.pathWith(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(constEnableString.prefix(7)) + String(kLocalPath)) { // 接收方发送stopCall回执
            //: self.byWork = false
            self.byWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            voiceSum()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(user_distributionDepthKey.prefix(6)) + "llTim" + String(app_favoritePath))] as? Int {
                //: ChartShowSceneScientific.share.loginUserMode.freeCallTimes = freeCallTimes
                ChartShowSceneScientific.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: ChartShowSceneScientific.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            ChartShowSceneScientific.share.loginUserMode.mf_coin = json[(String(show_heaveFormat))][(user_frontString.replacingOccurrences(of: "install", with: "m") + String(kEqualData.prefix(5)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            noMaterial(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_rateBridgeStr, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(k_expectedId.prefix(4)) + "ssCoin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.offFunctional(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (kReliabilityPath.replacingOccurrences(of: "lose", with: "c") + String(data_poString.prefix(7))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.byWork = true
            self.byWork = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            linkEventPer()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.featureReplacement(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(app_submitMessage)) { // 接收方/拨打方 挂断了通话
            //: self.byWork = false
            self.byWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            voiceSum()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.robot(data: data)
            // 更新余额
            //: ChartShowSceneScientific.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            ChartShowSceneScientific.share.loginUserMode.mf_coin = json[(String(show_heaveFormat))][(user_frontString.replacingOccurrences(of: "install", with: "m") + String(kEqualData.prefix(5)))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(user_distributionDepthKey.prefix(6)) + "llTim" + String(app_favoritePath))] as? Int {
                //: ChartShowSceneScientific.share.loginUserMode.freeCallTimes = freeCallTimes
                ChartShowSceneScientific.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            noMaterial(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_rateBridgeStr, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false

            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(showLoopMessage) + String(const_domainMobileData.prefix(4))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kMovieValue, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: ChartShowSceneScientific.share.recieve1v1TalkRequest(info: data)
                ChartShowSceneScientific.share.coca(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(kBeId) + notiSinceQueryerUrl.replacingOccurrences(of: "chart", with: "e") + String(show_broadDonIncludeFormat.prefix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: dataScenarioId.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(show_heaveFormat))][(String(user_memberUrl))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: user_inmateFavoriteTitle.map{uniqueMaterial(restore: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                TemporaryGrain.examine(uid: String(json[(String(show_heaveFormat))][(String(main_commitHolidayMsg))].intValue),
                                          //: title: data["nickname"] as? String,
                                          title: data[(String(mainPeerKingFormat) + showUpperId.replacingOccurrences(of: "architecture", with: "m"))] as? String,
                                          //: body: body,
                                          body: body,
                                          //: imageUrl: data["headPic"] as? String)
                                          imageUrl: data[(String(userRejectValue.suffix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                TemporaryGrain.shared.overEmbarrass()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(show_meetingString.prefix(6)) + String(app_totalStr)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.byWork = true
            self.byWork = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            linkEventPer()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(show_meetingString.prefix(6)) + String(app_totalStr))] = "1"
            //: ChartShowSceneScientific.share.recieve1v1TalkResumeRequest(info: resumeData)
            ChartShowSceneScientific.share.sink(info: resumeData)

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (kSensorMsg.replacingOccurrences(of: "her", with: "l") + "Call") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.byWork = false
            self.byWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            voiceSum()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.underRespective(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                TemporaryGrain.dram(identifier: String(json[(String(show_heaveFormat))][(String(main_commitHolidayMsg))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                TemporaryGrain.shared.heaveMaintain()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(notiMaximumMirrorTitle) + data_platformString.lowercased()) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.byWork = false
            self.byWork = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            voiceSum()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.stackFor(data: data)

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(app_resourceMsg) + appSpecialName.replacingOccurrences(of: "practical", with: "g") + String(data_upKey)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.allInsidePosition(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(userRefName.suffix(4)) + "meFs") { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(noti_spaceId))] as? Int ?? -1
            //: if ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue {
            if ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: k_advancedUndertakeId, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: k_reasonData, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: k_advancedUndertakeId, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(k_viaCountegrationPath)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            KeepMethod.insteadIn().productPool(msg: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(showComposeMatchLeadingData.prefix(4)) + String(const_designDynamicsPeopleUrl.prefix(7))) { // 用户被封号
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: const_dimensionFrameMsg, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension ToPresent {
    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func noMaterial(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        ThreadShared.shared.sinceReward()
        //: guard ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue else {
        guard ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(showPracticalCapacityTitle) + String(dataOuterName))] as? Bool == false {
            //: BeforeAssociate.share.func_pushToDiscountRechargeView()
            BeforeAssociate.share.currentQuantityent()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(user_clickDraftPath.lowercased() + String(app_shadeName))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(constStandardValue.suffix(9))) || source == (userSameMsg.lowercased() + String(user_gemPath.prefix(5))) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(appEasyName.replacingOccurrences(of: "per", with: "c"))] as? Int) ?? 0 < 60 {
            //: BeforeAssociate.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            BeforeAssociate.share.deadlineBy(webViewType: .RechargeHalfPage)
        }
    }
}
