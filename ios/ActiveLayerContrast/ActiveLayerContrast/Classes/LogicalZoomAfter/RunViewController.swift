
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiOriginalYeString:[UInt8] = [0x35,0x32,0x35,0x28,0x74,0x3f,0x33,0x38,0x39,0x2e,0x66,0x75,0x7c,0x34,0x3d,0x2f,0x7c,0x32,0x33,0x28,0x7c,0x3e,0x39,0x39,0x32,0x7c,0x35,0x31,0x2c,0x30,0x39,0x31,0x39,0x32,0x28,0x39,0x38]

private func forceReceiverName(woman num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "#128CFF" :*/
fileprivate let notiMAppFormat:String = "fill right system#128CF"
fileprivate let show_dueName:String = "orange"

/*: "Feedback" :*/
fileprivate let main_belowFormat:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let mainHusbandMsg:[Character] = ["#","8","3","7","0"]
fileprivate let k_constructPath:String = "hihi"

/*: "Quick Greeting" :*/
fileprivate let user_eachExistPath:String = "pion appointed everyone translationQuick"
fileprivate let show_blocPath:String = " Greret execute"

/*: "Beauty Photos" :*/
fileprivate let constPutMessage:String = "Beauany manager connection orange"
fileprivate let show_propertyConsumeMsg:String = "otospot"

/*: "url" :*/
fileprivate let userHealthyUrl:String = "usubjectl"

/*: "unlockGift" :*/
fileprivate let app_airId:String = "unprofile"

/*: "isBurn" :*/
fileprivate let appNativeStr:[UInt8] = [0x6e,0x72,0x75,0x42,0x73,0x69]

/*: "giftId" :*/
fileprivate let kTroubleTitle:String = "number fast current occasion stagegiftId"

/*: "rYMsgType" :*/
fileprivate let mainHomeName:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let showWarningSpreadStr:String = "GJ:Calmission am save bullet decrease"
fileprivate let const_dateExImpactStr:String = "tcontrolm"

/*: "changeDirection" :*/
fileprivate let show_tailIdentifyId:String = "CHANGE"
fileprivate let constAgileUrl:String = "Directionfellow basic"

/*: "extra" :*/
fileprivate let constHidePath:String = "expicturea"

/*: "msgInfo" :*/
fileprivate let kChartStr:String = "comment"
fileprivate let appSimultaneouslyTitle:[Character] = ["s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let constImplementationSessionString:String = "toUidcurate class properly"

/*: "wantGift" :*/
fileprivate let kCartValue:[Character] = ["w","a","n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let main_popularData:String = "account app string maintainjumpKe"
fileprivate let kDetailDeleteName:String = "distance"

/*: "logType" :*/
fileprivate let user_imageKey:String = "egg theme wouldlogType"

/*: "content" :*/
fileprivate let kFlowKey:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let noti_alwaysString:[UInt8] = [0xef,0xf5,0xe9,0xd6,0xfb,0xf2,0xe7]

fileprivate func sendDoing(around num: UInt8) -> UInt8 {
    let value = Int(num) + 126
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "txt" :*/
fileprivate let notiFactorValue:String = "TXT"

/*: "message" :*/
fileprivate let userFilterSystemResistanceStr:[UInt8] = [0xe9,0xe1,0xf7,0xf7,0xe5,0xe3,0xe1]

private func adjustAverage(entity num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "replayId" :*/
fileprivate let mainRegionMechanismMsg:[UInt8] = [0xcd,0xc0,0xcb,0xc7,0xbc,0xd4,0xa4,0xbf]

fileprivate func routeRespective(rank num: UInt8) -> UInt8 {
    let value = Int(num) - 91
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let showGiveUrl:[Character] = ["t","y","p","e"]

/*: "TxtMsg" :*/
fileprivate let kDetectStr:String = "injury me hundred fee falseTxtMsg"

/*: "params" :*/
fileprivate let dataVideoWayName:[UInt8] = [0xcf,0xc0,0xd1,0xc0,0xcc,0xd2]

fileprivate func maximumDomain(upper num: UInt8) -> UInt8 {
    let value = Int(num) - 95
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "MultiQuickMsg" :*/
fileprivate let k_pressureRobotPath:String = "Multiat dirt theme identify"
fileprivate let kStickContentureData:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "QuickMsg" :*/
fileprivate let const_productionFormat:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "txtId" :*/
fileprivate let app_unlikelyString:[Character] = ["t","x","t","I","d"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let constTooData:[UInt8] = [0x6f,0x88,0x82,0x7c,0x7e,0x39,0x81,0x7e,0x85,0x85,0x88,0x39,0x7d,0x88,0x90,0x87,0x85,0x88,0x7a,0x7d,0x39,0x7f,0x7a,0x82,0x85,0x7e,0x7d,0x47,0x39,0x69,0x8b,0x88,0x7c,0x7e,0x7d,0x8e,0x8b,0x7e]

fileprivate func ariaNote(receive num: UInt8) -> UInt8 {
    let value = Int(num) - 25
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let k_ageTabValue:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let user_marginPath:String = "audclicko"

/*: "audioId" :*/
fileprivate let app_natureMsg:String = "AUDIO"
fileprivate let main_tierIndicationKey:String = "at listener nothingId"

/*: "audioLength" :*/
fileprivate let noti_increasinglyId:[UInt8] = [0xa0,0xb4,0xa3,0xa8,0xae,0x8b,0xa4,0xad,0xa6,0xb3,0xa7]

fileprivate func elementRef(way num: UInt8) -> UInt8 {
    let value = Int(num) - 63
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let main_sufficientData:String = "administrative black indicator written smileaudioS"
fileprivate let k_inputKey:String = "andbotau"

/*: "senderId" :*/
fileprivate let kSuccessTemporaryId:[UInt8] = [0x7d,0x6b,0x60,0x6a,0x6b,0x7c,0x47,0x6a]

/*: "AudioMsg" :*/
fileprivate let userDisplayPath:String = "AudioMtechnique multi convert translate signal"
fileprivate let show_coalitionHaveId:String = "broadg"

/*: "Can't send blank message" :*/
fileprivate let noti_smileName:[Character] = ["C","a","n","\'","t"," ","s","e"]
fileprivate let notiMemberValue:[Character] = ["n","d"," ","b","l","a","n","k"," ","m"]
fileprivate let noti_coordinateMsg:[Character] = ["e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let notiChallengeTitle:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let userAirId:String = "quoteMgrin combined"

/*: "contentType" :*/
fileprivate let const_freshGalleryName:[UInt8] = [0x76,0x7a,0x7b,0x61,0x70,0x7b,0x61,0x41,0x6c,0x65,0x70]

private func musicGrade(critical num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "ImgMsg" :*/
fileprivate let appYieldKey:String = "ImgMsgmanage extend publisher delicate"

/*: "img" :*/
fileprivate let const_zoneData:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let user_delicateFormat:[UInt8] = [0xf9,0xfd,0xf1,0xf7,0xf5,0xc5,0xe2,0xf9]

private func ownerSatisfy(recover num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "customSend" :*/
fileprivate let const_pinValue:[UInt8] = [0xe1,0xf7,0xf1,0xf6,0xed,0xef,0xd1,0xe7,0xec,0xe6]

private func forceList(discount num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "pic" :*/
fileprivate let appSwitchlyCurrentStickStr:String = "piphase"

/*: "VideoMsg" :*/
fileprivate let appLanguageSinceTitle:String = "kind weVideo"
fileprivate let mainMicRecordString:[Character] = ["M","s","g"]

/*: "video" :*/
fileprivate let const_hardFreshUrl:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let showApproveMessage:[UInt8] = [0x2d,0x21,0x38,0x2b,0x3c,0x7,0x23,0x29]

private func elbowMedium(writing num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "videoUrl" :*/
fileprivate let k_continuingName:[UInt8] = [0x3,0x1c,0x11,0x10,0x1a,0x20,0x7,0x19]

private func sineFlex(mutt num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "duration" :*/
fileprivate let app_activityCellValue:[UInt8] = [0x30,0x21,0x26,0x35,0x20,0x3d,0x3b,0x3a]

private func originSumo(toward num: UInt8) -> UInt8 {
    return num ^ 84
}

/*: "len" :*/
fileprivate let mainPerceptPath:[UInt8] = [0xd9,0xd2,0xdb]

fileprivate func prosecutionAccept(ermine num: UInt8) -> UInt8 {
    let value = Int(num) + 147
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let notiApartmentPromptId:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r"]
fileprivate let user_arcPauseName:String = "every outfit common texture capable/voice/"

/*: "Documents/User/record/" :*/
fileprivate let appFinishKey:[Character] = ["D","o","c","u","m","e","n","t"]
fileprivate let user_minUrl:String = "successfully transition subtles/User"

/*: "msgIncome" :*/
fileprivate let const_selectionKey:String = "mfirstg"
fileprivate let kHeapCeilingName:String = "Incomedeny today para signature con"

/*: "messageType" :*/
fileprivate let app_sufficientString:String = "melightlylightlyage"
fileprivate let userAppealTimeStr:[Character] = ["T","y","p","e"]

/*: "WantGift" :*/
fileprivate let showManagerUrl:String = "kind special group capture cutWantGift"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let show_hereMsg:String = "_Privfeed tab future admin platform"
fileprivate let k_bellyOverwhelmId:String = "ateChamystery combine impression burn out"
fileprivate let noti_writeId:[Character] = ["t","T","i","p","s","T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let main_pointShapeData:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let user_absoluteString:String = "assistoassistal"
fileprivate let noti_pionValue:String = "tolerance analyze bottom deadline crushMfCoin"

/*: "attachSysMsg" :*/
fileprivate let notiThreeString:String = "asameach"
fileprivate let userMotiveValue:String = "SysMsgtrouble word"

/*: "intimatePhoto" :*/
fileprivate let data_wordUrl:String = "jumpntjump"
fileprivate let userApprovePath:[Character] = ["h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let userOriginalId:[Character] = ["t","o","t","a","l","I","n"]
fileprivate let main_yesterdayWriteMessage:String = "whymate"

/*: "uid" :*/
fileprivate let const_arrowUrl:[UInt8] = [0x6b,0x5f,0x5a]

fileprivate func mixYear(impact num: UInt8) -> UInt8 {
    let value = Int(num) + 10
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let notiFutureUrl:[UInt8] = [0x11,0x16,0x1c,0x11,0x15,0x9,0x1c,0xd]

fileprivate func profileGo(tick num: UInt8) -> UInt8 {
    let value = Int(num) + 88
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let dataDenseCanTrustKey:[UInt8] = [0x2e,0x6c,0x65,0x76,0x65,0x6c,0x20,0x79,0x63,0x61,0x6d,0x69,0x74,0x6e,0x69,0x20,0x66,0x6f,0x20,0x6b,0x63,0x61,0x6c,0x20,0x72,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x65,0x75,0x64,0x20,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "giftNum" :*/
fileprivate let constBlindMsg:String = "giftNumnext partner initial"

/*: "pkgItemsetId" :*/
fileprivate let show_increaseData:String = "clot offer refuse meetpkgItem"
fileprivate let mainYesTactTitle:[Character] = ["s","e","t","I","d"]

/*: "from" :*/
fileprivate let showSaveMsg:String = "delayrom"

/*: "wantGiftId" :*/
fileprivate let noti_fractionMessage:String = "wantGiftIcritical comprehensive"
fileprivate let show_screenRepublicData:[Character] = ["d"]

/*: "WantGiftMsg" :*/
fileprivate let dataRequestFailNumberFormat:[Character] = ["W","a","n","t","G","i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let main_helloFormat:String = "giprecis"

/*: "View info" :*/
fileprivate let show_certainGrabMsg:[Character] = ["V","i","e","w"," "]
fileprivate let notiRadioTitle:String = "addfo"

/*: "Notes" :*/
fileprivate let mainDownTitle:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let user_perMoveUrl:[Character] = ["R"]
fileprivate let constFunImplyName:String = "eamrt"

/*: "View read receipts?" :*/
fileprivate let dataReadingKey:String = "Viewnobody day factory again"
fileprivate let noti_orientationKey:String = "receipts?re his characteristic behind weight"

/*: "She" :*/
fileprivate let constDetailedFormat:String = "Shechallenge instruction"

/*: "%@ is using too low a version" :*/
fileprivate let data_captureDonUrl:[Character] = ["%","@"," ","i","s"," ","u","s"]
fileprivate let const_speedMiniId:String = "ing tsis server crop mortal"
fileprivate let notiWrapTitle:String = "heat readingw a ve"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let noti_denyMutualUrl:String = "material multiple qualityShoot"
fileprivate let constUsPath:String = "automatic light whyo at"
fileprivate let kPublisherLightKey:[Character] = [" ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let userSignId:String = "segment finish adUploa"

/*: "播放状态: :*/
fileprivate let app_implementChapterStr:String = "播放状态:"

/*: "c2c_" :*/
fileprivate let noti_documentChartValue:[Character] = ["c","2","c","_"]

/*: "msg" :*/
fileprivate let main_shotReflectData:String = "mscalendar"

/*: "tips" :*/
fileprivate let user_primaryUrl:String = "tipdram"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let user_romanValue:[UInt8] = [0x6c,0x6c,0x61,0x63,0x20,0x61,0x20,0x40,0x25,0x20,0x65,0x76,0x69,0x67,0x20,0x6f,0x74,0x20,0x65,0x72,0x75,0x74,0x61,0x65,0x66,0x20,0x74,0x61,0x68,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x6e,0x61,0x63,0x20,0x75,0x6f,0x59]

/*: "cellData" :*/
fileprivate let const_kingStr:String = "cellDataappropriate distinction"

/*: "F5F5F5" :*/
fileprivate let userBucketPath:String = "universaluniversaluniversal"

/*: "System notification" :*/
fileprivate let dataConstructPath:String = "appeal invisible renderSystem"
fileprivate let k_keepStr:[Character] = [" ","n","o","t","i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RunViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ConstantWith {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class RunViewController: TrainBrush {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: UserChatType = .private
    private var conversationType: UserChatType = .private
    //: private var userWrap = KeepArchitecture()
    private var userWrap = KeepArchitecture() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ConstantWith.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: KeepArchitecture = KeepArchitecture()) {
    init(chatID: String, isFrom: ConstantWith = .Normal, userWrap: KeepArchitecture = KeepArchitecture()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        RunViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        FactoryWriteTranslateFound.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == MacroDefine.getXiaoMiID() {
        if msgView.targetID == CharacteristicSine.beforeVent() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == MacroDefine.getCustomerServiceID() {
        } else if msgView.targetID == CharacteristicSine.reflectOff() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiOriginalYeString.map{forceReceiverName(woman: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
            if ChartShowSceneScientific.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = DetailView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        noFilter()
        //: createUI()
        kit()
        //: createUIConstraints()
        columnAppropriate()
        //: bindInteraction()
        fit()
        //: addTapGestureRecognizer()
        projectionWindow()
        //: func__requestChatInfo()
        pastDiscount()
        //: func__getFillDraftText()
        ignoreDown()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        SkipEnableWait.shared.kindChannel()
        //: func__addVoiceNotification()
        padDown()
        //: addReadPeceiptNotification()
        composeFitMaxMemoryIndicator()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            transfer()
            //: func__TipsNotification()
            suspendFore()
            //: if ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue {
            if ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                handleSustainable()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                light()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        similar(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        similar(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            backSpring()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            ThreadShared.shared.sinceReward()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: CounteractionTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = CounteractionTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(AdjustBecomePaint.self, forCellReuseIdentifier: AdjustBecomePaint.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ExecuteSystemVertical.self, forCellReuseIdentifier: ExecuteSystemVertical.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(ValueGroup.self, forCellReuseIdentifier: ValueGroup.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(BlockLocal.self, forCellReuseIdentifier: BlockLocal.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ToleranceImpact.self, forCellReuseIdentifier: ToleranceImpact.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(TemporaryView.self, forCellReuseIdentifier: TemporaryView.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(AspectContain.self, forCellReuseIdentifier: AspectContain.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(CanvasSubstantialRoman.self, forCellReuseIdentifier: CanvasSubstantialRoman.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(IndependentSkirt.self, forCellReuseIdentifier: IndependentSkirt.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ImplementProperty.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(AtSkipThread.self, forCellReuseIdentifier: ImplementProperty.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: RapidSpell = {
        //: let m = TalkingCallMenuManager()
        let m = RapidSpell()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.groupGold()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: HoldView = {
        //: var type: InputViewType = .PrivateChat
        var type: ResolveSince = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = HoldView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(notiMAppFormat.suffix(6)) + show_dueName.replacingOccurrences(of: "orange", with: "F")))!, .font: UIFont.bagUniform(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(main_belowFormat)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(succeedAcrossRefuse), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: SpineView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = SpineView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: CustomView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = CustomView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bagUniform(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(mainHusbandMsg) + k_constructPath.replacingOccurrences(of: "hi", with: "F"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(user_eachExistPath.suffix(5)) + String(show_blocPath.prefix(4)) + "eting").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.special().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(question), for: .touchUpInside)
        //: btn.isHidden = !ChartShowSceneScientific.share.appUserConfigMode.showDirectGreet || ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue || ChartShowSceneScientific.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !ChartShowSceneScientific.share.appUserConfigMode.showDirectGreet || ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue || ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: HoldDepth? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = HoldDepth()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.bagUniform(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(mainHusbandMsg) + k_constructPath.replacingOccurrences(of: "hi", with: "F"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(constPutMessage.prefix(4)) + "ty Ph" + show_propertyConsumeMsg.replacingOccurrences(of: "spot", with: "s")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.special().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(currencyToDeadline), for: .touchUpInside)
        //: btn.isHidden = ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue || ChartShowSceneScientific.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue || ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: AwayInclude = {
        //: let v = TalkingLiveRoomGamesView()
        let v = AwayInclude()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: IndicatorValue = {
        //: let view = TalkingPrivateBannerView.init()
        let view = IndicatorValue()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: DescriptionDirectly = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = DescriptionDirectly()
        //: v.isHidden = (ChartShowSceneScientific.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: SinkView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = SinkView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.entityVolume()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: TagView = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = TagView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: BeforeAssociate.share.func_pushToDiscountRechargeView()
                BeforeAssociate.share.currentQuantityent()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: BeforeAssociate.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                BeforeAssociate.share.deadlineBy(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: OffView = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = OffView()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.ondUntil(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension RunViewController: PadException {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func no(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.trailing.equalTo(-4)
                    make.trailing.equalTo(-4)
                    //: make.height.equalTo(36)
                    make.height.equalTo(36)
                    //: make.width.equalTo(89)
                    make.width.equalTo(89)
                }
                //: } else {
            } else {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.cameraInserter(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension RunViewController: CouldExitSource {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func question() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        FactoryWriteTranslateFound.addHost(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.bury(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.beyondFor(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func currencyToDeadline() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        FactoryWriteTranslateFound.withFraction(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().moreThen(urlStr: dict?[(userHealthyUrl.replacingOccurrences(of: "subject", with: "r"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(app_airId.replacingOccurrences(of: "profile", with: "lo") + "ckGift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ScreenModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: appNativeStr.reversed(), encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(kTroubleTitle.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.afterStroke(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EmbraceShadow! {
    func message(_ controller: CounteractionTableViewController!, rounding data: V2TIMMessage!) -> EmbraceShadow! {
        //: let dic = CompareOddSecond.parseTXMessageData(data: data.customElem.data)
        let dic = CompareOddSecond.activity(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = AgainstName.need(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: BasicCollection.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(mainHomeName))].stringValue == (String(showWarningSpreadStr.prefix(6)) + "lCus" + const_dateExImpactStr.replacingOccurrences(of: "control", with: "o")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(show_tailIdentifyId.lowercased() + String(constAgileUrl.prefix(9)))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(constHidePath.replacingOccurrences(of: "picture", with: "tr"))][(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))][(String(constImplementationSessionString.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = VariableChain(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = VariableChain(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: SustainableCenter.self) == true ||
        if cellData?.isKind(of: SustainableCenter.self) == true ||
            //: cellData?.isKind(of: WantText.self) == true ||
            cellData?.isKind(of: WantText.self) == true ||
            //: cellData?.isKind(of: VariableChain.self) == true ||
            cellData?.isKind(of: VariableChain.self) == true ||
            //: cellData?.isKind(of: RoundingAsset.self) == true {
            cellData?.isKind(of: RoundingAsset.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            FactoryWriteTranslateFound.substantial(cellData: cellData, msgView: self.msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            agent()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EmbraceShadow!) -> ImplementProperty! {
    func reload(_ controller: CounteractionTableViewController!, embraceDown data: EmbraceShadow!) -> ImplementProperty! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = instead(data: data)

        //: if data.isKind(of: VariableChain.self) {
        if data.isKind(of: VariableChain.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = AdjustBecomePaint(style: .default, reuseIdentifier: AdjustBecomePaint.className())
            //: let cellData = data as! VariableChain
            let cellData = data as! VariableChain

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.yield() {
                //: if cellData.msgModel.quoteMsgInfo.uid == ChartShowSceneScientific.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == ChartShowSceneScientific.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = ChartShowSceneScientific.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = ChartShowSceneScientific.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: WantText.self) {
        if data.isKind(of: WantText.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ExecuteSystemVertical(style: .default, reuseIdentifier: ExecuteSystemVertical.className())
            //: let cellData = data as! WantText
            let cellData = data as! WantText
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: RoundingAsset.self) {
        if data.isKind(of: RoundingAsset.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = ValueGroup(style: .default, reuseIdentifier: ValueGroup.className())
            //: let cellData = data as! RoundingAsset
            let cellData = data as! RoundingAsset
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: FeatureOccasion.self) {
        if data.isKind(of: FeatureOccasion.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = BlockLocal(style: .default, reuseIdentifier: BlockLocal.className())
            //: let cellData = data as! FeatureOccasion
            let cellData = data as! FeatureOccasion
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: VisitPrepare.self) {
        if data.isKind(of: VisitPrepare.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ToleranceImpact(style: .default, reuseIdentifier: ToleranceImpact.className())
            //: let cellData = data as! VisitPrepare
            let cellData = data as! VisitPrepare
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = TierBrush()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.factoryAssociate(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(kCartValue)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: SustainableCenter.self) {
        if data.isKind(of: SustainableCenter.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = CanvasSubstantialRoman(style: .default, reuseIdentifier: CanvasSubstantialRoman.className())
            //: let cellData = data as! SustainableCenter
            let cellData = data as! SustainableCenter
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: BasicCollection.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = AspectContain(style: .default, reuseIdentifier: AspectContain.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! BasicCollection
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: RegulateThan.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = TemporaryView(style: .default, reuseIdentifier: TemporaryView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! RegulateThan
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: CapacityDelivery.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = IndependentSkirt(style: .default, reuseIdentifier: IndependentSkirt.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! CapacityDelivery
            //: cell.fill(with: cellData)
            cell.grain(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ImplementProperty!) {
    func pic(_ controller: CounteractionTableViewController!, immediateDrawing cell: ImplementProperty!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != CharacteristicSine.reflectOff() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: BeforeAssociate.share.func__pushToUserDetailVC(uid: String(uid))
            BeforeAssociate.share.outsideTechnique(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ImplementProperty!) {
    func over(_ controller: CounteractionTableViewController!, tagUponDataOf cell: ImplementProperty!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ExecuteSystemVertical.self) { // 图片
            //: let imageCellData = cell.messageData as! WantText
            let imageCellData = cell.messageData as! WantText
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = NormLevel(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.user(imageCellData, insert: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.tip(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: show_successData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.someDeal(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.root(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                teeHoney()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(user_availableValue)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = SendSmall()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = FinishEdge()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [SendSmall] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = DistributionNorm(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                teeHoney()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ValueGroup.self) { // 视频
            //: let videoCellData = cell.messageData as! RoundingAsset
            let videoCellData = cell.messageData as! RoundingAsset
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = AroundSumegrity(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                teeHoney()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = BoundAdvanced(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.visit(videoCellData, history: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.tip(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: show_successData)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.someDeal(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.root(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                teeHoney()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: CanvasSubstantialRoman.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! CanvasSubstantialRoman
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! SustainableCenter, cellView: audioCell)
            greet(cellData: audioCell.cellData as! SustainableCenter, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: AdjustBecomePaint.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! AdjustBecomePaint
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = CompareOddSecond.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = CompareOddSecond.activity(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(constHidePath.replacingOccurrences(of: "picture", with: "tr"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(main_popularData.suffix(6)) + kDetailDeleteName.replacingOccurrences(of: "distance", with: "y"))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(main_popularData.suffix(6)) + kDetailDeleteName.replacingOccurrences(of: "distance", with: "y"))] as? String ?? ""

                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AlwaysFit>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BeforeAssociate.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BeforeAssociate.share.nearEconomical(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(mainHomeName))] as? String == (String(showWarningSpreadStr.prefix(6)) + "lCus" + const_dateExImpactStr.replacingOccurrences(of: "control", with: "o")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))][(String(user_imageKey.suffix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    associatedOff()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    text()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TemporaryView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TemporaryView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = CompareOddSecond.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = CompareOddSecond.activity(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(constHidePath.replacingOccurrences(of: "picture", with: "tr"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(main_popularData.suffix(6)) + kDetailDeleteName.replacingOccurrences(of: "distance", with: "y"))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AlwaysFit>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BeforeAssociate.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BeforeAssociate.share.nearEconomical(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: AspectContain.self) {
            //: BeforeAssociate.share.func__pushToUserDetailVC(uid: msgView.targetID)
            BeforeAssociate.share.outsideTechnique(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ImplementProperty!) {
    func lastSkirt(_ controller: CounteractionTableViewController!, information cell: ImplementProperty!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: ReductionProfile?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ExecuteSystemVertical.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ExecuteSystemVertical
            //: cellData = imageCell.cellData as! WantText
            cellData = imageCell.cellData as! WantText
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ValueGroup.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ValueGroup
            //: cellData = videoCell.cellData as! RoundingAsset
            cellData = videoCell.cellData as! RoundingAsset
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: CanvasSubstantialRoman.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! CanvasSubstantialRoman
            //: cellData = audioCell.cellData as! SustainableCenter
            cellData = audioCell.cellData as! SustainableCenter
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: AdjustBecomePaint.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! AdjustBecomePaint
            //: cellData = textCell.cellData as! VariableChain
            cellData = textCell.cellData as! VariableChain
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: EmbraceShadow? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: EmbraceShadow? = AgainstName.distance(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: RoundingAsset.self) {
                if reCellData!.isKind(of: RoundingAsset.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    selected(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    scenario(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: EmbraceShadow!) {
    func roman(_ controller: CounteractionTableViewController!, countegration cellData: EmbraceShadow!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = FactoryWriteTranslateFound.numbero(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: EmbraceShadow!) {
    func attach(_ controller: CounteractionTableViewController!, pro cellData: EmbraceShadow!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        FactoryWriteTranslateFound.album(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: EmbraceShadow) -> EmbraceShadow {
    private func instead(data: EmbraceShadow) -> EmbraceShadow {
        //: let isSelf = data.msgModel.user.uid == Int(ChartShowSceneScientific.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(ChartShowSceneScientific.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if ChartShowSceneScientific.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if ChartShowSceneScientific.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = ChartShowSceneScientific.share.loginUserMode.headPic!
                data.msgModel.user.headPic = ChartShowSceneScientific.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = ChartShowSceneScientific.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = ChartShowSceneScientific.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func ondUntil(_ model: EditModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: kFlowKey.reversed(), encoding: .utf8)!: model.content, String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (notiFactorValue.lowercased())]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: userFilterSystemResistanceStr.map{adjustAverage(entity: $0)}, encoding: .utf8)!: model.content, String(bytes: mainRegionMechanismMsg.map{routeRespective(rank: $0)}, encoding: .utf8)!: model.replayId, (String(showGiveUrl)): (String(kDetectStr.suffix(6)))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.scenario(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func reflectFor() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (notiFactorValue.lowercased())]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(String(showGiveUrl)): (String(k_pressureRobotPath.prefix(5)) + String(kStickContentureData))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.scenario(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func towardBlack() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (notiFactorValue.lowercased())]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(showGiveUrl)): (String(const_productionFormat))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.scenario(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func beyondFor(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: kFlowKey.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: kFlowKey.reversed(), encoding: .utf8)!: content, (String(app_unlikelyString)): greetId, String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (notiFactorValue.lowercased())]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: userFilterSystemResistanceStr.map{adjustAverage(entity: $0)}, encoding: .utf8)!: content, (String(app_unlikelyString)): greetId, (String(showGiveUrl)): (String(kDetectStr.suffix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.scenario(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            MovementMulti.shared.structureColor(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.bury(showMsg: String(bytes: constTooData.map{ariaNote(receive: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        FactoryWriteTranslateFound.outAppInfo(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(k_ageTabValue))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (user_marginPath.replacingOccurrences(of: "click", with: "i")),
                               //: "audioId": greetId,
                               (app_natureMsg.lowercased() + String(main_tierIndicationKey.suffix(2))): greetId,
                               //: "audioLength": length,
                               String(bytes: noti_increasinglyId.map{elementRef(way: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x")): model.unZipFilePath,
                               //: "senderId": ChartShowSceneScientific.share.loginUserMode.userID]
                               String(bytes: kSuccessTemporaryId.map{$0^14}, encoding: .utf8)!: ChartShowSceneScientific.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(app_natureMsg.lowercased() + String(main_tierIndicationKey.suffix(2))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: noti_increasinglyId.map{elementRef(way: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(showGiveUrl)): (String(userDisplayPath.prefix(6)) + show_coalitionHaveId.replacingOccurrences(of: "broad", with: "s"))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.scenario(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func bit(msgStr: String, quoteModel: FrameworkTimePlainModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            bury(showMsg: (String(noti_smileName) + String(notiMemberValue) + String(noti_coordinateMsg)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: kFlowKey.reversed(), encoding: .utf8)!: temStr, String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (notiFactorValue.lowercased())]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: userFilterSystemResistanceStr.map{adjustAverage(entity: $0)}, encoding: .utf8)!: temStr, (String(showGiveUrl)): (String(kDetectStr.suffix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(notiChallengeTitle))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(userAirId.prefix(6)) + "sgInfo")] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? FrameworkTimePlainModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            scenario(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func afterStroke(img: UIImage, sendModel: ScreenModel) {
        //: let imagePath = WantText.getChatImagePath(image: img)
        let imagePath = WantText.consumption(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: const_freshGalleryName.map{musicGrade(critical: $0)}, encoding: .utf8)!: (String(appYieldKey.prefix(6))), String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (String(const_zoneData)), String(bytes: user_delicateFormat.map{ownerSatisfy(recover: $0)}, encoding: .utf8)!: imagePath, String(bytes: const_pinValue.map{forceList(discount: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(appSwitchlyCurrentStickStr.replacingOccurrences(of: "phase", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(showGiveUrl)): (String(appYieldKey.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: appNativeStr.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(kTroubleTitle.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            scenario(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: EmbraceShadow?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func selected(msgData: EmbraceShadow?, sendModel: ScreenModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: const_freshGalleryName.map{musicGrade(critical: $0)}, encoding: .utf8)!: (String(appLanguageSinceTitle.suffix(5)) + String(mainMicRecordString)), String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (String(const_hardFreshUrl)), String(bytes: const_pinValue.map{forceList(discount: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(showGiveUrl)): (String(appLanguageSinceTitle.suffix(5)) + String(mainMicRecordString)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: showApproveMessage.map{elbowMedium(writing: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: k_continuingName.map{sineFlex(mutt: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: app_activityCellValue.map{originSumo(toward: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: appNativeStr.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(kTroubleTitle.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = AgainstName.policyPan(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.searched(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.stroke(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.rebuildFirst(cellData, successUniform: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.stroke(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.loopTotalegrity(dict, away: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        scenario(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func image(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (user_marginPath.replacingOccurrences(of: "click", with: "i")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: noti_increasinglyId.map{elementRef(way: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x")): voicePath,
                       //: "senderId": ChartShowSceneScientific.share.loginUserMode.userID]
                       String(bytes: kSuccessTemporaryId.map{$0^14}, encoding: .utf8)!: ChartShowSceneScientific.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (String(showGiveUrl)): (String(userDisplayPath.prefix(6)) + show_coalitionHaveId.replacingOccurrences(of: "broad", with: "s")),
                         //: "len": "\(voiceTime)"]
                         String(bytes: mainPerceptPath.map{prosecutionAccept(ermine: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            scenario(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EmbraceShadow, reSend: Bool) {
    private func scenario(msgData: EmbraceShadow, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bury(showMsg: show_plainHalfName)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(constImplementationSessionString.prefix(5)))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: RoundingAsset.self) == false && msgData.isKind(of: VisitPrepare.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: RoundingAsset.self) == false, msgData.isKind(of: VisitPrepare.self) == false, params[(String(showGiveUrl))] as? String != (String(const_productionFormat)), params[(String(showGiveUrl))] as? String != (String(k_pressureRobotPath.prefix(5)) + String(kStickContentureData)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.searched(msgData)
        }
//        let recordTime = CompareOddSecond.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((appSwitchlyCurrentStickStr.replacingOccurrences(of: "phase", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(appSwitchlyCurrentStickStr.replacingOccurrences(of: "phase", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(appSwitchlyCurrentStickStr.replacingOccurrences(of: "phase", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as? String
        let type = params[(String(showGiveUrl))] as? String
        //: if type == "AudioMsg" { // 语音
        if type == (String(userDisplayPath.prefix(6)) + show_coalitionHaveId.replacingOccurrences(of: "broad", with: "s")) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(main_sufficientData.suffix(6)) + k_inputKey.replacingOccurrences(of: "tau", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(notiApartmentPromptId) + String(user_arcPauseName.suffix(7)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = mainShowMsg + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(appFinishKey) + String(user_minUrl.suffix(6)) + "/record/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = dataSquareRandomValue + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(app_natureMsg.lowercased() + String(main_tierIndicationKey.suffix(2)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(user_marginPath.replacingOccurrences(of: "click", with: "i"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        NameProjection.searchM(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(const_productionFormat)) || type == (String(k_pressureRobotPath.prefix(5)) + String(kStickContentureData)) {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.analyze(msgData, field: nil, action: NSNumber(value: 0), responsePresent: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.tip(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kReplaceData)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(app_unlikelyString))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    FactoryWriteTranslateFound.outAppInfo(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.constantEnvironment(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((const_selectionKey.replacingOccurrences(of: "first", with: "s") + String(kHeapCeilingName.prefix(6)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(const_selectionKey.replacingOccurrences(of: "first", with: "s") + String(kHeapCeilingName.prefix(6)))] as! NSNumber
            }

            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.say(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(app_sufficientString.replacingOccurrences(of: "lightly", with: "s") + String(userAppealTimeStr))] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(const_productionFormat)) || type == (String(k_pressureRobotPath.prefix(5)) + String(kStickContentureData)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!] = (String(showManagerUrl.suffix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    data_arrayMsg.automat(eventID: app_oldFormat)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(const_productionFormat)) || type == (String(k_pressureRobotPath.prefix(5)) + String(kStickContentureData)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!] = (notiFactorValue.lowercased())
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.searched(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue &&
                if ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(const_productionFormat)),
                   //: type != "MultiQuickMsg" {
                   type != (String(k_pressureRobotPath.prefix(5)) + String(kStickContentureData))
                {
                    //: let key =  "\(ChartShowSceneScientific.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)" + (String(show_hereMsg.prefix(5)) + String(k_bellyOverwhelmId.prefix(6)) + String(noti_writeId)) + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.musicDown()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.analyze(updateMsgData, field: msgInfo[(String(main_pointShapeData))] as? String, action: msgIncome, responsePresent: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((user_absoluteString.replacingOccurrences(of: "assist", with: "t") + String(noti_pionValue.suffix(6)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(user_absoluteString.replacingOccurrences(of: "assist", with: "t") + String(noti_pionValue.suffix(6)))] as! NSNumber
                //: ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((notiThreeString.replacingOccurrences(of: "same", with: "tt") + String(userMotiveValue.prefix(6)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(notiThreeString.replacingOccurrences(of: "same", with: "tt") + String(userMotiveValue.prefix(6)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.be(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((data_wordUrl.replacingOccurrences(of: "jump", with: "i") + "mateP" + String(userApprovePath))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(data_wordUrl.replacingOccurrences(of: "jump", with: "i") + "mateP" + String(userApprovePath))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? CurrencyModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.user(updateMsgData, insert: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(const_hardFreshUrl))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(const_hardFreshUrl))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? KitEaseModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.visit(updateMsgData, history: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        bottomStop()

        //: updateSession()
        thresholdOnAwake()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func thresholdOnAwake() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        TranslateScreen.now(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func tip(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard ChartShowSceneScientific.share.loginUserMode.status != 1 else {
            guard ChartShowSceneScientific.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    bury(showMsg: errorStr)
                }
                //: return
                return
            }

            //: BeforeAssociate.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            BeforeAssociate.share.computeEstimated(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.analyze()
            //: ChartShowSceneScientific.share.loginUserMode.freeMsgTimes = ""
            ChartShowSceneScientific.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.groupGold()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = PriorityView(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
            //: view.show()
            view.beforeEqually()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                bury(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func root(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.say(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func painter() {
        //: if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.result(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (show_modelHideFormat.array(forKey: const_amongString)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.slideToo()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                show_modelHideFormat.set(intimateCacheData, forKey: const_amongString)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func connection(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = CompareOddSecond.parseTXMessageData(data: data.customElem.data)
        let dic = CompareOddSecond.activity(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(constHidePath.replacingOccurrences(of: "picture", with: "tr"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(userOriginalId) + main_yesterdayWriteMessage.replacingOccurrences(of: "why", with: "ti"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.criticalJob(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func say(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(userOriginalId) + main_yesterdayWriteMessage.replacingOccurrences(of: "why", with: "ti"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(userOriginalId) + main_yesterdayWriteMessage.replacingOccurrences(of: "why", with: "ti"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.criticalJob(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func criticalJob(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = TranslateScreen.drownBlindAnonymousEnhanceSetProvide(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.result(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.okPlus(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.charm(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.charm(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        TranslateScreen.overMeasure(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: const_arrowUrl.map{mixYear(impact: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: notiFutureUrl.map{profileGo(tick: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: constObserverName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension RunViewController: EaseSmart {
    //: func func_inputContentIntervalOneSecond() {
    func too() {}

    //: func func__selectMentionUser(range: NSRange) {}
    func icon(range: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func ready(actionType: SymbolProcessor) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    checkShadow()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    minimize()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                checkShadow()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if ChartShowSceneScientific.share.loginUserMode.loungePlus {
            if ChartShowSceneScientific.share.loginUserMode.loungePlus { // 男性会员
                //: videoStr = self.userWrap.videoVIPPrice
                videoStr = self.userWrap.videoVIPPrice
                //: voiceStr = self.userWrap.voiceVIPPrice
                voiceStr = self.userWrap.voiceVIPPrice
                //: } else {
            } else {
                //: videoStr = self.userWrap.videoPrice
                videoStr = self.userWrap.videoPrice
                //: voiceStr = self.userWrap.voicePrice
                voiceStr = self.userWrap.voicePrice
            }
            //: if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr)
                videoCallManager.wrapWeek(videoPrice: videoStr, voicePrice: voiceStr)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                minimize()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    moment()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    minimize()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                moment()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.decideOf(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func minimize() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.bury(showMsg: String(bytes: dataDenseCanTrustKey.reversed(), encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func pressure(giftModel: TierBrush, giftNum: String, model: RomanMagnitudeerval) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func each(giftModel: TierBrush, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        factoryAssociate(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: VisitPrepare?, fromStr: String?) {
    func factoryAssociate(giftModel: TierBrush, giftNum: String, cellData: VisitPrepare?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bury(showMsg: show_plainHalfName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(constImplementationSessionString.prefix(5)))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(kTroubleTitle.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(constBlindMsg.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(show_increaseData.suffix(7)) + String(mainYesTactTitle))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(showSaveMsg.replacingOccurrences(of: "delay", with: "f"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        NameProjection.notEdit(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.tip(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: noti_evenProviderActionStr)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.cropManual(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.someDeal(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.thresholdOnAwake()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(userOriginalId) + main_yesterdayWriteMessage.replacingOccurrences(of: "why", with: "ti"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(userOriginalId) + main_yesterdayWriteMessage.replacingOccurrences(of: "why", with: "ti"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.criticalJob(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.outTask(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                data_arrayMsg.automat(eventID: kComputeStr)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func pair(giftModel: TierBrush, giftNum: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        data_arrayMsg.automat(eventID: showObtainAngleKey)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bury(showMsg: show_plainHalfName)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= ChartShowSceneScientific.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            minimize()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(noti_fractionMessage.prefix(9)) + String(show_screenRepublicData))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(showGiveUrl))] = (String(dataRequestFailNumberFormat))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (String(showManagerUrl.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle)): msgInfo, String(bytes: dataVideoWayName.map{maximumDomain(upper: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AgainstName.policyPan(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            scenario(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func trigger(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(mainBlockPath - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.sample(true)
            self.msgView.sample(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func boxAlong(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.sample(true)
            self.msgView.sample(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func coverMomentum(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.coverMomentum(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                numbereraction(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func thinRe() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if SkipEnableWait.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SkipEnableWait.shared.quit()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func someDeal(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!] = (main_helloFormat.replacingOccurrences(of: "precis", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(kChartStr.replacingOccurrences(of: "comment", with: "m") + String(appSimultaneouslyTitle))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = AgainstName.policyPan(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.searched(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((const_selectionKey.replacingOccurrences(of: "first", with: "s") + String(kHeapCeilingName.prefix(6)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(const_selectionKey.replacingOccurrences(of: "first", with: "s") + String(kHeapCeilingName.prefix(6)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.analyze(cellData, field: msgInfoDic[(String(main_pointShapeData))] as? String, action: msgIncome, responsePresent: .Msg_Status_Succ)

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        aboveUp(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((user_absoluteString.replacingOccurrences(of: "assist", with: "t") + String(noti_pionValue.suffix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(user_absoluteString.replacingOccurrences(of: "assist", with: "t") + String(noti_pionValue.suffix(6)))] as! NSNumber
            //: ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            ChartShowSceneScientific.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: ChartShowSceneScientific.share.loginUserMode.mf_coin)
            msgInputView.giftView.reliability(needReload: false, mf_coin: ChartShowSceneScientific.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func transfer() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.blackTo(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func aboveUp(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.appear(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func white(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.gestureComprehensive(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func beforeReplace() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func associatedOff() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.flexibleWithGreat { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.thresholdOnAwake()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func text() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.nameStatus { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.thresholdOnAwake()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension RunViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func succeedAcrossRefuse() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = AccessibleVisit()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        teeHoney()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func account() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = TailView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.playAltogether(cellTitleList: [(String(show_certainGrabMsg) + notiRadioTitle.replacingOccurrences(of: "add", with: "in")).localized, (String(mainDownTitle)).localized, (String(user_perMoveUrl) + constFunImplyName.replacingOccurrences(of: "am", with: "po")).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .sharkHighlightMaintain()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: BeforeAssociate.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                BeforeAssociate.share.outsideTechnique(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                entityVolume()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = EagerView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.obtainButton(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func entityVolume() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = BoundView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.club(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.adjust(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func crop() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            numbereraction(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            teeHoney()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func typical(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = PropertyModel.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.recordPosition(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func composeFitMaxMemoryIndicator() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(found(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: k_robotString,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(flex),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: noti_scaleGuideValue,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func found(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard ChartShowSceneScientific.share.loginUserMode.loungePlus == true else { return }
        guard ChartShowSceneScientific.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func flex() {
        //: ChartShowSceneScientific.share.loginUserMode.loungePlus = true
        ChartShowSceneScientific.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        light()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func bounceDoing() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? EmbraceShadow else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? EmbraceShadow else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if FactoryWriteTranslateFound.conversation(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.display(tipStr: (String(dataReadingKey.prefix(4)) + " read " + String(noti_orientationKey.prefix(9))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension RunViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func checkShadow() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ContainPlate.send(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = AccessiblePop.descriptionBy(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.awayWrap(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func promise() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ContainPlate.sharkThen(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.awayWrap(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func awayWrap(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else {
            ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ScreenModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.afterStroke(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = DistributionNorm(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ScreenModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.afterStroke(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        teeHoney()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func moment() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard FactoryWriteTranslateFound.dayLocal(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == SharedOdd.male.rawValue ? "He".localized : (String(constDetailedFormat.prefix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.bury(showMsg: (String(data_captureDonUrl) + String(const_speedMiniId.prefix(5)) + "oo lo" + String(notiWrapTitle.suffix(6)) + "rsion").skipLive(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.similarClear()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func similarClear() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = AccessiblePop.descriptionBy(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (coverImage: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            AccessiblePop.roundAnswer(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.finish(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.bury(showMsg: (String(noti_denyMutualUrl.suffix(5)) + " vide" + String(constUsPath.suffix(4)) + " least" + String(kPublisherLightKey)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func finish(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: MaintainTheme = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = HandleViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ScreenModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.selected(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        GrayView.at()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        teeHoney()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: EmbraceShadow, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func stroke(msgData: EmbraceShadow, sendModel: ScreenModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        RateFactory.shared.sinceMomentum(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.bury(showMsg: (String(userSignId.suffix(5)) + "d failed").localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.analyze(msgData, field: nil, action: NSNumber(value: 0), responsePresent: .Msg_Status_Fail)
                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.selected(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension RunViewController {
    //: func func__addVoiceNotification() {
    func padDown() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(strengthMend),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(closeer),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(device(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(pureAcrossBroadcast(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: main_delayName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(connection(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: showMomentumName,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func similar(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            SkipEnableWait.shared.kindChannel()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            SkipEnableWait.shared.remarkBullet()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func strengthMend() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        similar(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func closeer() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        similar(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func device(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            similar(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            similar(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: SustainableCenter, cellView: TalkingChatAudioMsgCell) {
    func greet(cellData: SustainableCenter, cellView: CanvasSubstantialRoman) {
        //: guard TalkingSocketManager.shared.byWork == false else {
        guard ToPresent.shared.byWork == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bury(showMsg: noti_scientificCurrencyValue)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? EmbraceShadow)!.isKind(of: SustainableCenter.self) == false {
            if (indexModel as? EmbraceShadow)!.isKind(of: SustainableCenter.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! SustainableCenter
            let audioModel = indexModel as! SustainableCenter
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = TranslateScreen.alwaysA(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        SkipEnableWait.shared.quit()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if SkipEnableWait.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (app_implementChapterStr.capitalized) + "\(SkipEnableWait.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if SkipEnableWait.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                SkipEnableWait.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        SkipEnableWait.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        SkipEnableWait.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: EmbraceShadow = self.msgView.uiMsgs.object(at: index) as! EmbraceShadow
                let subModel: EmbraceShadow = self.msgView.uiMsgs.object(at: index) as! EmbraceShadow
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: SustainableCenter.self) {
                if subModel.isKind(of: SustainableCenter.self) {
                    //: let audioModel = subModel as! SustainableCenter
                    let audioModel = subModel as! SustainableCenter
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = TranslateScreen.alwaysA(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    MoveBar.matchYellowBottom(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        SkipEnableWait.shared.train(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func pastDiscount() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.chapterTween()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.beyondFor(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? EmbraceShadow
                let indexModel = model as? EmbraceShadow
                //: if indexModel?.isKind(of: SustainableCenter.self) == true || indexModel?.isKind(of: WantText.self) == true || indexModel?.isKind(of: VariableChain.self) == true || indexModel?.isKind(of: RoundingAsset.self) == true {
                if indexModel?.isKind(of: SustainableCenter.self) == true || indexModel?.isKind(of: WantText.self) == true || indexModel?.isKind(of: VariableChain.self) == true || indexModel?.isKind(of: RoundingAsset.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.bottomStop()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.feedUpon()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.bounceDoing()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func chapterTween() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.fireIn(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.toExpected(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.painter()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == ChartShowSceneScientific.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == ChartShowSceneScientific.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == ChartShowSceneScientific.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == ChartShowSceneScientific.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.adjust(userWrap)
    }

    //: func func__getFillDraftText() {
    func ignoreDown() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(noti_documentChartValue)) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                }
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }

    //: func func__saveFillDraftText() {
    func backSpring() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(noti_documentChartValue)) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension RunViewController {
    //: func func__TipsNotification() {
    func suspendFore() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(alwaysDistribution(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: k_transitionMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func alwaysDistribution(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: RegulateThan = userinfo[(main_shotReflectData.replacingOccurrences(of: "calendar", with: "g"))] as! RegulateThan

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.sample(true)
        self.msgView.sample(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func display(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: kFlowKey.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: noti_alwaysString.map{sendDoing(around: $0)}, encoding: .utf8)!: (user_primaryUrl.replacingOccurrences(of: "dram", with: "s"))]
        //: var message: EmbraceShadow?
        var message: EmbraceShadow?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = AgainstName.easy(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = AgainstName.policyPan(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.sample(true)
        self.msgView.sample(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func feedUpon() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        FactoryWriteTranslateFound.job { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.display(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func be(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.display(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func bottomStop() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        FactoryWriteTranslateFound.begin(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.display(tipStr: String(bytes: user_romanValue.reversed(), encoding: .utf8)!.skipLive(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func pureAcrossBroadcast(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: EmbraceShadow = dict?["cellData"] as! EmbraceShadow
        let cellData: EmbraceShadow = dict?[(String(const_kingStr.prefix(8)))] as! EmbraceShadow
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.sample(true)
        self.msgView.sample(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension RunViewController: ResourceExecute {
    //: func func__setReplyTipsView() {
    func handleSustainable() {
        //: let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)" + (String(show_hereMsg.prefix(5)) + String(k_bellyOverwhelmId.prefix(6)) + String(noti_writeId)) + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.zone()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            no(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            musicDown()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func inside(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        bit(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        musicDown()
    }

    //: func removeReplyTipsView() {
    func musicDown() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            no(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func light() {
        //: guard ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.agent()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func agent() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            FactoryWriteTranslateFound.body(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.postAccept(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func noFilter() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: dataIconFormat, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: dataChainValue - mainBlockPath, width: show_licenseTitle, height: dataChainValue)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: show_licenseTitle,
                                     //: height: ScreenHeight)
                                     height: dataChainValue)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func numbereraction(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.chemist() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: AdvancedReduction.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: dataChainValue - mainBlockPath,
                                     //: width: ScreenWidth,
                                     width: show_licenseTitle,
                                     //: height: ScreenHeight)
                                     height: dataChainValue)
            //: } completion: { finish in
        } completion: { finish in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension RunViewController {
    /// 创建UI
    //: private func createUI() {
    private func kit() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (userBucketPath.replacingOccurrences(of: "universal", with: "F5")))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(dataConstructPath.suffix(6)) + String(k_keepStr)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func columnAppropriate() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? mainBlockPath : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.oi(width: contentView.width,
                           //: height: contentView.height,
                           height: contentView.height,
                           //: corners: [.topLeft, .topRight],
                           corners: [.topLeft, .topRight],
                           //: cornerRadii: .init(width: 8, height: 8))
                           cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? appPurpleVersusMessage : dataSuccessResponseMessage)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? appPurpleVersusMessage : dataSuccessResponseMessage)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + const_controlData)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func fit() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.crop()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.account()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.ready(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                data_arrayMsg.automat(eventID: show_marginUrl, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
