
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_professionalKey:[UInt8] = [0x39,0x3e,0x39,0x24,0x78,0x33,0x3f,0x34,0x35,0x22,0x6a,0x79,0x70,0x38,0x31,0x23,0x70,0x3e,0x3f,0x24,0x70,0x32,0x35,0x35,0x3e,0x70,0x39,0x3d,0x20,0x3c,0x35,0x3d,0x35,0x3e,0x24,0x35,0x34]

private func propertyInstead(secure num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "Cancel" :*/
fileprivate let app_upperCalculateFormat:String = "c"
fileprivate let data_thumbTitle:String = "anmultiel"

/*: "Submit" :*/
fileprivate let dataBounceString:[Character] = ["S","u","b","m","i"]
fileprivate let constBeforeData:String = "frame"

/*: "Please tell us your problems                   " :*/
fileprivate let notiRegionId:[UInt8] = [0x55,0x69,0x60,0x64,0x76,0x60,0x25,0x71,0x60,0x69,0x69,0x25,0x70,0x76,0x25,0x7c,0x6a,0x70,0x77,0x25,0x75,0x77,0x6a,0x67,0x69,0x60,0x68,0x76,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25,0x25]

private func halfPainter(tun num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "You need to choose at least one." :*/
fileprivate let const_elbowMessage:[UInt8] = [0xef,0xd9,0xc3,0x96,0xd8,0xd3,0xd3,0xd2,0x96,0xc2,0xd9,0x96,0xd5,0xde,0xd9,0xd9,0xc5,0xd3,0x96,0xd7,0xc2,0x96,0xda,0xd3,0xd7,0xc5,0xc2,0x96,0xd9,0xd8,0xd3,0x98]

private func chemistWarnAssert(on num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "Please tell us your problems." :*/
fileprivate let app_stripMessage:[Character] = ["P","l","e","a","s","e"," ","t","e","l","l"," ","u","s"," ","y"]
fileprivate let constBotSpineId:[Character] = ["o","u"]
fileprivate let notiEnhanceWarningTitle:String = "under unionr pro"

/*: "app/userReport" :*/
fileprivate let dataGlobalValue:String = "outer practical along keep picapp/"
fileprivate let main_wrapKey:String = "Reporthundred line away bucket"

/*: "reason" :*/
fileprivate let notiLocationData:String = "reashn"

/*: "desc" :*/
fileprivate let main_paraKey:[UInt8] = [0x63,0x73,0x65,0x64]

/*: "mf/moment/report" :*/
fileprivate let kProperlyData:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let app_preventionMarginKey:[UInt8] = [0xf3,0xf5,0xf3,0xeb,0xf4,0xfa,0xcf,0xea]

fileprivate func legacyPurple(manual num: UInt8) -> UInt8 {
    let value = Int(num) - 134
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/momentReply/report" :*/
fileprivate let app_concretePairStr:String = "mf/moprogram expression green heap"
fileprivate let mainFactorName:[Character] = ["e","p","l","y","/"]
fileprivate let constProtectionBirthdayMsg:String = "republishrt"

/*: "app/videoReport" :*/
fileprivate let notiTechniqueString:String = "app/viheat explain framework"
fileprivate let noti_broadMsg:String = "minimizeort"

/*: "logId" :*/
fileprivate let main_canvasStepMsg:[UInt8] = [0x1b,0x18,0x10,0x3e,0x13]

private func rowEmerge(now num: UInt8) -> UInt8 {
    return num ^ 119
}

/*: "Submitted successfully" :*/
fileprivate let appRawString:String = "Submnear watch alive sample missing"
fileprivate let notiThinExistKey:String = "application plat social statue consumer succe"
fileprivate let user_impactUrl:[Character] = ["l","l","y"]

/*: "Nudity" :*/
fileprivate let noti_dateId:[Character] = ["N","u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let mainFrequencyValue:String = "Violmode grass fire memo"

/*: "Harassment" :*/
fileprivate let showTransitionName:String = "Harassmmission hour"
fileprivate let mainAskMsg:[Character] = ["e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let kEmptyFormat:String = "temporary minimum tapSuici"
fileprivate let mainSpitCharmFeeFormat:String = " Self-difficult border"
fileprivate let const_tingUrl:String = "immediate"

/*: "False Information" :*/
fileprivate let user_altogetherFormat:String = "False path consumption man phase locate"
fileprivate let const_nomineeResignData:String = "leadingon"

/*: "Spam" :*/
fileprivate let app_reduceYellowData:String = "withdraw remove icon dynamicSpam"

/*: "Hate Speech" :*/
fileprivate let data_withCropName:String = "Hate title entity broadcast implementation"
fileprivate let app_documentTitle:String = "subtle"

/*: "Terrorism" :*/
fileprivate let notiProsecutionUrl:String = "quit dimensionTer"
fileprivate let data_originTitle:[Character] = ["r","o","r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let k_actualData:[UInt8] = [0x73,0x64,0x6f,0x6f,0x47,0x20,0x64,0x65,0x74,0x61,0x6c,0x75,0x67,0x65,0x52,0x20,0x72,0x6f,0x20,0x6c,0x61,0x67,0x65,0x6c,0x6c,0x49,0x20,0x66,0x6f,0x20,0x65,0x6c,0x61,0x53]

/*: "Sending disgusting messages" :*/
fileprivate let show_obtainIndexKey:[Character] = ["S","e","n","d","i","n","g"]
fileprivate let notiProductUrl:[Character] = [" ","d","i","s","g"]
fileprivate let user_fixedFormat:String = "exclusivetin"
fileprivate let userFenderData:String = "insideageinside"

/*: "Using photos of nudity or drugs" :*/
fileprivate let mainArchitectureData:[UInt8] = [0x92,0xb4,0xae,0xa9,0xa0,0xe7,0xb7,0xaf,0xa8,0xb3,0xa8,0xb4,0xe7,0xa8,0xa1,0xe7,0xa9,0xb2,0xa3,0xae,0xb3,0xbe,0xe7,0xa8,0xb5,0xe7,0xa3,0xb5,0xb2,0xa0,0xb4]

private func distanceCapture(mac num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "Harassing or sending ads" :*/
fileprivate let noti_hmmMsg:String = "root abs outside topicHarassi"
fileprivate let data_randomMessage:[Character] = ["n","g"]
fileprivate let user_skirtCorruptionString:String = " or semask pair reader publisher"

/*: "Defraud or using fake info" :*/
fileprivate let show_greenNetworkPath:String = "row window appearanceDefrau"
fileprivate let showHmmKey:String = "using fexplicit locate no specify"
fileprivate let main_stickBackgroundValue:String = "ake rating model"

/*: "Others" :*/
fileprivate let showRawString:String = "Otherssecure guide off shape"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EagerView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum SizeFitSumerval: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class EagerView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: SizeFitSumerval?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [AverageOn] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = AverageOn()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: SizeFitSumerval?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        doBean()
        //: setupSubViewsConstraint()
        switchicialCompute()
        //: setupReasons()
        corner()
        //: bindInteraction()
        pastRankAttach()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_professionalKey.map{propertyInstead(secure: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((app_upperCalculateFormat.uppercased() + data_thumbTitle.replacingOccurrences(of: "multi", with: "c")).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.special(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.bagUniform(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.special().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(fail), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(dataBounceString) + constBeforeData.replacingOccurrences(of: "frame", with: "t")).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.bagUniform(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.managerMouse(colors: UIColor.raceGold(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(programUniversal), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: show_licenseTitle - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.doingShelter(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = OutsideSmart(placeholder: String(bytes: notiRegionId.map{halfPainter(tun: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.beforePainter(), frame: CGRect(x: 10, y: 10, width: show_licenseTitle - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .bagUniform(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.sharkHighlightMaintain()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .bagUniform(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.doingShelter(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(PanExercise.self, forCellReuseIdentifier: PanExercise.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension EagerView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func table() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func obtainButton(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = MacroDefine.getWindow()
            let window = CharacteristicSine.asCap()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func hi(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func fail() {
        //: self.dismissReportView(succeed: false)
        self.hi(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func programUniversal() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.bury(showMsg: String(bytes: const_elbowMessage.map{chemistWarnAssert(on: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.bury(showMsg: (String(app_stripMessage) + String(constBotSpineId) + String(notiEnhanceWarningTitle.suffix(5)) + "blems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(dataGlobalValue.suffix(4)) + "user" + String(main_wrapKey.prefix(6)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (notiLocationData.replacingOccurrences(of: "sh", with: "so")): selectedModel.type, String(bytes: main_paraKey.reversed(), encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(kProperlyData))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: app_preventionMarginKey.map{legacyPurple(manual: $0)}, encoding: .utf8)!: tempId, (notiLocationData.replacingOccurrences(of: "sh", with: "so")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(app_concretePairStr.prefix(5)) + "mentR" + String(mainFactorName) + constProtectionBirthdayMsg.replacingOccurrences(of: "publish", with: "po"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (notiLocationData.replacingOccurrences(of: "sh", with: "so")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(notiTechniqueString.prefix(6)) + "deoRe" + noti_broadMsg.replacingOccurrences(of: "minimize", with: "p"))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: main_canvasStepMsg.map{rowEmerge(now: $0)}, encoding: .utf8)!: tempId, (notiLocationData.replacingOccurrences(of: "sh", with: "so")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        AttachPut.target(reqPath: path, reqParams: param) { succeed, result, errorModel in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.allInsidePosition(showMsg: (String(appRawString.prefix(4)) + "itted" + String(notiThinExistKey.suffix(6)) + "ssfu" + String(user_impactUrl)).localized)
                //: self.dismissReportView(succeed: true)
                self.hi(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension EagerView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: PanExercise.className()) as! PanExercise
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.brown(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension EagerView {
    /// 添加视图
    //: private func setupSubviews() {
    private func doBean() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func switchicialCompute() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func corner() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(noti_dateId)).localized,
                           //: "Violence".localized,
                           (String(mainFrequencyValue.prefix(4)) + "ence").localized,
                           //: "Harassment".localized,
                           (String(showTransitionName.prefix(7)) + String(mainAskMsg)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(kEmptyFormat.suffix(5)) + "de or" + String(mainSpitCharmFeeFormat.prefix(6)) + "injur" + const_tingUrl.replacingOccurrences(of: "immediate", with: "y")).localized,
                           //: "False Information".localized,
                           (String(user_altogetherFormat.prefix(6)) + "Informa" + const_nomineeResignData.replacingOccurrences(of: "leading", with: "ti")).localized,
                           //: "Spam".localized,
                           (String(app_reduceYellowData.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(data_withCropName.prefix(5)) + "Spee" + app_documentTitle.replacingOccurrences(of: "subtle", with: "ch")).localized,
                           //: "Terrorism".localized,
                           (String(notiProsecutionUrl.suffix(3)) + String(data_originTitle)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: k_actualData.reversed(), encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(show_obtainIndexKey) + String(notiProductUrl) + user_fixedFormat.replacingOccurrences(of: "exclusive", with: "us") + "g mes" + userFenderData.replacingOccurrences(of: "inside", with: "s")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: mainArchitectureData.map{distanceCapture(mac: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(noti_hmmMsg.suffix(7)) + String(data_randomMessage) + String(user_skirtCorruptionString.prefix(6)) + "nding ads").localized,
                           //: "Defraud or using fake info".localized,
                           (String(show_greenNetworkPath.suffix(6)) + "d or " + String(showHmmKey.prefix(7)) + String(main_stickBackgroundValue.prefix(4)) + "info").localized,
                           //: "Others".localized]
                           (String(showRawString.prefix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = ChartShowSceneScientific.share.appConfigMode.videoReport["reason"] {
            if let reason = ChartShowSceneScientific.share.appConfigMode.videoReport[(notiLocationData.replacingOccurrences(of: "sh", with: "so"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = AverageOn()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func pastRankAttach() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] recognizer in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
