
//: Declare String Begin

/*: "CacheIntimatePhotoUnlockMsgIdArrayKey" :*/
fileprivate let constEvenPath:[UInt8] = [0x29,0xb,0x9,0x2,0xf,0x23,0x4,0x1e,0x3,0x7,0xb,0x1e,0xf,0x3a,0x2,0x5,0x1e,0x5,0x3f,0x4,0x6,0x5,0x9,0x1,0x27,0x19,0xd,0x23,0xe,0x2b,0x18,0x18,0xb,0x13,0x21,0xf,0x13]

private func addressVolume(step num: UInt8) -> UInt8 {
    return num ^ 106
}

/*: "CacheQuestionReplayMsgIdIdArrayKey" :*/
fileprivate let dataSecureEvaluateKey:[UInt8] = [0xc9,0xe7,0xe9,0xee,0xeb,0xd7,0xfb,0xeb,0xf9,0xfa,0xef,0xf5,0xf4,0xd8,0xeb,0xf6,0xf2,0xe7,0xff,0xd3,0xf9,0xed,0xcf,0xea,0xcf,0xea,0xc7,0xf8,0xf8,0xe7,0xff,0xd1,0xeb,0xff]

fileprivate func agentResult(container num: UInt8) -> UInt8 {
    let value = Int(num) + 122
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "CachePrivateChatVideoTipsIsShow_ :*/
fileprivate let notiFrequencyFormat:[UInt8] = [0x5f,0x77,0x6f,0x68,0x53,0x73,0x49,0x73,0x70,0x69,0x54,0x6f,0x65,0x64,0x69,0x56,0x74,0x61,0x68,0x43,0x65,0x74,0x61,0x76,0x69,0x72,0x50,0x65,0x68,0x63,0x61,0x43]

/*: "Quick greeting can only be sent to him once" :*/
fileprivate let main_componentCrucialTitle:[UInt8] = [0xf6,0x1a,0xe,0x8,0x10,0xc5,0xc,0x17,0xa,0xa,0x19,0xe,0x13,0xc,0xc5,0x8,0x6,0x13,0xc5,0x14,0x13,0x11,0x1e,0xc5,0x7,0xa,0xc5,0x18,0xa,0x13,0x19,0xc5,0x19,0x14,0xc5,0xd,0xe,0x12,0xc5,0x14,0x13,0x8,0xa]

fileprivate func outfitPort(response num: UInt8) -> UInt8 {
    let value = Int(num) - 165
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "txt" :*/
fileprivate let data_exceptFormat:String = "tcheck"

/*: "audio" :*/
fileprivate let mainSearcherMessage:[Character] = ["a","u","d","i","o"]

/*: "Please add greeting text" :*/
fileprivate let kMomentumValue:String = "Pleaway information"
fileprivate let constStartSceneValue:String = "gdeliveryet"
fileprivate let kPeopleSlideKey:String = "skirt observe keying text"

/*: "You have not set up a photo greeting, please set it up before sending!" :*/
fileprivate let appFeedbackFormat:[UInt8] = [0x94,0xaa,0xb0,0x5b,0xa3,0x9c,0xb1,0xa0,0x5b,0xa9,0xaa,0xaf,0x5b,0xae,0xa0,0xaf,0x5b,0xb0,0xab,0x5b,0x9c,0x5b,0xab,0xa3,0xaa,0xaf,0xaa,0x5b,0xa2,0xad,0xa0,0xa0,0xaf,0xa4,0xa9,0xa2,0x67,0x5b,0xab,0xa7,0xa0,0x9c,0xae,0xa0,0x5b,0xae,0xa0,0xaf,0x5b,0xa4,0xaf,0x5b,0xb0,0xab,0x5b,0x9d,0xa0,0xa1,0xaa,0xad,0xa0,0x5b,0xae,0xa0,0xa9,0x9f,0xa4,0xa9,0xa2,0x5c]

fileprivate func equipmentSign(clot num: UInt8) -> UInt8 {
    let value = Int(num) + 197
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let showRhoId:String = "Cancelmain data here own brush"

/*: "Go to set" :*/
fileprivate let user_pinMsg:String = "Go to secycle auto tiff map"
fileprivate let show_questionRagPath:[Character] = ["t"]

/*: "#startTime#" :*/
fileprivate let notiExposeWhoUrl:[Character] = ["#","s","t","a","r","t","T","i","m","e"]
fileprivate let mainDiscloseId:[Character] = ["#"]

/*: "#endTime#" :*/
fileprivate let k_listenerData:[Character] = ["#","e","n","d","T","i","m","e","#"]

/*: "replayId" :*/
fileprivate let userIncomeName:String = "replayIdscript spit"

/*: "text" :*/
fileprivate let const_faceMessage:String = "TEXT"

/*: "img" :*/
fileprivate let kLibUrl:String = "IMG"

/*: "video" :*/
fileprivate let app_portfolioName:[Character] = ["v","i","d","e","o"]

/*: "gift" :*/
fileprivate let userCommandName:[Character] = ["g","i","f","t"]

/*: "PrivateChat 点击引用消息 :*/
fileprivate let main_offMsg:String = "Privinterest in ago"
fileprivate let main_discloseStr:String = "hat \u{70b9}击"
fileprivate let const_othersTimeStr:String = "\u{5f15}\u{7528}消息"

/*: "Sent " :*/
fileprivate let notiAppearanceString:String = "alive choice quantitySent "

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FactoryWriteTranslateFound.swift
//  AbroadTalking
//
//  Created by young on 2023/1/5.
//

//: import UIKit
import UIKit

/// 一键打招呼Block
//: typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void
typealias QuickGreetingBlock = (_ errorCode: Int?, _ errorMsg: String?, _ textDict: [String: Any]?, _ audioDict: [String: Any]?) -> Void

/// 图片打招呼Block
//: typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void
typealias BeautyPhotosBlock = (_ isPushSet: Bool, _ PhotoDict: [String: Any]?) -> Void

//: public class TalkingPrivateChatManager: NSObject {
public class FactoryWriteTranslateFound: NSObject {
    /// 记录私信对端已读回执时间 [key：userID，value：对端最后已读回执时间]
    //: var readReceiptDict = [String: Int]()
    var readReceiptDict = [String: Int]()

    //: @objc static let share = TalkingPrivateChatManager()
    @objc static let share = FactoryWriteTranslateFound()
    //: private override init() {}
    override private init() {}
    // 私密照片已解锁礼物消息数组的key
    //: @objc let UnlockMsgIdArrayKey = "CacheIntimatePhotoUnlockMsgIdArrayKey"
    @objc let UnlockMsgIdArrayKey = String(bytes: constEvenPath.map{addressVolume(step: $0)}, encoding: .utf8)!
    // 问题回答消息已回复消息数组的key
    //: let QuestionReplayMsgIdIdArrayKey = "CacheQuestionReplayMsgIdIdArrayKey"
    let QuestionReplayMsgIdIdArrayKey = String(bytes: dataSecureEvaluateKey.map{agentResult(container: $0)}, encoding: .utf8)!
}

// MARK: - 插入音视频通话提示消息

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 判断是否需要插入音视频通话提示消息
    /// - Parameters:
    ///   - toUid: 对方Id
    ///   - msgView: TUI视图
    ///   - completionHandler: 回调
    //: static func chat_privateChatVideo_insertTipMsg(toUid: String, msgView: TUIMessageController, needInsertHandler: (() -> Void)?) {
    static func begin(toUid: String, msgView: CounteractionTableViewController, needInsertHandler: (() -> Void)?) {
        // 非审核模式
        //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return }
        // 男性
        //: guard ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue else { return }

        // 是否展示过提示文案
        //: let TalkingPrivateChatVideoTipsIsShow = "CachePrivateChatVideoTipsIsShow_\(String(ChartShowSceneScientific.share.loginUserMode.userID))_\(toUid)"
        let TalkingPrivateChatVideoTipsIsShow = String(bytes: notiFrequencyFormat.reversed(), encoding: .utf8)! + "\(String(ChartShowSceneScientific.share.loginUserMode.userID))_\(toUid)"
        //: let isShow = Defaults.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        let isShow = show_modelHideFormat.bool(forKey: TalkingPrivateChatVideoTipsIsShow)
        //: guard isShow == false else { return }
        guard isShow == false else { return }

        // 消息达到5条
        //: guard msgView.uiMsgs.count >= 5 else { return }
        guard msgView.uiMsgs.count >= 5 else { return }

        //: var hasVoiceVideo = false
        var hasVoiceVideo = false // 是否有音视频通话消息
        //: var outgoingMsgCount = 0
        var outgoingMsgCount = 0 // 消息发送方已发送消息数量
        //: for indexModel in msgView.uiMsgs {
        for indexModel in msgView.uiMsgs {
            //: let indexModel = indexModel as? EmbraceShadow
            let indexModel = indexModel as? EmbraceShadow
            //: if indexModel != nil {
            if indexModel != nil {
                //: if indexModel!.isKind(of: VariableChain.self) {
                if indexModel!.isKind(of: VariableChain.self) { // 是否包含音视频文本
                    //: let textMsgModel = indexModel as! VariableChain
                    let textMsgModel = indexModel as! VariableChain
                    //: if textMsgModel.isVoiceVideo == true {
                    if textMsgModel.isVoiceVideo == true {
                        //: hasVoiceVideo = true
                        hasVoiceVideo = true
                        //: break
                        break
                    }
                }

                // 语音消息 || 文本消息 || 图片消息 || 视频消息
                //: if indexModel!.isKind(of: SustainableCenter.self) ||
                if indexModel!.isKind(of: SustainableCenter.self) ||
                    //: indexModel!.isKind(of: VariableChain.self) ||
                    indexModel!.isKind(of: VariableChain.self) ||
                    //: indexModel!.isKind(of: WantText.self) ||
                    indexModel!.isKind(of: WantText.self) ||
                    //: indexModel!.isKind(of: RoundingAsset.self) {
                    indexModel!.isKind(of: RoundingAsset.self)
                {
                    //: if indexModel?.direction == .MsgDirectionOutgoing {
                    if indexModel?.direction == .MsgDirectionOutgoing { // 消息发送方
                        //: outgoingMsgCount += 1
                        outgoingMsgCount += 1
                    }
                }
            }
        }

        //: guard hasVoiceVideo == false else {
        guard hasVoiceVideo == false else {
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            show_modelHideFormat.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            //: return
            return
        }
        //: guard outgoingMsgCount >= 5 else { return }
        guard outgoingMsgCount >= 5 else { return }

        // 回调外部插入音视频提示消息
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!()
            needInsertHandler!()
            //: Defaults.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
            show_modelHideFormat.set(true, forKey: TalkingPrivateChatVideoTipsIsShow)
        }
    }
}

// MARK: - 一键打招呼

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 判断是否可以一键打招呼，回调结果
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据;
    //: static func chat_sendQuickGreeting(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
    static func addHost(toUserId: String, completionHandler: @escaping QuickGreetingBlock) {
        //: let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_\(data_deploySubsequentMsg)_\(toUserId)"
        //: let ishave = UserDefaults.standard.bool(forKey: key)
        let ishave = UserDefaults.standard.bool(forKey: key)
        //: guard !ishave else {
        guard !ishave else {
            //: completionHandler(1, "Quick greeting can only be sent to him once".localized, nil, nil)
            completionHandler(1, String(bytes: main_componentCrucialTitle.map{outfitPort(response: $0)}, encoding: .utf8)!.localized, nil, nil)
            //: return
            return
        }

        //: ProgressHUD.show()
        GrayView.blackCorner()
        //: TalkingChatRequestTool.req_getAutoGreetContent { succeed, result, errorModel in
        NameProjection.petOp { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            GrayView.at()
            //: guard succeed == true else {
            guard succeed == true else {
                //: completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                completionHandler(2, errorModel?.errorMsg ?? "", nil, nil)
                //: return
                return
            }
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: completionHandler(2, "", nil, nil)
                completionHandler(2, "", nil, nil)
                //: return
                return
            }
            //: let textDict = dict["txt"] as? [String: Any]
            let textDict = dict[(data_exceptFormat.replacingOccurrences(of: "check", with: "xt"))] as? [String: Any]
            //: let audioDict = dict["audio"] as? [String: Any]
            let audioDict = dict[(String(mainSearcherMessage))] as? [String: Any]
            //: if textDict?.count == 0 && audioDict?.count == 0 {
            if textDict?.count == 0, audioDict?.count == 0 {
                //: completionHandler(2, "Please add greeting text".localized, nil, nil)
                completionHandler(2, (String(kMomentumValue.prefix(4)) + "se add " + constStartSceneValue.replacingOccurrences(of: "delivery", with: "re") + String(kPeopleSlideKey.suffix(8))).localized, nil, nil)
                //: return
                return
            }

            // 一键打招呼本地存储
            //: chat_changeSaveGreetData(toUserId: toUserId, isHave: true)
            outAppInfo(toUserId: toUserId, isHave: true)
            //: completionHandler(nil, nil, textDict, audioDict)
            completionHandler(nil, nil, textDict, audioDict)
        }
    }

    /// 一键打招呼状态本地存储
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - isHave: 是否已发送
    //: static func chat_changeSaveGreetData(toUserId: String, isHave: Bool) {
    static func outAppInfo(toUserId: String, isHave: Bool) {
        // 一键打招呼本地cunc
        //: let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_\(TalkingSendAutoGreetIsToKey)_\(toUserId)"
        let key = "\(ChartShowSceneScientific.share.loginUserMode.userID)_\(data_deploySubsequentMsg)_\(toUserId)"
        //: UserDefaults.standard.set(isHave, forKey: key)
        UserDefaults.standard.set(isHave, forKey: key)
    }

    /// 图片打招呼
    /// - Parameters:
    ///   - toUserId: 接收方UserId
    ///   - handler: errorCode：1 = 已发送过；2=未添加打招呼数据；
    //: static func chat_sendBeautyPhotos(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
    static func withFraction(toUserId: String, completionHandler: @escaping BeautyPhotosBlock) {
        //: ProgressHUD.show()
        GrayView.blackCorner()
        //: TalkingChatRequestTool.req_getIntimatePhoto(toUid: toUserId) { succeed, result, errorModel in
        NameProjection.enableHard(toUid: toUserId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            GrayView.at()
            /// 未添加打招呼图片
            //: if errorModel?.errorCode == 111 {
            if errorModel?.errorCode == 111 {
                //: let config = ShowAlertConfig()
                let config = MissionField()
                //: config.alignment = .center
                config.alignment = .center
                //: if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
                if TotalSearch.share.interfaceLang == ChainAgile.pt.rawValue {
                    //: config.width = 330
                    config.width = 330
                }
                //: TalkingAlertShow.customAlert(message: "You have not set up a photo greeting, please set it up before sending!".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Go to set".localized, leftBlock: {
                ImmediateThan.belowGrab(message: String(bytes: appFeedbackFormat.map{equipmentSign(clot: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(showRhoId.prefix(6))).localized, rightBtnTitle: (String(user_pinMsg.prefix(8)) + String(show_questionRagPath)).localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    ImmediateThan.mage()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: TalkingAlertShow.hideAlert()
                    ImmediateThan.mage()
                    //: completionHandler(true, nil)
                    completionHandler(true, nil)
                    //: }, config: config)
                }, config: config)
                //: return
                return
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? [String: Any] else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: completionHandler(false, dict)
                completionHandler(false, dict)
            }
        }
    }
}

// MARK: - 插入与官方客服聊天提示消息

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    ///  判断是否需要插入与官方客服聊天提示消息
    /// - Parameter needInsertHandler: 回调
    //: static func chat_customerServiceCenter_insertTipMsg(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
    static func job(needInsertHandler: ((_ tipsStr: String?) -> Void)?) {
        //: guard ChartShowSceneScientific.share.appConfigMode.CSConfig.systemTips.count > 0 else {
        guard ChartShowSceneScientific.share.appConfigMode.CSConfig.systemTips.count > 0 else { // 接口没下发，无需提示
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 根据设备所在时区转换时间
        //: let startTime = NSDate.getCurrentZoneDateString(shDateStr: ChartShowSceneScientific.share.appConfigMode.CSConfig.startTime)
        let startTime = NSDate.yellowStrength(shDateStr: ChartShowSceneScientific.share.appConfigMode.CSConfig.startTime)
        //: let endTime = NSDate.getCurrentZoneDateString(shDateStr: ChartShowSceneScientific.share.appConfigMode.CSConfig.endTime)
        let endTime = NSDate.yellowStrength(shDateStr: ChartShowSceneScientific.share.appConfigMode.CSConfig.endTime)
        //: let isIncluded = NSDate.compareCurrentTimeIsIncluded(startTime: startTime, endTime: endTime)
        let isIncluded = NSDate.outsideUniversal(startTime: startTime, endTime: endTime)

        //: guard isIncluded == false else {
        guard isIncluded == false else {
            //: if needInsertHandler != nil {
            if needInsertHandler != nil {
                //: needInsertHandler!(nil)
                needInsertHandler!(nil)
            }
            //: return
            return
        }

        // 非工作时间段内，添加提示
        //: var tips = ChartShowSceneScientific.share.appConfigMode.CSConfig.systemTips
        var tips = ChartShowSceneScientific.share.appConfigMode.CSConfig.systemTips
        //: tips = tips.replacingOccurrences(of: "#startTime#", with: "\(startTime)")
        tips = tips.replacingOccurrences(of: (String(notiExposeWhoUrl) + String(mainDiscloseId)), with: "\(startTime)")
        //: tips = tips.replacingOccurrences(of: "#endTime#", with: "\(endTime)")
        tips = tips.replacingOccurrences(of: (String(k_listenerData)), with: "\(endTime)")
        //: if needInsertHandler != nil {
        if needInsertHandler != nil {
            //: needInsertHandler!(tips)
            needInsertHandler!(tips)
        }
    }
}

// MARK: - 刷新私密消息（照片、视频）展示状态

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 接收到解锁私密消息（照片、视频）的礼物，刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static func chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: EmbraceShadow, msgView: TUIMessageController) {
    static func substantial(cellData: EmbraceShadow, msgView: CounteractionTableViewController) {
        //: guard msgView.uiMsgs.count > 0 else {
        guard msgView.uiMsgs.count > 0 else {
            // --------- 【外部页面】收到了解锁礼物消息 ---------
            // 首次进入到当前页面，刷新消息状态
            //: var array = Defaults.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String]
            var array = show_modelHideFormat.object(forKey: FactoryWriteTranslateFound.share.UnlockMsgIdArrayKey) as? [String]

            // 本地有私密消息解锁礼物缓存，且是图片消息 或 视频消息
            //: guard array?.count ?? 0 > 0 &&
            guard array?.count ?? 0 > 0,
                  //: (cellData.isKind(of: WantText.self) ||
                  cellData.isKind(of: WantText.self) ||
                  //: cellData.isKind(of: RoundingAsset.self)) else { return }
                  cellData.isKind(of: RoundingAsset.self) else { return }

            // 找到本人发送的私密消息，更新状态为已解锁
            //: if array!.contains(cellData.msgModel.msgInfo.msgId) {
            if array!.contains(cellData.msgModel.msgInfo.msgId) {
                //: inner_changeIntimateMsg(cellData, msgView)
                operateBy(cellData, msgView)

                // 更新本地缓存
                //: array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                array!.removeAll(where: { $0 == cellData.msgModel.msgInfo.msgId })
                //: Defaults.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
                show_modelHideFormat.set(array, forKey: FactoryWriteTranslateFound.share.UnlockMsgIdArrayKey)
            }

            //: return
            return
        }

        //  --------- 在【当前页面】收到解锁礼物消息 ---------
        // 只有收到礼物解锁消息才刷新
        //: guard cellData.isKind(of: FeatureOccasion.self) && cellData.msgModel.gift.unlockMsgId.count > 0 else { return }
        guard cellData.isKind(of: FeatureOccasion.self), cellData.msgModel.gift.unlockMsgId.count > 0 else { return }

        // 找到本人发送的私密消息，更新状态为已解锁
        //: let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        let unlockMsgId = cellData.msgModel.gift.unlockMsgId
        //: for msgCellData in msgView.uiMsgs {
        for msgCellData in msgView.uiMsgs {
            //: let data = msgCellData as! EmbraceShadow
            let data = msgCellData as! EmbraceShadow
            //: if data.msgModel.msgInfo.msgId == unlockMsgId {
            if data.msgModel.msgInfo.msgId == unlockMsgId {
                //: inner_changeIntimateMsg(data, msgView)
                operateBy(data, msgView)
                //: break
                break
            }
        }
    }

    /// 刷新本地消息状态
    /// - Parameters:
    ///   - cellData: 数据
    ///   - msgView: TUI视图
    //: static private func inner_changeIntimateMsg(_ cellData: EmbraceShadow, _ msgView: TUIMessageController) {
    private static func operateBy(_ cellData: EmbraceShadow, _ msgView: CounteractionTableViewController) {
        //: if cellData.isKind(of: WantText.self) {
        if cellData.isKind(of: WantText.self) { // 私密照片
            //: let model = cellData.msgModel.msgInfo.intimatePhoto
            let model = cellData.msgModel.msgInfo.intimatePhoto
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimatePhotoMsg(cellData, photoModel: model)
                msgView.user(cellData, insert: model)
            }

            //: } else if cellData.isKind(of: RoundingAsset.self) {
        } else if cellData.isKind(of: RoundingAsset.self) { // 私密视频
            //: let model = cellData.msgModel.msgInfo.video
            let model = cellData.msgModel.msgInfo.video
            //: if model.lockStatus != 0 {
            if model.lockStatus != 0 {
                //: model.lockStatus = 0
                model.lockStatus = 0
                //: msgView.changeIntimateVideoMsg(cellData, videoModel: model)
                msgView.visit(cellData, history: model)
            }
        }
    }
}

// MARK: - 问题消息快捷回复

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 接收到问题回答消息，判断是否需要展示消息快捷回复弹窗
    /// - Parameters:
    ///   - isRecharge: 是否充值
    ///   - msgView: TUI视图
    ///   - handler: 回调
    //: static func chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: Bool,
    static func body(isRecharge: Bool,
                     //: msgView: TUIMessageController,
                     msgView: CounteractionTableViewController,
                     //: handler: ((AbTalkingChatMsgInfoModel?) -> Void)?) {
                     handler: ((PhoneModel?) -> Void)?)
    {
        //: guard ChartShowSceneScientific.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard ChartShowSceneScientific.share.loginUserMode.sex == SharedOdd.male.rawValue else { return }
        //: guard isRecharge == false else { return }
        guard isRecharge == false else { return }
        // 获取已处理消息ID数组
        //: let cacheMsgIds = Defaults.object(forKey: TalkingPrivateChatManager.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        let cacheMsgIds = show_modelHideFormat.object(forKey: FactoryWriteTranslateFound.share.QuestionReplayMsgIdIdArrayKey) as? [String] ?? []
        //: for case let msgModel as EmbraceShadow in msgView.uiMsgs.reversed() {
        for case let msgModel as EmbraceShadow in msgView.uiMsgs.reversed() {
            //: let msgInfo = msgModel.msgModel.msgInfo
            let msgInfo = msgModel.msgModel.msgInfo
            //: if msgInfo.isEnd && msgInfo.replay.isEmpty {
            if msgInfo.isEnd, msgInfo.replay.isEmpty { // 问答已结束
                //: handler?(nil)
                handler?(nil)
                //: return
                return
            }
            //: if msgInfo.isEnd && !msgInfo.replay.isEmpty && !cacheMsgIds.contains(msgInfo.msgId) {
            if msgInfo.isEnd, !msgInfo.replay.isEmpty, !cacheMsgIds.contains(msgInfo.msgId) { // 找到符合条件问答消息
                //: handler?(msgInfo)
                handler?(msgInfo)
                //: return
                return
            }
        }
        //: handler?(nil)
        handler?(nil)
    }
}

// MARK: - 未回复Reply to get points~逻辑

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 是否是回复获取积分消息
    /// - Parameters:
    ///   - direction: 消息方向
    ///   - msgType: 消息类型
    ///   - msgTime: 消息时间
    /// - Returns: 结果
    //: static func isReplyToGetPointsMsg(_ direction: TMsgDirection, _ msgType: Int, msgTime: Date?) -> Bool {
    static func revenue(_ direction: ThresholdCorner, _ msgType: Int, msgTime: Date?) -> Bool {
        // 非审核模式, messageType == 5, 时间<24小时
        //: guard ChartShowSceneScientific.share.appStatus == AppSkinStatus.normal.rawValue else { return false }
        guard ChartShowSceneScientific.share.appStatus == SophisticatedMultiple.normal.rawValue else { return false }
        //: guard direction == .MsgDirectionIncoming else { return false }
        guard direction == .MsgDirectionIncoming else { return false }
        //: guard msgType == 5 else { return false }
        guard msgType == 5 else { return false }
        //: guard let msgTime = msgTime else { return false }
        guard let msgTime = msgTime else { return false }
        //: guard Date().timeIntervalSince(msgTime) < 24*60*60 else { return false }
        guard Date().timeIntervalSince(msgTime) < 24 * 60 * 60 else { return false }
        //: return true
        return true
    }
}

// MARK: - VIP展示消息已读逻辑

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 是否需要插入已读回执引导充值VIP提醒消息
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func needInsertVIPReadReceiptMsg(cellData: EmbraceShadow) -> Bool {
    static func conversation(cellData: EmbraceShadow) -> Bool {
        //: if ChartShowSceneScientific.share.loginUserMode.loungePlus == false,
        if ChartShowSceneScientific.share.loginUserMode.loungePlus == false,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 是否展示消息 已读/未读 标识
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func isShowReadMsg(cellData: EmbraceShadow) -> Bool {
    static func visualSatisfy(cellData: EmbraceShadow) -> Bool {
        // vip && 非群聊 && 发送成功 && 本人发送 && 非音视频通话文本消息，展示消息是否已读标识
        //: if ChartShowSceneScientific.share.loginUserMode.loungePlus == true,
        if ChartShowSceneScientific.share.loginUserMode.loungePlus == true,
           //: cellData.innerMessage.groupID == nil,
           cellData.innerMessage.groupID == nil,
           //: cellData.status == .Msg_Status_Succ,
           cellData.status == .Msg_Status_Succ,
           //: cellData.direction == .MsgDirectionOutgoing,
           cellData.direction == .MsgDirectionOutgoing,
           //: cellData.msgModel.msgInfo.callCmd.count <= 0 {
           cellData.msgModel.msgInfo.callCmd.count <= 0
        {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 消息对方是否已读
    /// - Parameter cellData: 消息数据
    /// - Returns: 结果
    //: static func msgIsRead(cellData: EmbraceShadow) -> Bool {
    static func overConsumption(cellData: EmbraceShadow) -> Bool {
        //: if cellData.msgModel.params["replayId"] != nil { // 问题回答消息默认已读
        if cellData.msgModel.params[(String(userIncomeName.prefix(8)))] != nil { // 问题回答消息默认已读
            //: return true
            return true
        }
        //: guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        guard let selfMsgTime = cellData.innerMessage.timestamp else { return false }
        //: if cellData.innerMessage.isPeerRead == true {
        if cellData.innerMessage.isPeerRead == true {
            //: return true
            return true
        }
        //: let readTimeInterval = TalkingPrivateChatManager.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        let readTimeInterval = FactoryWriteTranslateFound.share.readReceiptDict[cellData.innerMessage.userID] ?? 0
        //: let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        let readTime = Date(timeIntervalSince1970: TimeInterval(readTimeInterval))
        //: if selfMsgTime <= readTime {
        if selfMsgTime <= readTime {
            //: return true
            return true
        }
        //: return false
        return false
    }
}

// MARK: - Reply消息逻辑

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 回复消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    /// - Returns: 引用消息模型
    //: static func handleReplyMsg(cellData: EmbraceShadow, targetId: String) -> AbTalkingChatMsgQuoteModel {
    static func numbero(cellData: EmbraceShadow, targetId: String) -> FrameworkTimePlainModel {
        //: let quoteModel = AbTalkingChatMsgQuoteModel()
        let quoteModel = FrameworkTimePlainModel()
        //: quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        quoteModel.msgId = cellData.msgModel.msgInfo.msgId
        //: if cellData.direction == .MsgDirectionIncoming {
        if cellData.direction == .MsgDirectionIncoming {
            //: quoteModel.uid = targetId
            quoteModel.uid = targetId
            //: } else {
        } else {
            //: quoteModel.uid = ChartShowSceneScientific.share.loginUserMode.userID
            quoteModel.uid = ChartShowSceneScientific.share.loginUserMode.userID
        }
        //: let renderData = AbTalkingChatMsgQuoteDataModel()
        let renderData = ScientificHandleModel()
        //: if cellData.isKind(of: VariableChain.self) {
        if cellData.isKind(of: VariableChain.self) {
            //: quoteModel.renderType = "text"
            quoteModel.renderType = (const_faceMessage.lowercased())
            //: renderData.content = cellData.msgModel.msgInfo.content
            renderData.content = cellData.msgModel.msgInfo.content

            //: } else if cellData.isKind(of: WantText.self) {
        } else if cellData.isKind(of: WantText.self) {
            //: quoteModel.renderType = "img"
            quoteModel.renderType = (kLibUrl.lowercased())
            //: renderData.previewUrl = cellData.msgModel.msgInfo.imageUri
            renderData.previewUrl = cellData.msgModel.msgInfo.imageUri

            //: } else if cellData.isKind(of: RoundingAsset.self) {
        } else if cellData.isKind(of: RoundingAsset.self) {
            //: quoteModel.renderType = "video"
            quoteModel.renderType = (String(app_portfolioName))
            //: renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            renderData.videoUrl = cellData.msgModel.msgInfo.video.videoUrl
            //: renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg
            renderData.coverUrl = cellData.msgModel.msgInfo.video.coverImg

            //: } else if cellData.isKind(of: SustainableCenter.self) {
        } else if cellData.isKind(of: SustainableCenter.self) {
            //: let audioCelldata = cellData as! SustainableCenter
            let audioCelldata = cellData as! SustainableCenter
            //: quoteModel.renderType = "audio"
            quoteModel.renderType = (String(mainSearcherMessage))
            //: let voiceCache = DBUserInfoManager.cache_getAudioDB(message: audioCelldata.innerMessage)
            let voiceCache = TranslateScreen.alwaysA(message: audioCelldata.innerMessage)
            //: renderData.duration = Int(voiceCache.db_audioLength) ?? 0
            renderData.duration = Int(voiceCache.db_audioLength) ?? 0

            //: } else if cellData.isKind(of: FeatureOccasion.self) {
        } else if cellData.isKind(of: FeatureOccasion.self) {
            //: quoteModel.renderType = "gift"
            quoteModel.renderType = (String(userCommandName))
            //: let giftCellData = cellData as! FeatureOccasion
            let giftCellData = cellData as! FeatureOccasion
            //: renderData.giftName = giftCellData.msgModel.gift.name
            renderData.giftName = giftCellData.msgModel.gift.name
            //: renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            renderData.giftNameI18n = giftCellData.msgModel.gift.nameI18n
            //: renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            renderData.giftIcon = giftCellData.msgModel.gift.imgPreview
            //: renderData.giftPrice = giftCellData.msgModel.gift.price
            renderData.giftPrice = giftCellData.msgModel.gift.price
            //: renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
            renderData.giftNum = Int(giftCellData.msgModel.gift.num) ?? 0
        }

        //: quoteModel.renderData = renderData
        quoteModel.renderData = renderData
        //: return quoteModel
        return quoteModel
    }

    /// 点击“引用”消息
    /// - Parameters:
    ///   - cellData: 数据
    ///   - targetId: 对方uid
    //: static func handleClickReplyMsg(cellData: EmbraceShadow, targetId: String) {
    static func album(cellData: EmbraceShadow, targetId: String) {
        //: let renderData = cellData.msgModel.quoteMsgInfo
        let renderData = cellData.msgModel.quoteMsgInfo
        //: UploadLogTool.writeLog(msg: "PrivateChat 点击引用消息\(renderData.renderType).")
        KitTolerance.evenIndex(msg: (String(main_offMsg.prefix(4)) + "ateC" + main_discloseStr + const_othersTimeStr) + "\(renderData.renderType).")
        //: if renderData.renderType == "text" {
        if renderData.renderType == (const_faceMessage.lowercased()) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = ContextPriorImage(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
            //: popView.msgDetailText = renderData.renderData.content
            popView.msgDetailText = renderData.renderData.content
            //: popView.show()
            popView.chapter()

            //: } else if renderData.renderType == "img" {
        } else if renderData.renderType == (kLibUrl.lowercased()) {
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(user_availableValue)
            //: filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((renderData.renderData.previewUrl as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = SendSmall()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: model.imgUrl =  renderData.renderData.previewUrl
                model.imgUrl = renderData.renderData.previewUrl
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = FinishEdge()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [SendSmall] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = DistributionNorm(momentModel: currenModel, index: 0, type: .normal)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            FactoryWriteTranslateFound.share.teeHoney()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "video" {
        } else if renderData.renderType == (String(app_portfolioName)) {
            //: let videoPath = renderData.renderData.videoUrl
            let videoPath = renderData.renderData.videoUrl
            //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
            let vc = AroundSumegrity(videoPath: videoPath)
            //: TalkingPrivateChatManager.share.getNavigationController()?.pushViewController(vc, animated: true)
            FactoryWriteTranslateFound.share.teeHoney()?.pushViewController(vc, animated: true)

            //: } else if renderData.renderType == "audio" {
        } else if renderData.renderType == (String(mainSearcherMessage)) {
            //: let cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: renderData.msgId)
            let cacheWrap = MoveBar.speakerDiscount(with: renderData.msgId)
            //: let playModel = TalkingVoiceMsgPlayModel()
            let playModel = RefreshReceiver()
            //: if cacheWrap != nil {
            if cacheWrap != nil {
                //: playModel.db_voiceCacheWrap = cacheWrap!
                playModel.db_voiceCacheWrap = cacheWrap!

                //: } else {
            } else {
                //: let model = WCDBVoiceMsgTable()
                let model = MoveBar()
                //: model.msgId = renderData.msgId
                model.msgId = renderData.msgId
                //: model.db_voiceUri = renderData.renderData.url
                model.db_voiceUri = renderData.renderData.url
                //: model.db_senduid = renderData.uid
                model.db_senduid = renderData.uid
                //: if renderData.uid == ChartShowSceneScientific.share.loginUserMode.userID {
                if renderData.uid == ChartShowSceneScientific.share.loginUserMode.userID {
                    //: model.db_touid = targetId
                    model.db_touid = targetId
                    //: } else {
                } else {
                    //: model.db_touid = ChartShowSceneScientific.share.loginUserMode.userID
                    model.db_touid = ChartShowSceneScientific.share.loginUserMode.userID
                }
                //: model.db_audioLength = "\(renderData.renderData.duration)"
                model.db_audioLength = "\(renderData.renderData.duration)"
                //: WCDBVoiceMsgTable.db_insertVoiceMsg(model)
                MoveBar.dailyWrapCap(model)
                //: playModel.db_voiceCacheWrap = model
                playModel.db_voiceCacheWrap = model
            }

            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SkipEnableWait.shared.quit()
            //: TalkingVoiceMsgPlayManager.shared.starPlayReplyMsg(playModel: playModel)
            SkipEnableWait.shared.multi(playModel: playModel)

            //: } else if renderData.renderType == "gift" {
        } else if renderData.renderType == (String(userCommandName)) {
            //: let popView = QuoteDetailPopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let popView = ContextPriorImage(frame: CGRect(x: 0, y: 0, width: show_licenseTitle, height: dataChainValue))
            //: popView.msgDetailText = "Sent ".localized + renderData.renderData.giftNameLocal() + " x\(renderData.renderData.giftNum)"
            popView.msgDetailText = (String(notiAppearanceString.suffix(5))).localized + renderData.renderData.lightLength() + " x\(renderData.renderData.giftNum)"
            //: popView.show()
            popView.chapter()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatManager {
extension FactoryWriteTranslateFound {
    /// 检测用户短视频版本是否可用【> = 1.1.6 可用】
    /// - Returns: 是否可用
    //: static func checkShortVideo(userVersion: String) -> Bool {
    static func dayLocal(userVersion: String) -> Bool {
        //: guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
        guard let version = Int(userVersion.replacingOccurrences(of: ".", with: "")) else {
            //: return false
            return false
        }
        //: if version*100 < 116*100 {
        if version * 100 < 116 * 100 {
            //: return false
            return false
        }

        //: return true
        return true
    }
}
